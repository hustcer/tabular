// Generated by scripts/import-transform-tests.nu.
// Source revision: 6f40434650aa0c8a27ecb243d42af605b4206b73

///|
// Upstream: tabled/tests/settings/reverse_test.rs::test_0x0_reverse_rows
test "upstream reverse test_0x0_reverse_rows" {
  let actual = @tabular.Table::from_rows([])
    .reverse(@tabular.Reverse::rows(0))
    .to_string()
  assert_eq(actual, "")
}

///|
// Upstream: tabled/tests/settings/reverse_test.rs::test_0x0_reverse_columns
test "upstream reverse test_0x0_reverse_columns" {
  let actual = @tabular.Table::from_rows([])
    .reverse(@tabular.Reverse::cols(0))
    .to_string()
  assert_eq(actual, "")
}

///|
// Upstream: tabled/tests/settings/reverse_test.rs::test_3x3_reverse_rows
test "upstream reverse test_3x3_reverse_rows" {
  let actual = upstream_int_table([[123, 456, 789], [234, 567, 891]])
    .reverse(@tabular.Reverse::rows(0))
    .to_string()
  assert_eq(
    actual,
    (
      #|+-----+-----+-----+
      #|| 234 | 567 | 891 |
      #|+-----+-----+-----+
      #|| 123 | 456 | 789 |
      #|+-----+-----+-----+
      #|| i32 | i32 | i32 |
      #|+-----+-----+-----+
    ),
  )
}

///|
// Upstream: tabled/tests/settings/reverse_test.rs::test_3x3_reverse_rows_skip_start
test "upstream reverse test_3x3_reverse_rows_skip_start" {
  let actual = upstream_int_table([[123, 456, 789], [234, 567, 891]])
    .reverse(@tabular.Reverse::rows(1))
    .to_string()
  assert_eq(
    actual,
    (
      #|+-----+-----+-----+
      #|| i32 | i32 | i32 |
      #|+-----+-----+-----+
      #|| 234 | 567 | 891 |
      #|+-----+-----+-----+
      #|| 123 | 456 | 789 |
      #|+-----+-----+-----+
    ),
  )
}

///|
// Upstream: tabled/tests/settings/reverse_test.rs::test_3x3_reverse_rows_skip_end
test "upstream reverse test_3x3_reverse_rows_skip_end" {
  let actual = upstream_int_table([[123, 456, 789], [234, 567, 891]])
    .reverse(@tabular.Reverse::rows(0).exclude_last(1))
    .to_string()
  assert_eq(
    actual,
    (
      #|+-----+-----+-----+
      #|| 123 | 456 | 789 |
      #|+-----+-----+-----+
      #|| i32 | i32 | i32 |
      #|+-----+-----+-----+
      #|| 234 | 567 | 891 |
      #|+-----+-----+-----+
    ),
  )
}

///|
// Upstream: tabled/tests/settings/reverse_test.rs::test_3x3_reverse_rows_skip_end_0
test "upstream reverse test_3x3_reverse_rows_skip_end_0" {
  let actual = upstream_int_table([
      [123, 456, 789],
      [234, 567, 891],
      [345, 678, 912],
    ])
    .reverse(@tabular.Reverse::rows(1).take(2))
    .to_string()
  assert_eq(
    actual,
    (
      #|+-----+-----+-----+
      #|| i32 | i32 | i32 |
      #|+-----+-----+-----+
      #|| 234 | 567 | 891 |
      #|+-----+-----+-----+
      #|| 123 | 456 | 789 |
      #|+-----+-----+-----+
      #|| 345 | 678 | 912 |
      #|+-----+-----+-----+
    ),
  )
}

///|
// Upstream: tabled/tests/settings/reverse_test.rs::test_4x4_reverse_rows_skip_start_and_end
test "upstream reverse test_4x4_reverse_rows_skip_start_and_end" {
  let actual = upstream_int_table([
      [123, 456, 789],
      [234, 567, 891],
      [345, 678, 901],
    ])
    .reverse(@tabular.Reverse::rows(1).exclude_last(1))
    .to_string()
  assert_eq(
    actual,
    (
      #|+-----+-----+-----+
      #|| i32 | i32 | i32 |
      #|+-----+-----+-----+
      #|| 234 | 567 | 891 |
      #|+-----+-----+-----+
      #|| 123 | 456 | 789 |
      #|+-----+-----+-----+
      #|| 345 | 678 | 901 |
      #|+-----+-----+-----+
    ),
  )
}

///|
// Upstream: tabled/tests/settings/reverse_test.rs::test_3x3_reverse_columns
test "upstream reverse test_3x3_reverse_columns" {
  let actual = upstream_int_table([[123, 456, 789], [234, 567, 891]])
    .reverse(@tabular.Reverse::cols(0))
    .to_string()
  assert_eq(
    actual,
    (
      #|+-----+-----+-----+
      #|| i32 | i32 | i32 |
      #|+-----+-----+-----+
      #|| 789 | 456 | 123 |
      #|+-----+-----+-----+
      #|| 891 | 567 | 234 |
      #|+-----+-----+-----+
    ),
  )
}

///|
// Upstream: tabled/tests/settings/reverse_test.rs::test_3x3_reverse_columns_skip_start
test "upstream reverse test_3x3_reverse_columns_skip_start" {
  let actual = upstream_int_table([[123, 456, 789], [234, 567, 891]])
    .reverse(@tabular.Reverse::cols(1))
    .to_string()
  assert_eq(
    actual,
    (
      #|+-----+-----+-----+
      #|| i32 | i32 | i32 |
      #|+-----+-----+-----+
      #|| 123 | 789 | 456 |
      #|+-----+-----+-----+
      #|| 234 | 891 | 567 |
      #|+-----+-----+-----+
    ),
  )
}

///|
// Upstream: tabled/tests/settings/reverse_test.rs::test_3x3_reverse_columns_skip_end
test "upstream reverse test_3x3_reverse_columns_skip_end" {
  let actual = upstream_int_table([[123, 456, 789], [234, 567, 891]])
    .reverse(@tabular.Reverse::cols(0).exclude_last(1))
    .to_string()
  assert_eq(
    actual,
    (
      #|+-----+-----+-----+
      #|| i32 | i32 | i32 |
      #|+-----+-----+-----+
      #|| 456 | 123 | 789 |
      #|+-----+-----+-----+
      #|| 567 | 234 | 891 |
      #|+-----+-----+-----+
    ),
  )
}

///|
// Upstream: tabled/tests/settings/reverse_test.rs::test_3x3_reverse_columns_skip_end_0
test "upstream reverse test_3x3_reverse_columns_skip_end_0" {
  let actual = upstream_int_table([
      [123, 456, 789],
      [234, 567, 891],
      [345, 678, 912],
    ])
    .reverse(@tabular.Reverse::cols(0).take(2))
    .to_string()
  assert_eq(
    actual,
    (
      #|+-----+-----+-----+
      #|| i32 | i32 | i32 |
      #|+-----+-----+-----+
      #|| 456 | 123 | 789 |
      #|+-----+-----+-----+
      #|| 567 | 234 | 891 |
      #|+-----+-----+-----+
      #|| 678 | 345 | 912 |
      #|+-----+-----+-----+
    ),
  )
}

///|
// Upstream: tabled/tests/settings/reverse_test.rs::test_4x3_reverse_columns_skip_start_and_end
test "upstream reverse test_4x3_reverse_columns_skip_start_and_end" {
  let actual = upstream_int_table([[123, 456, 789, 123], [234, 567, 891, 234]])
    .reverse(@tabular.Reverse::cols(1).exclude_last(1))
    .to_string()
  assert_eq(
    actual,
    (
      #|+-----+-----+-----+-----+
      #|| i32 | i32 | i32 | i32 |
      #|+-----+-----+-----+-----+
      #|| 123 | 789 | 456 | 123 |
      #|+-----+-----+-----+-----+
      #|| 234 | 891 | 567 | 234 |
      #|+-----+-----+-----+-----+
    ),
  )
}
