// Generated by scripts/import-width-state-tests.nu.

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference list" {
  let table = upstream_width_state("list")
  assert_eq(
    table.to_string(),
    "+--------+-----+\n| abc    | xy  |\n+--------+-----+\n| de     | z   |\n+--------+-----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), Some([8, 5]))
  assert_eq(table.dimension_snapshot().get_heights(), None)
  assert_eq(table.total_width(), 16)
  assert_eq(table.total_height(), 5)
  assert_eq(table.rows, [["abc", "xy"], ["de", "z"]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference long-list" {
  let table = upstream_width_state("long-list")
  assert_eq(
    table.to_string(),
    "+--------+-----+\n| abc    | xy  |\n+--------+-----+\n| de     | z   |\n+--------+-----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), Some([8, 5, 99]))
  assert_eq(table.dimension_snapshot().get_heights(), None)
  assert_eq(table.total_width(), 16)
  assert_eq(table.total_height(), 5)
  assert_eq(table.rows, [["abc", "xy"], ["de", "z"]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference short-list" {
  let table = upstream_width_state("short-list")
  assert_eq(
    table.to_string(),
    "+--------+-----+\n| abc    | xy  |\n+--------+-----+\n| de     | z   |\n+--------+-----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), Some([8, 5]))
  assert_eq(table.dimension_snapshot().get_heights(), None)
  assert_eq(table.total_width(), 16)
  assert_eq(table.total_height(), 5)
  assert_eq(table.rows, [["abc", "xy"], ["de", "z"]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference align" {
  let table = upstream_width_state("align")
  assert_eq(
    table.to_string(),
    "+--------+-----+\n|    abc |  xy |\n+--------+-----+\n|     de |   z |\n+--------+-----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), Some([8, 5]))
  assert_eq(table.dimension_snapshot().get_heights(), None)
  assert_eq(table.total_width(), 16)
  assert_eq(table.total_height(), 5)
  assert_eq(table.rows, [["abc", "xy"], ["de", "z"]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference cell-align" {
  let table = upstream_width_state("cell-align")
  assert_eq(
    table.to_string(),
    "+-----+----+\n| abc | xy |\n+-----+----+\n| de  | z  |\n+-----+----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), None)
  assert_eq(table.dimension_snapshot().get_heights(), None)
  assert_eq(table.total_width(), 12)
  assert_eq(table.total_height(), 5)
  assert_eq(table.rows, [["abc", "xy"], ["de", "z"]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference settings" {
  let table = upstream_width_state("settings")
  assert_eq(
    table.to_string(),
    "+--------+-----+\n|    abc |  xy |\n+--------+-----+\n|     de |   z |\n+--------+-----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), Some([8, 5]))
  assert_eq(table.dimension_snapshot().get_heights(), None)
  assert_eq(table.total_width(), 16)
  assert_eq(table.total_height(), 5)
  assert_eq(table.rows, [["abc", "xy"], ["de", "z"]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference array" {
  let table = upstream_width_state("array")
  assert_eq(
    table.to_string(),
    "+-----+----+\n| abc | xy |\n+-----+----+\n| de  | z  |\n+-----+----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), None)
  assert_eq(table.dimension_snapshot().get_heights(), None)
  assert_eq(table.total_width(), 12)
  assert_eq(table.total_height(), 5)
  assert_eq(table.rows, [["abc", "xy"], ["de", "z"]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference nested" {
  let table = upstream_width_state("nested")
  assert_eq(
    table.to_string(),
    "+--------------------+----+\n| a very long string | xy |\n+--------------------+----+\n| de                 | z  |\n+--------------------+----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), None)
  assert_eq(table.dimension_snapshot().get_heights(), None)
  assert_eq(table.total_width(), 27)
  assert_eq(table.total_height(), 5)
  assert_eq(table.rows, [["a very long string", "xy"], ["de", "z"]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference record-wrap" {
  let table = upstream_width_state("record-wrap")
  assert_eq(
    table.to_string(),
    "+----+----+\n| ab | xy |\n| c  |    |\n+----+----+\n| de | z  |\n+----+----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), None)
  assert_eq(table.dimension_snapshot().get_heights(), None)
  assert_eq(table.total_width(), 11)
  assert_eq(table.total_height(), 6)
  assert_eq(table.rows, [["ab\nc", "xy"], ["de", "z"]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference record-increase" {
  let table = upstream_width_state("record-increase")
  assert_eq(
    table.to_string(),
    "+-------+-------+\n| abc.. | xy... |\n+-------+-------+\n| de... | z.... |\n+-------+-------+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), None)
  assert_eq(table.dimension_snapshot().get_heights(), None)
  assert_eq(table.total_width(), 17)
  assert_eq(table.total_height(), 5)
  assert_eq(table.rows, [["abc..", "xy..."], ["de...", "z...."]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference max" {
  let table = upstream_width_state("max")
  assert_eq(
    table.to_string(),
    "+-----+-----+\n| abc | xy  |\n+-----+-----+\n| de  | z   |\n+-----+-----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), None)
  assert_eq(table.dimension_snapshot().get_heights(), None)
  assert_eq(table.total_width(), 13)
  assert_eq(table.total_height(), 5)
  assert_eq(table.rows, [["abc", "xy "], ["de ", "z  "]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference min" {
  let table = upstream_width_state("min")
  assert_eq(
    table.to_string(),
    "+----+----+\n| ab | xy |\n+----+----+\n| de | z  |\n+----+----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), None)
  assert_eq(table.dimension_snapshot().get_heights(), None)
  assert_eq(table.total_width(), 11)
  assert_eq(table.total_height(), 5)
  assert_eq(table.rows, [["ab", "xy"], ["de", "z"]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference percent" {
  let table = upstream_width_state("percent")
  assert_eq(
    table.to_string(),
    "+---+---+\n| a | x |\n| b | y |\n| c |   |\n+---+---+\n| d | z |\n| e |   |\n+---+---+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), Some([3, 3]))
  assert_eq(table.dimension_snapshot().get_heights(), None)
  assert_eq(table.total_width(), 9)
  assert_eq(table.total_height(), 8)
  assert_eq(table.rows, [["a\nb\nc", "x\ny"], ["d\ne", "z"]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference percent-increase" {
  let table = upstream_width_state("percent-increase")
  assert_eq(
    table.to_string(),
    "+-----------+----------+\n| abc       | xy       |\n+-----------+----------+\n| de        | z        |\n+-----------+----------+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), Some([11, 10]))
  assert_eq(table.dimension_snapshot().get_heights(), Some([1, 1]))
  assert_eq(table.total_width(), 24)
  assert_eq(table.total_height(), 5)
  assert_eq(table.rows, [["abc", "xy"], ["de", "z"]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference minimum-priority" {
  let table = upstream_width_state("minimum-priority")
  assert_eq(
    table.to_string(),
    "+---------+--------+\n| abc     | xy     |\n+---------+--------+\n| de      | z      |\n+---------+--------+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), Some([9, 8]))
  assert_eq(table.dimension_snapshot().get_heights(), Some([1, 1]))
  assert_eq(table.total_width(), 20)
  assert_eq(table.total_height(), 5)
  assert_eq(table.rows, [["abc", "xy"], ["de", "z"]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference maximum-priority" {
  let table = upstream_width_state("maximum-priority")
  assert_eq(
    table.to_string(),
    "+-------------+----+\n| abc         | xy |\n+-------------+----+\n| de          | z  |\n+-------------+----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), Some([13, 4]))
  assert_eq(table.dimension_snapshot().get_heights(), Some([1, 1]))
  assert_eq(table.total_width(), 20)
  assert_eq(table.total_height(), 5)
  assert_eq(table.rows, [["abc", "xy"], ["de", "z"]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference justify-max" {
  let table = upstream_width_state("justify-max")
  assert_eq(
    table.to_string(),
    "+-----+-----+\n| abc | xy  |\n+-----+-----+\n| de  | z   |\n+-----+-----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), None)
  assert_eq(table.dimension_snapshot().get_heights(), None)
  assert_eq(table.total_width(), 13)
  assert_eq(table.total_height(), 5)
  assert_eq(table.rows, [["abc", "xy "], ["de ", "z  "]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference justify-min" {
  let table = upstream_width_state("justify-min")
  assert_eq(
    table.to_string(),
    "+----+----+\n| ab | xy |\n+----+----+\n| de | z  |\n+----+----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), None)
  assert_eq(table.dimension_snapshot().get_heights(), None)
  assert_eq(table.total_width(), 11)
  assert_eq(table.total_height(), 5)
  assert_eq(table.rows, [["ab", "xy"], ["de", "z "]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference mixed-padding" {
  let table = upstream_width_state("mixed-padding")
  assert_eq(
    table.to_string(),
    "+------+-----+\n|      | xy  |\n|      |     |\n| abc  |     |\n|      |     |\n+------+-----+\n| de   | z   |\n+------+-----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), Some([6, 5]))
  assert_eq(table.dimension_snapshot().get_heights(), Some([4, 1]))
  assert_eq(table.total_width(), 14)
  assert_eq(table.total_height(), 8)
  assert_eq(table.rows, [["abc", "xy"], ["de", "z"]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference row-span" {
  let table = upstream_width_state("row-span")
  assert_eq(
    table.to_string(),
    "+------+-----+\n| abc  | xy  |\n+      +-----+\n|      | z   |\n+------+-----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), Some([6, 5]))
  assert_eq(table.dimension_snapshot().get_heights(), Some([1, 1]))
  assert_eq(table.total_width(), 14)
  assert_eq(table.total_height(), 5)
  assert_eq(table.rows, [["abc", "xy"], ["a very long hidden value", "z"]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference crlf" {
  let table = upstream_width_state("crlf")
  assert_eq(
    table.to_string(),
    "+-----+----+\n| a\r  | xy |\n| b\r  |    |\n|     |    |\n+-----+----+\n| de  | z  |\n+-----+----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), Some([5, 4]))
  assert_eq(table.dimension_snapshot().get_heights(), Some([3, 1]))
  assert_eq(table.total_width(), 12)
  assert_eq(table.total_height(), 7)
  assert_eq(table.rows, [["a\r\nb\r\n", "xy"], ["de", "z"]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference heights" {
  let table = upstream_width_state("heights")
  assert_eq(
    table.to_string(),
    "+-----+----+\n| abc | xy |\n|     |    |\n|     |    |\n+-----+----+\n| de  | z  |\n|     |    |\n+-----+----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), None)
  assert_eq(table.dimension_snapshot().get_heights(), Some([3, 2]))
  assert_eq(table.total_width(), 12)
  assert_eq(table.total_height(), 8)
  assert_eq(table.rows, [["abc", "xy"], ["de", "z"]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference height-row-hint" {
  let table = upstream_width_state("height-row-hint")
  assert_eq(
    table.to_string(),
    "+-----+----+\n| abc | xy |\n+-----+----+\n| de  | z  |\n+-----+----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), Some([5, 4]))
  assert_eq(table.dimension_snapshot().get_heights(), None)
  assert_eq(table.total_width(), 12)
  assert_eq(table.total_height(), 5)
  assert_eq(table.rows, [["abc", "xy"], ["de", "z"]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference height-no-hint" {
  let table = upstream_width_state("height-no-hint")
  assert_eq(
    table.to_string(),
    "+-----+----+\n| abc | xy |\n|     |    |\n|     |    |\n+-----+----+\n| de  | z  |\n|     |    |\n+-----+----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), Some([5, 4]))
  assert_eq(table.dimension_snapshot().get_heights(), Some([3, 2]))
  assert_eq(table.total_width(), 12)
  assert_eq(table.total_height(), 8)
  assert_eq(table.rows, [["abc", "xy"], ["de", "z"]])
}

///|
// Reference: tabled width options and CompleteDimension at the pinned source revision.
test "width state reference color" {
  let table = upstream_width_state("color")
  assert_eq(
    table.to_string(),
    "+--------+-----+\n| \u001b[31mabc\u001b[39m    | \u001b[31mxy\u001b[39m  |\n+--------+-----+\n| \u001b[31mde\u001b[39m     | \u001b[31mz\u001b[39m   |\n+--------+-----+",
  )
  assert_eq(table.dimension_snapshot().get_widths(), Some([8, 5]))
  assert_eq(table.dimension_snapshot().get_heights(), None)
  assert_eq(table.total_width(), 16)
  assert_eq(table.total_height(), 5)
  assert_eq(table.rows, [["abc", "xy"], ["de", "z"]])
}
