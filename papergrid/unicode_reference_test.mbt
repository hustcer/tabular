// Generated by scripts/import-unicode-width.nu.

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 0" {
  assert_eq(get_string_width("ｈｅｌｌｏ"), 10)
  assert_eq(get_string_width("\u0000\u0000\u0000\u0001\u0001"), 5)
  assert_eq(get_string_width(""), 0)
  assert_eq(get_string_width("₁₂₃₄"), 4)
  assert_eq(get_string_width("👩"), 2)
  assert_eq(get_string_width("🔬"), 2)
  assert_eq(get_string_width("👩‍🔬"), 2)
  assert_eq(get_string_width("क"), 1)
  assert_eq(get_string_width("ष"), 1)
  assert_eq(get_string_width("क्ष"), 2)
  assert_eq(get_string_width("·"), 1)
  assert_eq(get_string_width("·"), 1)
  assert_eq(get_string_width("¨"), 1)
  assert_eq(get_string_width("ˉ"), 1)
  assert_eq(get_string_width("ൎഉ"), 1)
  assert_eq(get_string_width("𑪉"), 0)
  assert_eq(get_string_width("≮"), 1)
  assert_eq(get_string_width("≠"), 1)
  assert_eq(get_string_width("≯"), 1)
  assert_eq(get_string_width("≠́"), 1)
  assert_eq(get_string_width("≠́"), 1)
  assert_eq(get_string_width("=️̸"), 1)
  assert_eq(get_string_width("#️̸"), 2)
  assert_eq(get_string_width("#̸️"), 1)
  assert_eq(get_string_width("ڸ̸ا"), 1)
  assert_eq(get_string_width("ڸ̸︎ا"), 1)
  assert_eq(get_string_width("ڸ̸️ا"), 1)
  assert_eq(get_string_width("ڸ︎̸ا"), 1)
  assert_eq(get_string_width("ڸ️̸ا"), 1)
  assert_eq(get_string_width("≠ا"), 2)
  assert_eq(get_string_width("#️"), 2)
  assert_eq(get_string_width("a#️a"), 4)
  assert_eq(get_string_width("#a️"), 2)
  assert_eq(get_string_width("a️"), 1)
  assert_eq(get_string_width("##️a"), 4)
  assert_eq(get_string_width("*️"), 2)
  assert_eq(get_string_width("⏹️"), 2)
  assert_eq(get_string_width("Ⓜ️"), 2)
  assert_eq(get_string_width("🛳️"), 2)
  assert_eq(get_string_width("🜀️"), 1)
  assert_eq(get_string_width("*́️"), 1)
  assert_eq(get_string_width("*‍️"), 1)
  assert_eq(get_string_width("*︎️"), 1)
  assert_eq(get_string_width("♈︎"), 1)
  assert_eq(get_string_width("♈︎️"), 1)
  assert_eq(get_string_width("♈️︎"), 2)
  assert_eq(get_string_width("🈚︎"), 2)
  assert_eq(get_string_width("́︎"), 0)
  assert_eq(get_string_width("a︎"), 1)
  assert_eq(get_string_width("𘀀︎"), 2)
  assert_eq(get_string_width("♈́︎"), 2)
  assert_eq(get_string_width("♈‍︎"), 2)
  assert_eq(get_string_width("\r"), 1)
  assert_eq(get_string_width("\n"), 1)
  assert_eq(get_string_width("\r\n"), 1)
  assert_eq(get_string_width("\u0000"), 1)
  assert_eq(get_string_width("1\t2\r\n34"), 7)
  assert_eq(get_string_width("\r️\n"), 2)
  assert_eq(get_string_width("\r‍\n"), 2)
  assert_eq(get_string_width("ꓪꓹ"), 2)
  assert_eq(get_string_width("ꓪꓹꓼ"), 2)
  assert_eq(get_string_width("ꓪꓹ️ꓼ"), 2)
  assert_eq(get_string_width("ꓪꓹ‍ꓼ"), 2)
  assert_eq(get_string_width("ꓪꓹꓼ️"), 2)
  assert_eq(get_string_width("ꓪꓹ́ꓼ"), 3)
  assert_eq(get_string_width("ꓪꓹꓹ"), 3)
  assert_eq(get_string_width("ꓪꓼꓼ"), 3)
  assert_eq(get_string_width("א"), 1)
  assert_eq(get_string_width("ל"), 1)
  assert_eq(get_string_width("אל"), 2)
  assert_eq(get_string_width("א‍ל"), 1)
  assert_eq(get_string_width("א‍‍‍‍‍‍‍ל"), 1)
  assert_eq(get_string_width("אא‍ל"), 2)
  assert_eq(get_string_width("אא‍‍‍‍‍‍ל"), 2)
  assert_eq(get_string_width("א️‍️ל️"), 1)
  assert_eq(get_string_width("א︎‍︎ל︎"), 1)
  assert_eq(get_string_width("ل"), 1)
  assert_eq(get_string_width("ڸ"), 1)
  assert_eq(get_string_width("أ"), 1)
  assert_eq(get_string_width("ا"), 1)
  assert_eq(get_string_width("لأ"), 1)
  assert_eq(get_string_width("لا"), 1)
  assert_eq(get_string_width("ڸأ"), 1)
  assert_eq(get_string_width("ڸا"), 1)
  assert_eq(get_string_width("لٟٞأ"), 1)
  assert_eq(get_string_width("لٟٞا"), 1)
  assert_eq(get_string_width("ڸٟٞأ"), 1)
  assert_eq(get_string_width("ڸٟٞا"), 1)
  assert_eq(get_string_width("ڸ︎ا"), 1)
  assert_eq(get_string_width("ڸ️ا"), 1)
  assert_eq(get_string_width("ڸ឵ا"), 1)
  assert_eq(get_string_width("للأ"), 2)
  assert_eq(get_string_width("للا"), 2)
  assert_eq(get_string_width("ڸڸأ"), 2)
  assert_eq(get_string_width("ڸڸا"), 2)
  assert_eq(get_string_width("ل‍أ"), 2)
  assert_eq(get_string_width("ل‍ا"), 2)
  assert_eq(get_string_width("ڸ‍أ"), 2)
  assert_eq(get_string_width("ڸ‍ا"), 2)
  assert_eq(get_string_width("ل𞥋أ"), 3)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 1" {
  assert_eq(get_string_width("ل𞥋ا"), 3)
  assert_eq(get_string_width("ڸ𞥋أ"), 3)
  assert_eq(get_string_width("ڸ𞥋ا"), 3)
  assert_eq(get_string_width("ᨕ"), 1)
  assert_eq(get_string_width("ᨗ"), 0)
  assert_eq(get_string_width("ᨐ"), 1)
  assert_eq(get_string_width("ᨕᨗ‍ᨐ"), 1)
  assert_eq(get_string_width("ᨕᨗ‍‍‍‍ᨐ"), 1)
  assert_eq(get_string_width("ᨕᨗ‍̸"), 1)
  assert_eq(get_string_width("ᨕ︎ᨗ‍"), 1)
  assert_eq(get_string_width("ᨕ️ᨗ‍"), 1)
  assert_eq(get_string_width("ᨕᨗ︎‍"), 1)
  assert_eq(get_string_width("ᨕᨗ️‍"), 1)
  assert_eq(get_string_width("ᨕᨗ‍︎"), 1)
  assert_eq(get_string_width("ᨕᨗ‍️"), 1)
  assert_eq(get_string_width("ᨕ឵‍︎ᨗ‍️‍️"), 1)
  assert_eq(get_string_width("ᨕᨕᨗ‍ᨐ"), 2)
  assert_eq(get_string_width("ᨕᨕᨗ‍‍‍‍ᨐ"), 2)
  assert_eq(get_string_width("ᨕᨗᨐ"), 2)
  assert_eq(get_string_width("ᨕ‍ᨐ"), 2)
  assert_eq(get_string_width("ᨕᨐ"), 2)
  assert_eq(get_string_width("ᨕᨗᨗ‍ᨐ"), 2)
  assert_eq(get_string_width("ᨕ̸ᨗ‍ᨐ"), 2)
  assert_eq(get_string_width("ⵏ"), 1)
  assert_eq(get_string_width("ⴾ"), 1)
  assert_eq(get_string_width("⵿"), 1)
  assert_eq(get_string_width("ⵏ‍ⴾ"), 1)
  assert_eq(get_string_width("ⵏ⵿ⴾ"), 1)
  assert_eq(get_string_width("ⵏ️‍⵿︎‍឵ⴾ"), 1)
  assert_eq(get_string_width("ⵏ⵿́ⴾ"), 3)
  assert_eq(get_string_width("ⵏ́‍ⴾ"), 2)
  assert_eq(get_string_width("ⵏⴾ"), 2)
  assert_eq(get_string_width("ⵏ⵿⵿ⴾ"), 4)
  assert_eq(get_string_width("⵿ⴾ"), 2)
  assert_eq(get_string_width("⵿⵿ⵦ"), 3)
  assert_eq(get_string_width("ⵦ⵿ⴾ"), 3)
  assert_eq(get_string_width("𐰲"), 1)
  assert_eq(get_string_width("𐰃"), 1)
  assert_eq(get_string_width("𐰲𐰃"), 2)
  assert_eq(get_string_width("𐰲‍𐰃"), 1)
  assert_eq(get_string_width("𐰲️‍︎𐰃"), 1)
  assert_eq(get_string_width("𐰲⵿𐰃"), 3)
  assert_eq(get_string_width("𐰲́‍𐰃"), 2)
  assert_eq(get_string_width("𐰃‍𐰲"), 2)
  assert_eq(get_string_width("‍𐰲"), 1)
  assert_eq(get_string_width("ល"), 1)
  assert_eq(get_string_width("ង"), 1)
  assert_eq(get_string_width("លង"), 2)
  assert_eq(get_string_width("ល្ង"), 1)
  assert_eq(get_string_width("ឤ"), 2)
  assert_eq(get_string_width("អឦ"), 2)
  assert_eq(get_string_width("៘"), 3)
  assert_eq(get_string_width("។ល។"), 3)
  assert_eq(get_string_width("👪"), 2)
  assert_eq(get_string_width("🏻"), 2)
  assert_eq(get_string_width("👪🏻"), 2)
  assert_eq(get_string_width("👪‍‍🏻"), 4)
  assert_eq(get_string_width("🧑‍🤝‍🧑"), 2)
  assert_eq(get_string_width("🇮🇱🕊️🇵🇸"), 6)
  assert_eq(get_string_width("🇵🇸‍🕊️‍🇮🇱"), 2)
  assert_eq(get_string_width("🇮🇱‍🕊️‍‍🇵🇸"), 4)
  assert_eq(get_string_width("🇵🇸‍‍🕊️‍🇮🇱"), 4)
  assert_eq(get_string_width("🇦🇦‍🇦🇦"), 2)
  assert_eq(get_string_width("🇦🇦‍🇦🇦🇦"), 3)
  assert_eq(get_string_width("🇦🇦‍‍🇦🇦"), 4)
  assert_eq(get_string_width("🇦🇦‍🇦‍🇦🇦"), 5)
  assert_eq(get_string_width("🇦🇦‍🇦🇦‍🇦🇦"), 2)
  assert_eq(get_string_width("🇦🇦‍🇦🇦🇦‍🇦🇦"), 5)
  assert_eq(get_string_width("🇦🇦‍🇦🇦🇦🇦‍🇦🇦"), 4)
  assert_eq(get_string_width("🇦🇦‍🇦🇦🇦🇦🇦‍🇦🇦"), 7)
  assert_eq(
    get_string_width("🇦🇦‍🇦🇦🇦🇦🇦🇦‍🇦🇦"),
    6,
  )
  assert_eq(
    get_string_width("🇦🇦‍🇦🇦🇦🇦🇦🇦🇦‍🇦🇦"),
    9,
  )
  assert_eq(get_string_width("🏴󠁧󠁢󠁷󠁬󠁳󠁿"), 2)
  assert_eq(
    get_string_width(
      "🏴󠁧󠁢󠁥󠁮󠁧󠁿‍🏴󠁧󠁢󠁳󠁣󠁴󠁿‍🏴󠁧󠁢󠁷󠁬󠁳󠁿",
    ),
    2,
  )
  assert_eq(get_string_width("🇦👪‍🏿"), 3)
  assert_eq(get_string_width("🇦🏿‍🏿"), 3)
  assert_eq(get_string_width("󠀱"), 0)
  assert_eq(get_string_width("󠁣"), 0)
  assert_eq(get_string_width("󠁿"), 0)
  assert_eq(get_string_width("🏴‍Ⓜ️"), 2)
  assert_eq(get_string_width("🏴󠀱‍Ⓜ️"), 4)
  assert_eq(get_string_width("🏴󠁣‍Ⓜ️"), 4)
  assert_eq(get_string_width("🏴󠁿‍Ⓜ️"), 4)
  assert_eq(get_string_width("🏴󠀱󠁿‍Ⓜ️"), 4)
  assert_eq(get_string_width("🏴󠀱󠀱󠁿‍Ⓜ️"), 4)
  assert_eq(get_string_width("🏴󠀱󠀱󠀱󠁿‍Ⓜ️"), 2)
  assert_eq(get_string_width("🏴󠀱󠀱󠀱󠀱󠁿‍Ⓜ️"), 4)
  assert_eq(get_string_width("🏴󠀱󠀱󠀱󠁣󠁿‍Ⓜ️"), 2)
  assert_eq(get_string_width("🏴󠀱󠀱󠀱󠁣󠁣󠁿‍Ⓜ️"), 2)
  assert_eq(get_string_width("🏴󠀱󠀱󠀱󠁣󠁣󠁣󠁿‍Ⓜ️"), 2)
  assert_eq(
    get_string_width("🏴󠀱󠀱󠀱󠁣󠁣󠁣󠁣󠁿‍Ⓜ️"),
    2,
  )
  assert_eq(
    get_string_width("🏴󠀱󠀱󠀱󠁣󠁣󠁣󠁣󠁣󠁿‍Ⓜ️"),
    4,
  )
  assert_eq(get_string_width("🏴󠁣󠁣󠁿‍Ⓜ️"), 4)
  assert_eq(get_string_width("🏴󠁣󠁣󠁣󠁿‍Ⓜ️"), 2)
  assert_eq(get_string_width("🏴󠁣󠁣󠁣󠁣󠁿‍Ⓜ️"), 2)
  assert_eq(get_string_width("🏴󠁣󠁣󠁣󠁣󠁣󠁿‍Ⓜ️"), 2)
  assert_eq(get_string_width("🏴󠁣󠁣󠁣󠁣󠁣󠁣󠁿‍Ⓜ️"), 2)
  assert_eq(
    get_string_width("🏴󠁣󠁣󠁣󠁣󠁣󠁣󠁣󠁿‍Ⓜ️"),
    4,
  )
  assert_eq(get_string_width("a‍🏴󠁧󠁢󠁷󠁬󠁳󠁿"), 3)
  assert_eq(get_string_width("👪‍a"), 3)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 2" {
  assert_eq(get_string_width("a‍a"), 2)
  assert_eq(get_string_width("*⃣"), 1)
  assert_eq(get_string_width("*️⃣"), 2)
  assert_eq(get_string_width("*⃣️"), 1)
  assert_eq(get_string_width("*️⃣‍👪"), 2)
  assert_eq(get_string_width("*⃣️‍👪"), 3)
  assert_eq(get_string_width("*⃣‍👪"), 3)
  assert_eq(get_string_width("*️‍👪"), 2)
  assert_eq(get_string_width("*️⃣⃣‍👪"), 4)
  assert_eq(get_string_width("*️️⃣‍👪"), 4)
  assert_eq(
    get_string_width(
      "🇦👪‍🏿‍👪🏻‍Ⓜ️‍*️‍🇦🇦‍🏴󠁧󠁢󠁷󠁬󠁳󠁿‍👪",
    ),
    3,
  )
  assert_eq(get_string_width("⓪"), 1)
  assert_eq(get_string_width("☖"), 1)
  assert_eq(get_string_width("➀"), 1)
  assert_eq(get_string_width("😀"), 2)
  assert_eq(get_string_width("😃"), 2)
  assert_eq(get_string_width("😄"), 2)
  assert_eq(get_string_width("😁"), 2)
  assert_eq(get_string_width("😆"), 2)
  assert_eq(get_string_width("😅"), 2)
  assert_eq(get_string_width("🤣"), 2)
  assert_eq(get_string_width("😂"), 2)
  assert_eq(get_string_width("🙂"), 2)
  assert_eq(get_string_width("🙃"), 2)
  assert_eq(get_string_width("🫠"), 2)
  assert_eq(get_string_width("😉"), 2)
  assert_eq(get_string_width("😊"), 2)
  assert_eq(get_string_width("😇"), 2)
  assert_eq(get_string_width("🥰"), 2)
  assert_eq(get_string_width("😍"), 2)
  assert_eq(get_string_width("🤩"), 2)
  assert_eq(get_string_width("😘"), 2)
  assert_eq(get_string_width("😗"), 2)
  assert_eq(get_string_width("☺️"), 2)
  assert_eq(get_string_width("☺"), 1)
  assert_eq(get_string_width("😚"), 2)
  assert_eq(get_string_width("😙"), 2)
  assert_eq(get_string_width("🥲"), 2)
  assert_eq(get_string_width("😋"), 2)
  assert_eq(get_string_width("😛"), 2)
  assert_eq(get_string_width("😜"), 2)
  assert_eq(get_string_width("🤪"), 2)
  assert_eq(get_string_width("😝"), 2)
  assert_eq(get_string_width("🤑"), 2)
  assert_eq(get_string_width("🤗"), 2)
  assert_eq(get_string_width("🤭"), 2)
  assert_eq(get_string_width("🫢"), 2)
  assert_eq(get_string_width("🫣"), 2)
  assert_eq(get_string_width("🤫"), 2)
  assert_eq(get_string_width("🤔"), 2)
  assert_eq(get_string_width("🫡"), 2)
  assert_eq(get_string_width("🤐"), 2)
  assert_eq(get_string_width("🤨"), 2)
  assert_eq(get_string_width("😐"), 2)
  assert_eq(get_string_width("😑"), 2)
  assert_eq(get_string_width("😶"), 2)
  assert_eq(get_string_width("🫥"), 2)
  assert_eq(get_string_width("😶‍🌫️"), 2)
  assert_eq(get_string_width("😶‍🌫"), 3)
  assert_eq(get_string_width("😏"), 2)
  assert_eq(get_string_width("😒"), 2)
  assert_eq(get_string_width("🙄"), 2)
  assert_eq(get_string_width("😬"), 2)
  assert_eq(get_string_width("😮‍💨"), 2)
  assert_eq(get_string_width("🤥"), 2)
  assert_eq(get_string_width("🫨"), 2)
  assert_eq(get_string_width("🙂‍↔️"), 2)
  assert_eq(get_string_width("🙂‍↔"), 3)
  assert_eq(get_string_width("🙂‍↕️"), 2)
  assert_eq(get_string_width("🙂‍↕"), 3)
  assert_eq(get_string_width("😌"), 2)
  assert_eq(get_string_width("😔"), 2)
  assert_eq(get_string_width("😪"), 2)
  assert_eq(get_string_width("🤤"), 2)
  assert_eq(get_string_width("😴"), 2)
  assert_eq(get_string_width("🫩"), 2)
  assert_eq(get_string_width("😷"), 2)
  assert_eq(get_string_width("🤒"), 2)
  assert_eq(get_string_width("🤕"), 2)
  assert_eq(get_string_width("🤢"), 2)
  assert_eq(get_string_width("🤮"), 2)
  assert_eq(get_string_width("🤧"), 2)
  assert_eq(get_string_width("🥵"), 2)
  assert_eq(get_string_width("🥶"), 2)
  assert_eq(get_string_width("🥴"), 2)
  assert_eq(get_string_width("😵"), 2)
  assert_eq(get_string_width("😵‍💫"), 2)
  assert_eq(get_string_width("🤯"), 2)
  assert_eq(get_string_width("🤠"), 2)
  assert_eq(get_string_width("🥳"), 2)
  assert_eq(get_string_width("🥸"), 2)
  assert_eq(get_string_width("😎"), 2)
  assert_eq(get_string_width("🤓"), 2)
  assert_eq(get_string_width("🧐"), 2)
  assert_eq(get_string_width("😕"), 2)
  assert_eq(get_string_width("🫤"), 2)
  assert_eq(get_string_width("😟"), 2)
  assert_eq(get_string_width("🙁"), 2)
  assert_eq(get_string_width("☹️"), 2)
  assert_eq(get_string_width("☹"), 1)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 3" {
  assert_eq(get_string_width("😮"), 2)
  assert_eq(get_string_width("😯"), 2)
  assert_eq(get_string_width("😲"), 2)
  assert_eq(get_string_width("😳"), 2)
  assert_eq(get_string_width("🫪"), 2)
  assert_eq(get_string_width("🥺"), 2)
  assert_eq(get_string_width("🥹"), 2)
  assert_eq(get_string_width("😦"), 2)
  assert_eq(get_string_width("😧"), 2)
  assert_eq(get_string_width("😨"), 2)
  assert_eq(get_string_width("😰"), 2)
  assert_eq(get_string_width("😥"), 2)
  assert_eq(get_string_width("😢"), 2)
  assert_eq(get_string_width("😭"), 2)
  assert_eq(get_string_width("😱"), 2)
  assert_eq(get_string_width("😖"), 2)
  assert_eq(get_string_width("😣"), 2)
  assert_eq(get_string_width("😞"), 2)
  assert_eq(get_string_width("😓"), 2)
  assert_eq(get_string_width("😩"), 2)
  assert_eq(get_string_width("😫"), 2)
  assert_eq(get_string_width("🥱"), 2)
  assert_eq(get_string_width("😤"), 2)
  assert_eq(get_string_width("😡"), 2)
  assert_eq(get_string_width("😠"), 2)
  assert_eq(get_string_width("🤬"), 2)
  assert_eq(get_string_width("😈"), 2)
  assert_eq(get_string_width("👿"), 2)
  assert_eq(get_string_width("💀"), 2)
  assert_eq(get_string_width("☠️"), 2)
  assert_eq(get_string_width("☠"), 1)
  assert_eq(get_string_width("💩"), 2)
  assert_eq(get_string_width("🤡"), 2)
  assert_eq(get_string_width("👹"), 2)
  assert_eq(get_string_width("👺"), 2)
  assert_eq(get_string_width("👻"), 2)
  assert_eq(get_string_width("👽"), 2)
  assert_eq(get_string_width("👾"), 2)
  assert_eq(get_string_width("🤖"), 2)
  assert_eq(get_string_width("😺"), 2)
  assert_eq(get_string_width("😸"), 2)
  assert_eq(get_string_width("😹"), 2)
  assert_eq(get_string_width("😻"), 2)
  assert_eq(get_string_width("😼"), 2)
  assert_eq(get_string_width("😽"), 2)
  assert_eq(get_string_width("🙀"), 2)
  assert_eq(get_string_width("😿"), 2)
  assert_eq(get_string_width("😾"), 2)
  assert_eq(get_string_width("🙈"), 2)
  assert_eq(get_string_width("🙉"), 2)
  assert_eq(get_string_width("🙊"), 2)
  assert_eq(get_string_width("💌"), 2)
  assert_eq(get_string_width("💘"), 2)
  assert_eq(get_string_width("💝"), 2)
  assert_eq(get_string_width("💖"), 2)
  assert_eq(get_string_width("💗"), 2)
  assert_eq(get_string_width("💓"), 2)
  assert_eq(get_string_width("💞"), 2)
  assert_eq(get_string_width("💕"), 2)
  assert_eq(get_string_width("💟"), 2)
  assert_eq(get_string_width("❣️"), 2)
  assert_eq(get_string_width("❣"), 1)
  assert_eq(get_string_width("💔"), 2)
  assert_eq(get_string_width("❤️‍🔥"), 2)
  assert_eq(get_string_width("❤‍🔥"), 3)
  assert_eq(get_string_width("❤️‍🩹"), 2)
  assert_eq(get_string_width("❤‍🩹"), 3)
  assert_eq(get_string_width("❤️"), 2)
  assert_eq(get_string_width("❤"), 1)
  assert_eq(get_string_width("🩷"), 2)
  assert_eq(get_string_width("🧡"), 2)
  assert_eq(get_string_width("💛"), 2)
  assert_eq(get_string_width("💚"), 2)
  assert_eq(get_string_width("💙"), 2)
  assert_eq(get_string_width("🩵"), 2)
  assert_eq(get_string_width("💜"), 2)
  assert_eq(get_string_width("🤎"), 2)
  assert_eq(get_string_width("🖤"), 2)
  assert_eq(get_string_width("🩶"), 2)
  assert_eq(get_string_width("🤍"), 2)
  assert_eq(get_string_width("💋"), 2)
  assert_eq(get_string_width("💯"), 2)
  assert_eq(get_string_width("💢"), 2)
  assert_eq(get_string_width("🫯"), 2)
  assert_eq(get_string_width("💥"), 2)
  assert_eq(get_string_width("💫"), 2)
  assert_eq(get_string_width("💦"), 2)
  assert_eq(get_string_width("💨"), 2)
  assert_eq(get_string_width("🕳️"), 2)
  assert_eq(get_string_width("🕳"), 1)
  assert_eq(get_string_width("💬"), 2)
  assert_eq(get_string_width("👁️‍🗨️"), 2)
  assert_eq(get_string_width("👁‍🗨️"), 3)
  assert_eq(get_string_width("👁️‍🗨"), 3)
  assert_eq(get_string_width("👁‍🗨"), 2)
  assert_eq(get_string_width("🗨️"), 2)
  assert_eq(get_string_width("🗨"), 1)
  assert_eq(get_string_width("🗯️"), 2)
  assert_eq(get_string_width("🗯"), 1)
  assert_eq(get_string_width("💭"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 4" {
  assert_eq(get_string_width("💤"), 2)
  assert_eq(get_string_width("👋"), 2)
  assert_eq(get_string_width("👋🏻"), 2)
  assert_eq(get_string_width("👋🏼"), 2)
  assert_eq(get_string_width("👋🏽"), 2)
  assert_eq(get_string_width("👋🏾"), 2)
  assert_eq(get_string_width("👋🏿"), 2)
  assert_eq(get_string_width("🤚"), 2)
  assert_eq(get_string_width("🤚🏻"), 2)
  assert_eq(get_string_width("🤚🏼"), 2)
  assert_eq(get_string_width("🤚🏽"), 2)
  assert_eq(get_string_width("🤚🏾"), 2)
  assert_eq(get_string_width("🤚🏿"), 2)
  assert_eq(get_string_width("🖐️"), 2)
  assert_eq(get_string_width("🖐"), 1)
  assert_eq(get_string_width("🖐🏻"), 2)
  assert_eq(get_string_width("🖐🏼"), 2)
  assert_eq(get_string_width("🖐🏽"), 2)
  assert_eq(get_string_width("🖐🏾"), 2)
  assert_eq(get_string_width("🖐🏿"), 2)
  assert_eq(get_string_width("✋"), 2)
  assert_eq(get_string_width("✋🏻"), 2)
  assert_eq(get_string_width("✋🏼"), 2)
  assert_eq(get_string_width("✋🏽"), 2)
  assert_eq(get_string_width("✋🏾"), 2)
  assert_eq(get_string_width("✋🏿"), 2)
  assert_eq(get_string_width("🖖"), 2)
  assert_eq(get_string_width("🖖🏻"), 2)
  assert_eq(get_string_width("🖖🏼"), 2)
  assert_eq(get_string_width("🖖🏽"), 2)
  assert_eq(get_string_width("🖖🏾"), 2)
  assert_eq(get_string_width("🖖🏿"), 2)
  assert_eq(get_string_width("🫱"), 2)
  assert_eq(get_string_width("🫱🏻"), 2)
  assert_eq(get_string_width("🫱🏼"), 2)
  assert_eq(get_string_width("🫱🏽"), 2)
  assert_eq(get_string_width("🫱🏾"), 2)
  assert_eq(get_string_width("🫱🏿"), 2)
  assert_eq(get_string_width("🫲"), 2)
  assert_eq(get_string_width("🫲🏻"), 2)
  assert_eq(get_string_width("🫲🏼"), 2)
  assert_eq(get_string_width("🫲🏽"), 2)
  assert_eq(get_string_width("🫲🏾"), 2)
  assert_eq(get_string_width("🫲🏿"), 2)
  assert_eq(get_string_width("🫳"), 2)
  assert_eq(get_string_width("🫳🏻"), 2)
  assert_eq(get_string_width("🫳🏼"), 2)
  assert_eq(get_string_width("🫳🏽"), 2)
  assert_eq(get_string_width("🫳🏾"), 2)
  assert_eq(get_string_width("🫳🏿"), 2)
  assert_eq(get_string_width("🫴"), 2)
  assert_eq(get_string_width("🫴🏻"), 2)
  assert_eq(get_string_width("🫴🏼"), 2)
  assert_eq(get_string_width("🫴🏽"), 2)
  assert_eq(get_string_width("🫴🏾"), 2)
  assert_eq(get_string_width("🫴🏿"), 2)
  assert_eq(get_string_width("🫷"), 2)
  assert_eq(get_string_width("🫷🏻"), 2)
  assert_eq(get_string_width("🫷🏼"), 2)
  assert_eq(get_string_width("🫷🏽"), 2)
  assert_eq(get_string_width("🫷🏾"), 2)
  assert_eq(get_string_width("🫷🏿"), 2)
  assert_eq(get_string_width("🫸"), 2)
  assert_eq(get_string_width("🫸🏻"), 2)
  assert_eq(get_string_width("🫸🏼"), 2)
  assert_eq(get_string_width("🫸🏽"), 2)
  assert_eq(get_string_width("🫸🏾"), 2)
  assert_eq(get_string_width("🫸🏿"), 2)
  assert_eq(get_string_width("👌"), 2)
  assert_eq(get_string_width("👌🏻"), 2)
  assert_eq(get_string_width("👌🏼"), 2)
  assert_eq(get_string_width("👌🏽"), 2)
  assert_eq(get_string_width("👌🏾"), 2)
  assert_eq(get_string_width("👌🏿"), 2)
  assert_eq(get_string_width("🤌"), 2)
  assert_eq(get_string_width("🤌🏻"), 2)
  assert_eq(get_string_width("🤌🏼"), 2)
  assert_eq(get_string_width("🤌🏽"), 2)
  assert_eq(get_string_width("🤌🏾"), 2)
  assert_eq(get_string_width("🤌🏿"), 2)
  assert_eq(get_string_width("🤏"), 2)
  assert_eq(get_string_width("🤏🏻"), 2)
  assert_eq(get_string_width("🤏🏼"), 2)
  assert_eq(get_string_width("🤏🏽"), 2)
  assert_eq(get_string_width("🤏🏾"), 2)
  assert_eq(get_string_width("🤏🏿"), 2)
  assert_eq(get_string_width("✌️"), 2)
  assert_eq(get_string_width("✌"), 1)
  assert_eq(get_string_width("✌🏻"), 2)
  assert_eq(get_string_width("✌🏼"), 2)
  assert_eq(get_string_width("✌🏽"), 2)
  assert_eq(get_string_width("✌🏾"), 2)
  assert_eq(get_string_width("✌🏿"), 2)
  assert_eq(get_string_width("🤞"), 2)
  assert_eq(get_string_width("🤞🏻"), 2)
  assert_eq(get_string_width("🤞🏼"), 2)
  assert_eq(get_string_width("🤞🏽"), 2)
  assert_eq(get_string_width("🤞🏾"), 2)
  assert_eq(get_string_width("🤞🏿"), 2)
  assert_eq(get_string_width("🫰"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 5" {
  assert_eq(get_string_width("🫰🏻"), 2)
  assert_eq(get_string_width("🫰🏼"), 2)
  assert_eq(get_string_width("🫰🏽"), 2)
  assert_eq(get_string_width("🫰🏾"), 2)
  assert_eq(get_string_width("🫰🏿"), 2)
  assert_eq(get_string_width("🤟"), 2)
  assert_eq(get_string_width("🤟🏻"), 2)
  assert_eq(get_string_width("🤟🏼"), 2)
  assert_eq(get_string_width("🤟🏽"), 2)
  assert_eq(get_string_width("🤟🏾"), 2)
  assert_eq(get_string_width("🤟🏿"), 2)
  assert_eq(get_string_width("🤘"), 2)
  assert_eq(get_string_width("🤘🏻"), 2)
  assert_eq(get_string_width("🤘🏼"), 2)
  assert_eq(get_string_width("🤘🏽"), 2)
  assert_eq(get_string_width("🤘🏾"), 2)
  assert_eq(get_string_width("🤘🏿"), 2)
  assert_eq(get_string_width("🤙"), 2)
  assert_eq(get_string_width("🤙🏻"), 2)
  assert_eq(get_string_width("🤙🏼"), 2)
  assert_eq(get_string_width("🤙🏽"), 2)
  assert_eq(get_string_width("🤙🏾"), 2)
  assert_eq(get_string_width("🤙🏿"), 2)
  assert_eq(get_string_width("👈"), 2)
  assert_eq(get_string_width("👈🏻"), 2)
  assert_eq(get_string_width("👈🏼"), 2)
  assert_eq(get_string_width("👈🏽"), 2)
  assert_eq(get_string_width("👈🏾"), 2)
  assert_eq(get_string_width("👈🏿"), 2)
  assert_eq(get_string_width("👉"), 2)
  assert_eq(get_string_width("👉🏻"), 2)
  assert_eq(get_string_width("👉🏼"), 2)
  assert_eq(get_string_width("👉🏽"), 2)
  assert_eq(get_string_width("👉🏾"), 2)
  assert_eq(get_string_width("👉🏿"), 2)
  assert_eq(get_string_width("👆"), 2)
  assert_eq(get_string_width("👆🏻"), 2)
  assert_eq(get_string_width("👆🏼"), 2)
  assert_eq(get_string_width("👆🏽"), 2)
  assert_eq(get_string_width("👆🏾"), 2)
  assert_eq(get_string_width("👆🏿"), 2)
  assert_eq(get_string_width("🖕"), 2)
  assert_eq(get_string_width("🖕🏻"), 2)
  assert_eq(get_string_width("🖕🏼"), 2)
  assert_eq(get_string_width("🖕🏽"), 2)
  assert_eq(get_string_width("🖕🏾"), 2)
  assert_eq(get_string_width("🖕🏿"), 2)
  assert_eq(get_string_width("👇"), 2)
  assert_eq(get_string_width("👇🏻"), 2)
  assert_eq(get_string_width("👇🏼"), 2)
  assert_eq(get_string_width("👇🏽"), 2)
  assert_eq(get_string_width("👇🏾"), 2)
  assert_eq(get_string_width("👇🏿"), 2)
  assert_eq(get_string_width("☝️"), 2)
  assert_eq(get_string_width("☝"), 1)
  assert_eq(get_string_width("☝🏻"), 2)
  assert_eq(get_string_width("☝🏼"), 2)
  assert_eq(get_string_width("☝🏽"), 2)
  assert_eq(get_string_width("☝🏾"), 2)
  assert_eq(get_string_width("☝🏿"), 2)
  assert_eq(get_string_width("🫵"), 2)
  assert_eq(get_string_width("🫵🏻"), 2)
  assert_eq(get_string_width("🫵🏼"), 2)
  assert_eq(get_string_width("🫵🏽"), 2)
  assert_eq(get_string_width("🫵🏾"), 2)
  assert_eq(get_string_width("🫵🏿"), 2)
  assert_eq(get_string_width("👍"), 2)
  assert_eq(get_string_width("👍🏻"), 2)
  assert_eq(get_string_width("👍🏼"), 2)
  assert_eq(get_string_width("👍🏽"), 2)
  assert_eq(get_string_width("👍🏾"), 2)
  assert_eq(get_string_width("👍🏿"), 2)
  assert_eq(get_string_width("👎"), 2)
  assert_eq(get_string_width("👎🏻"), 2)
  assert_eq(get_string_width("👎🏼"), 2)
  assert_eq(get_string_width("👎🏽"), 2)
  assert_eq(get_string_width("👎🏾"), 2)
  assert_eq(get_string_width("👎🏿"), 2)
  assert_eq(get_string_width("✊"), 2)
  assert_eq(get_string_width("✊🏻"), 2)
  assert_eq(get_string_width("✊🏼"), 2)
  assert_eq(get_string_width("✊🏽"), 2)
  assert_eq(get_string_width("✊🏾"), 2)
  assert_eq(get_string_width("✊🏿"), 2)
  assert_eq(get_string_width("👊"), 2)
  assert_eq(get_string_width("👊🏻"), 2)
  assert_eq(get_string_width("👊🏼"), 2)
  assert_eq(get_string_width("👊🏽"), 2)
  assert_eq(get_string_width("👊🏾"), 2)
  assert_eq(get_string_width("👊🏿"), 2)
  assert_eq(get_string_width("🤛"), 2)
  assert_eq(get_string_width("🤛🏻"), 2)
  assert_eq(get_string_width("🤛🏼"), 2)
  assert_eq(get_string_width("🤛🏽"), 2)
  assert_eq(get_string_width("🤛🏾"), 2)
  assert_eq(get_string_width("🤛🏿"), 2)
  assert_eq(get_string_width("🤜"), 2)
  assert_eq(get_string_width("🤜🏻"), 2)
  assert_eq(get_string_width("🤜🏼"), 2)
  assert_eq(get_string_width("🤜🏽"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 6" {
  assert_eq(get_string_width("🤜🏾"), 2)
  assert_eq(get_string_width("🤜🏿"), 2)
  assert_eq(get_string_width("👏"), 2)
  assert_eq(get_string_width("👏🏻"), 2)
  assert_eq(get_string_width("👏🏼"), 2)
  assert_eq(get_string_width("👏🏽"), 2)
  assert_eq(get_string_width("👏🏾"), 2)
  assert_eq(get_string_width("👏🏿"), 2)
  assert_eq(get_string_width("🙌"), 2)
  assert_eq(get_string_width("🙌🏻"), 2)
  assert_eq(get_string_width("🙌🏼"), 2)
  assert_eq(get_string_width("🙌🏽"), 2)
  assert_eq(get_string_width("🙌🏾"), 2)
  assert_eq(get_string_width("🙌🏿"), 2)
  assert_eq(get_string_width("🫶"), 2)
  assert_eq(get_string_width("🫶🏻"), 2)
  assert_eq(get_string_width("🫶🏼"), 2)
  assert_eq(get_string_width("🫶🏽"), 2)
  assert_eq(get_string_width("🫶🏾"), 2)
  assert_eq(get_string_width("🫶🏿"), 2)
  assert_eq(get_string_width("👐"), 2)
  assert_eq(get_string_width("👐🏻"), 2)
  assert_eq(get_string_width("👐🏼"), 2)
  assert_eq(get_string_width("👐🏽"), 2)
  assert_eq(get_string_width("👐🏾"), 2)
  assert_eq(get_string_width("👐🏿"), 2)
  assert_eq(get_string_width("🤲"), 2)
  assert_eq(get_string_width("🤲🏻"), 2)
  assert_eq(get_string_width("🤲🏼"), 2)
  assert_eq(get_string_width("🤲🏽"), 2)
  assert_eq(get_string_width("🤲🏾"), 2)
  assert_eq(get_string_width("🤲🏿"), 2)
  assert_eq(get_string_width("🤝"), 2)
  assert_eq(get_string_width("🤝🏻"), 2)
  assert_eq(get_string_width("🤝🏼"), 2)
  assert_eq(get_string_width("🤝🏽"), 2)
  assert_eq(get_string_width("🤝🏾"), 2)
  assert_eq(get_string_width("🤝🏿"), 2)
  assert_eq(get_string_width("🫱🏻‍🫲🏼"), 2)
  assert_eq(get_string_width("🫱🏻‍🫲🏽"), 2)
  assert_eq(get_string_width("🫱🏻‍🫲🏾"), 2)
  assert_eq(get_string_width("🫱🏻‍🫲🏿"), 2)
  assert_eq(get_string_width("🫱🏼‍🫲🏻"), 2)
  assert_eq(get_string_width("🫱🏼‍🫲🏽"), 2)
  assert_eq(get_string_width("🫱🏼‍🫲🏾"), 2)
  assert_eq(get_string_width("🫱🏼‍🫲🏿"), 2)
  assert_eq(get_string_width("🫱🏽‍🫲🏻"), 2)
  assert_eq(get_string_width("🫱🏽‍🫲🏼"), 2)
  assert_eq(get_string_width("🫱🏽‍🫲🏾"), 2)
  assert_eq(get_string_width("🫱🏽‍🫲🏿"), 2)
  assert_eq(get_string_width("🫱🏾‍🫲🏻"), 2)
  assert_eq(get_string_width("🫱🏾‍🫲🏼"), 2)
  assert_eq(get_string_width("🫱🏾‍🫲🏽"), 2)
  assert_eq(get_string_width("🫱🏾‍🫲🏿"), 2)
  assert_eq(get_string_width("🫱🏿‍🫲🏻"), 2)
  assert_eq(get_string_width("🫱🏿‍🫲🏼"), 2)
  assert_eq(get_string_width("🫱🏿‍🫲🏽"), 2)
  assert_eq(get_string_width("🫱🏿‍🫲🏾"), 2)
  assert_eq(get_string_width("🙏"), 2)
  assert_eq(get_string_width("🙏🏻"), 2)
  assert_eq(get_string_width("🙏🏼"), 2)
  assert_eq(get_string_width("🙏🏽"), 2)
  assert_eq(get_string_width("🙏🏾"), 2)
  assert_eq(get_string_width("🙏🏿"), 2)
  assert_eq(get_string_width("✍️"), 2)
  assert_eq(get_string_width("✍"), 1)
  assert_eq(get_string_width("✍🏻"), 2)
  assert_eq(get_string_width("✍🏼"), 2)
  assert_eq(get_string_width("✍🏽"), 2)
  assert_eq(get_string_width("✍🏾"), 2)
  assert_eq(get_string_width("✍🏿"), 2)
  assert_eq(get_string_width("💅"), 2)
  assert_eq(get_string_width("💅🏻"), 2)
  assert_eq(get_string_width("💅🏼"), 2)
  assert_eq(get_string_width("💅🏽"), 2)
  assert_eq(get_string_width("💅🏾"), 2)
  assert_eq(get_string_width("💅🏿"), 2)
  assert_eq(get_string_width("🤳"), 2)
  assert_eq(get_string_width("🤳🏻"), 2)
  assert_eq(get_string_width("🤳🏼"), 2)
  assert_eq(get_string_width("🤳🏽"), 2)
  assert_eq(get_string_width("🤳🏾"), 2)
  assert_eq(get_string_width("🤳🏿"), 2)
  assert_eq(get_string_width("💪"), 2)
  assert_eq(get_string_width("💪🏻"), 2)
  assert_eq(get_string_width("💪🏼"), 2)
  assert_eq(get_string_width("💪🏽"), 2)
  assert_eq(get_string_width("💪🏾"), 2)
  assert_eq(get_string_width("💪🏿"), 2)
  assert_eq(get_string_width("🦾"), 2)
  assert_eq(get_string_width("🦿"), 2)
  assert_eq(get_string_width("🦵"), 2)
  assert_eq(get_string_width("🦵🏻"), 2)
  assert_eq(get_string_width("🦵🏼"), 2)
  assert_eq(get_string_width("🦵🏽"), 2)
  assert_eq(get_string_width("🦵🏾"), 2)
  assert_eq(get_string_width("🦵🏿"), 2)
  assert_eq(get_string_width("🦶"), 2)
  assert_eq(get_string_width("🦶🏻"), 2)
  assert_eq(get_string_width("🦶🏼"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 7" {
  assert_eq(get_string_width("🦶🏽"), 2)
  assert_eq(get_string_width("🦶🏾"), 2)
  assert_eq(get_string_width("🦶🏿"), 2)
  assert_eq(get_string_width("👂"), 2)
  assert_eq(get_string_width("👂🏻"), 2)
  assert_eq(get_string_width("👂🏼"), 2)
  assert_eq(get_string_width("👂🏽"), 2)
  assert_eq(get_string_width("👂🏾"), 2)
  assert_eq(get_string_width("👂🏿"), 2)
  assert_eq(get_string_width("🦻"), 2)
  assert_eq(get_string_width("🦻🏻"), 2)
  assert_eq(get_string_width("🦻🏼"), 2)
  assert_eq(get_string_width("🦻🏽"), 2)
  assert_eq(get_string_width("🦻🏾"), 2)
  assert_eq(get_string_width("🦻🏿"), 2)
  assert_eq(get_string_width("👃"), 2)
  assert_eq(get_string_width("👃🏻"), 2)
  assert_eq(get_string_width("👃🏼"), 2)
  assert_eq(get_string_width("👃🏽"), 2)
  assert_eq(get_string_width("👃🏾"), 2)
  assert_eq(get_string_width("👃🏿"), 2)
  assert_eq(get_string_width("🧠"), 2)
  assert_eq(get_string_width("🫀"), 2)
  assert_eq(get_string_width("🫁"), 2)
  assert_eq(get_string_width("🦷"), 2)
  assert_eq(get_string_width("🦴"), 2)
  assert_eq(get_string_width("👀"), 2)
  assert_eq(get_string_width("👁️"), 2)
  assert_eq(get_string_width("👁"), 1)
  assert_eq(get_string_width("👅"), 2)
  assert_eq(get_string_width("👄"), 2)
  assert_eq(get_string_width("🫦"), 2)
  assert_eq(get_string_width("👶"), 2)
  assert_eq(get_string_width("👶🏻"), 2)
  assert_eq(get_string_width("👶🏼"), 2)
  assert_eq(get_string_width("👶🏽"), 2)
  assert_eq(get_string_width("👶🏾"), 2)
  assert_eq(get_string_width("👶🏿"), 2)
  assert_eq(get_string_width("🧒"), 2)
  assert_eq(get_string_width("🧒🏻"), 2)
  assert_eq(get_string_width("🧒🏼"), 2)
  assert_eq(get_string_width("🧒🏽"), 2)
  assert_eq(get_string_width("🧒🏾"), 2)
  assert_eq(get_string_width("🧒🏿"), 2)
  assert_eq(get_string_width("👦"), 2)
  assert_eq(get_string_width("👦🏻"), 2)
  assert_eq(get_string_width("👦🏼"), 2)
  assert_eq(get_string_width("👦🏽"), 2)
  assert_eq(get_string_width("👦🏾"), 2)
  assert_eq(get_string_width("👦🏿"), 2)
  assert_eq(get_string_width("👧"), 2)
  assert_eq(get_string_width("👧🏻"), 2)
  assert_eq(get_string_width("👧🏼"), 2)
  assert_eq(get_string_width("👧🏽"), 2)
  assert_eq(get_string_width("👧🏾"), 2)
  assert_eq(get_string_width("👧🏿"), 2)
  assert_eq(get_string_width("🧑"), 2)
  assert_eq(get_string_width("🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏾"), 2)
  assert_eq(get_string_width("🧑🏿"), 2)
  assert_eq(get_string_width("👱"), 2)
  assert_eq(get_string_width("👱🏻"), 2)
  assert_eq(get_string_width("👱🏼"), 2)
  assert_eq(get_string_width("👱🏽"), 2)
  assert_eq(get_string_width("👱🏾"), 2)
  assert_eq(get_string_width("👱🏿"), 2)
  assert_eq(get_string_width("👨"), 2)
  assert_eq(get_string_width("👨🏻"), 2)
  assert_eq(get_string_width("👨🏼"), 2)
  assert_eq(get_string_width("👨🏽"), 2)
  assert_eq(get_string_width("👨🏾"), 2)
  assert_eq(get_string_width("👨🏿"), 2)
  assert_eq(get_string_width("🧔"), 2)
  assert_eq(get_string_width("🧔🏻"), 2)
  assert_eq(get_string_width("🧔🏼"), 2)
  assert_eq(get_string_width("🧔🏽"), 2)
  assert_eq(get_string_width("🧔🏾"), 2)
  assert_eq(get_string_width("🧔🏿"), 2)
  assert_eq(get_string_width("🧔‍♂️"), 2)
  assert_eq(get_string_width("🧔‍♂"), 3)
  assert_eq(get_string_width("🧔🏻‍♂️"), 2)
  assert_eq(get_string_width("🧔🏻‍♂"), 3)
  assert_eq(get_string_width("🧔🏼‍♂️"), 2)
  assert_eq(get_string_width("🧔🏼‍♂"), 3)
  assert_eq(get_string_width("🧔🏽‍♂️"), 2)
  assert_eq(get_string_width("🧔🏽‍♂"), 3)
  assert_eq(get_string_width("🧔🏾‍♂️"), 2)
  assert_eq(get_string_width("🧔🏾‍♂"), 3)
  assert_eq(get_string_width("🧔🏿‍♂️"), 2)
  assert_eq(get_string_width("🧔🏿‍♂"), 3)
  assert_eq(get_string_width("🧔‍♀️"), 2)
  assert_eq(get_string_width("🧔‍♀"), 3)
  assert_eq(get_string_width("🧔🏻‍♀️"), 2)
  assert_eq(get_string_width("🧔🏻‍♀"), 3)
  assert_eq(get_string_width("🧔🏼‍♀️"), 2)
  assert_eq(get_string_width("🧔🏼‍♀"), 3)
  assert_eq(get_string_width("🧔🏽‍♀️"), 2)
  assert_eq(get_string_width("🧔🏽‍♀"), 3)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 8" {
  assert_eq(get_string_width("🧔🏾‍♀️"), 2)
  assert_eq(get_string_width("🧔🏾‍♀"), 3)
  assert_eq(get_string_width("🧔🏿‍♀️"), 2)
  assert_eq(get_string_width("🧔🏿‍♀"), 3)
  assert_eq(get_string_width("👨‍🦰"), 2)
  assert_eq(get_string_width("👨🏻‍🦰"), 2)
  assert_eq(get_string_width("👨🏼‍🦰"), 2)
  assert_eq(get_string_width("👨🏽‍🦰"), 2)
  assert_eq(get_string_width("👨🏾‍🦰"), 2)
  assert_eq(get_string_width("👨🏿‍🦰"), 2)
  assert_eq(get_string_width("👨‍🦱"), 2)
  assert_eq(get_string_width("👨🏻‍🦱"), 2)
  assert_eq(get_string_width("👨🏼‍🦱"), 2)
  assert_eq(get_string_width("👨🏽‍🦱"), 2)
  assert_eq(get_string_width("👨🏾‍🦱"), 2)
  assert_eq(get_string_width("👨🏿‍🦱"), 2)
  assert_eq(get_string_width("👨‍🦳"), 2)
  assert_eq(get_string_width("👨🏻‍🦳"), 2)
  assert_eq(get_string_width("👨🏼‍🦳"), 2)
  assert_eq(get_string_width("👨🏽‍🦳"), 2)
  assert_eq(get_string_width("👨🏾‍🦳"), 2)
  assert_eq(get_string_width("👨🏿‍🦳"), 2)
  assert_eq(get_string_width("👨‍🦲"), 2)
  assert_eq(get_string_width("👨🏻‍🦲"), 2)
  assert_eq(get_string_width("👨🏼‍🦲"), 2)
  assert_eq(get_string_width("👨🏽‍🦲"), 2)
  assert_eq(get_string_width("👨🏾‍🦲"), 2)
  assert_eq(get_string_width("👨🏿‍🦲"), 2)
  assert_eq(get_string_width("👩🏻"), 2)
  assert_eq(get_string_width("👩🏼"), 2)
  assert_eq(get_string_width("👩🏽"), 2)
  assert_eq(get_string_width("👩🏾"), 2)
  assert_eq(get_string_width("👩🏿"), 2)
  assert_eq(get_string_width("👩‍🦰"), 2)
  assert_eq(get_string_width("👩🏻‍🦰"), 2)
  assert_eq(get_string_width("👩🏼‍🦰"), 2)
  assert_eq(get_string_width("👩🏽‍🦰"), 2)
  assert_eq(get_string_width("👩🏾‍🦰"), 2)
  assert_eq(get_string_width("👩🏿‍🦰"), 2)
  assert_eq(get_string_width("🧑‍🦰"), 2)
  assert_eq(get_string_width("🧑🏻‍🦰"), 2)
  assert_eq(get_string_width("🧑🏼‍🦰"), 2)
  assert_eq(get_string_width("🧑🏽‍🦰"), 2)
  assert_eq(get_string_width("🧑🏾‍🦰"), 2)
  assert_eq(get_string_width("🧑🏿‍🦰"), 2)
  assert_eq(get_string_width("👩‍🦱"), 2)
  assert_eq(get_string_width("👩🏻‍🦱"), 2)
  assert_eq(get_string_width("👩🏼‍🦱"), 2)
  assert_eq(get_string_width("👩🏽‍🦱"), 2)
  assert_eq(get_string_width("👩🏾‍🦱"), 2)
  assert_eq(get_string_width("👩🏿‍🦱"), 2)
  assert_eq(get_string_width("🧑‍🦱"), 2)
  assert_eq(get_string_width("🧑🏻‍🦱"), 2)
  assert_eq(get_string_width("🧑🏼‍🦱"), 2)
  assert_eq(get_string_width("🧑🏽‍🦱"), 2)
  assert_eq(get_string_width("🧑🏾‍🦱"), 2)
  assert_eq(get_string_width("🧑🏿‍🦱"), 2)
  assert_eq(get_string_width("👩‍🦳"), 2)
  assert_eq(get_string_width("👩🏻‍🦳"), 2)
  assert_eq(get_string_width("👩🏼‍🦳"), 2)
  assert_eq(get_string_width("👩🏽‍🦳"), 2)
  assert_eq(get_string_width("👩🏾‍🦳"), 2)
  assert_eq(get_string_width("👩🏿‍🦳"), 2)
  assert_eq(get_string_width("🧑‍🦳"), 2)
  assert_eq(get_string_width("🧑🏻‍🦳"), 2)
  assert_eq(get_string_width("🧑🏼‍🦳"), 2)
  assert_eq(get_string_width("🧑🏽‍🦳"), 2)
  assert_eq(get_string_width("🧑🏾‍🦳"), 2)
  assert_eq(get_string_width("🧑🏿‍🦳"), 2)
  assert_eq(get_string_width("👩‍🦲"), 2)
  assert_eq(get_string_width("👩🏻‍🦲"), 2)
  assert_eq(get_string_width("👩🏼‍🦲"), 2)
  assert_eq(get_string_width("👩🏽‍🦲"), 2)
  assert_eq(get_string_width("👩🏾‍🦲"), 2)
  assert_eq(get_string_width("👩🏿‍🦲"), 2)
  assert_eq(get_string_width("🧑‍🦲"), 2)
  assert_eq(get_string_width("🧑🏻‍🦲"), 2)
  assert_eq(get_string_width("🧑🏼‍🦲"), 2)
  assert_eq(get_string_width("🧑🏽‍🦲"), 2)
  assert_eq(get_string_width("🧑🏾‍🦲"), 2)
  assert_eq(get_string_width("🧑🏿‍🦲"), 2)
  assert_eq(get_string_width("👱‍♀️"), 2)
  assert_eq(get_string_width("👱‍♀"), 3)
  assert_eq(get_string_width("👱🏻‍♀️"), 2)
  assert_eq(get_string_width("👱🏻‍♀"), 3)
  assert_eq(get_string_width("👱🏼‍♀️"), 2)
  assert_eq(get_string_width("👱🏼‍♀"), 3)
  assert_eq(get_string_width("👱🏽‍♀️"), 2)
  assert_eq(get_string_width("👱🏽‍♀"), 3)
  assert_eq(get_string_width("👱🏾‍♀️"), 2)
  assert_eq(get_string_width("👱🏾‍♀"), 3)
  assert_eq(get_string_width("👱🏿‍♀️"), 2)
  assert_eq(get_string_width("👱🏿‍♀"), 3)
  assert_eq(get_string_width("👱‍♂️"), 2)
  assert_eq(get_string_width("👱‍♂"), 3)
  assert_eq(get_string_width("👱🏻‍♂️"), 2)
  assert_eq(get_string_width("👱🏻‍♂"), 3)
  assert_eq(get_string_width("👱🏼‍♂️"), 2)
  assert_eq(get_string_width("👱🏼‍♂"), 3)
  assert_eq(get_string_width("👱🏽‍♂️"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 9" {
  assert_eq(get_string_width("👱🏽‍♂"), 3)
  assert_eq(get_string_width("👱🏾‍♂️"), 2)
  assert_eq(get_string_width("👱🏾‍♂"), 3)
  assert_eq(get_string_width("👱🏿‍♂️"), 2)
  assert_eq(get_string_width("👱🏿‍♂"), 3)
  assert_eq(get_string_width("🧓"), 2)
  assert_eq(get_string_width("🧓🏻"), 2)
  assert_eq(get_string_width("🧓🏼"), 2)
  assert_eq(get_string_width("🧓🏽"), 2)
  assert_eq(get_string_width("🧓🏾"), 2)
  assert_eq(get_string_width("🧓🏿"), 2)
  assert_eq(get_string_width("👴"), 2)
  assert_eq(get_string_width("👴🏻"), 2)
  assert_eq(get_string_width("👴🏼"), 2)
  assert_eq(get_string_width("👴🏽"), 2)
  assert_eq(get_string_width("👴🏾"), 2)
  assert_eq(get_string_width("👴🏿"), 2)
  assert_eq(get_string_width("👵"), 2)
  assert_eq(get_string_width("👵🏻"), 2)
  assert_eq(get_string_width("👵🏼"), 2)
  assert_eq(get_string_width("👵🏽"), 2)
  assert_eq(get_string_width("👵🏾"), 2)
  assert_eq(get_string_width("👵🏿"), 2)
  assert_eq(get_string_width("🙍"), 2)
  assert_eq(get_string_width("🙍🏻"), 2)
  assert_eq(get_string_width("🙍🏼"), 2)
  assert_eq(get_string_width("🙍🏽"), 2)
  assert_eq(get_string_width("🙍🏾"), 2)
  assert_eq(get_string_width("🙍🏿"), 2)
  assert_eq(get_string_width("🙍‍♂️"), 2)
  assert_eq(get_string_width("🙍‍♂"), 3)
  assert_eq(get_string_width("🙍🏻‍♂️"), 2)
  assert_eq(get_string_width("🙍🏻‍♂"), 3)
  assert_eq(get_string_width("🙍🏼‍♂️"), 2)
  assert_eq(get_string_width("🙍🏼‍♂"), 3)
  assert_eq(get_string_width("🙍🏽‍♂️"), 2)
  assert_eq(get_string_width("🙍🏽‍♂"), 3)
  assert_eq(get_string_width("🙍🏾‍♂️"), 2)
  assert_eq(get_string_width("🙍🏾‍♂"), 3)
  assert_eq(get_string_width("🙍🏿‍♂️"), 2)
  assert_eq(get_string_width("🙍🏿‍♂"), 3)
  assert_eq(get_string_width("🙍‍♀️"), 2)
  assert_eq(get_string_width("🙍‍♀"), 3)
  assert_eq(get_string_width("🙍🏻‍♀️"), 2)
  assert_eq(get_string_width("🙍🏻‍♀"), 3)
  assert_eq(get_string_width("🙍🏼‍♀️"), 2)
  assert_eq(get_string_width("🙍🏼‍♀"), 3)
  assert_eq(get_string_width("🙍🏽‍♀️"), 2)
  assert_eq(get_string_width("🙍🏽‍♀"), 3)
  assert_eq(get_string_width("🙍🏾‍♀️"), 2)
  assert_eq(get_string_width("🙍🏾‍♀"), 3)
  assert_eq(get_string_width("🙍🏿‍♀️"), 2)
  assert_eq(get_string_width("🙍🏿‍♀"), 3)
  assert_eq(get_string_width("🙎"), 2)
  assert_eq(get_string_width("🙎🏻"), 2)
  assert_eq(get_string_width("🙎🏼"), 2)
  assert_eq(get_string_width("🙎🏽"), 2)
  assert_eq(get_string_width("🙎🏾"), 2)
  assert_eq(get_string_width("🙎🏿"), 2)
  assert_eq(get_string_width("🙎‍♂️"), 2)
  assert_eq(get_string_width("🙎‍♂"), 3)
  assert_eq(get_string_width("🙎🏻‍♂️"), 2)
  assert_eq(get_string_width("🙎🏻‍♂"), 3)
  assert_eq(get_string_width("🙎🏼‍♂️"), 2)
  assert_eq(get_string_width("🙎🏼‍♂"), 3)
  assert_eq(get_string_width("🙎🏽‍♂️"), 2)
  assert_eq(get_string_width("🙎🏽‍♂"), 3)
  assert_eq(get_string_width("🙎🏾‍♂️"), 2)
  assert_eq(get_string_width("🙎🏾‍♂"), 3)
  assert_eq(get_string_width("🙎🏿‍♂️"), 2)
  assert_eq(get_string_width("🙎🏿‍♂"), 3)
  assert_eq(get_string_width("🙎‍♀️"), 2)
  assert_eq(get_string_width("🙎‍♀"), 3)
  assert_eq(get_string_width("🙎🏻‍♀️"), 2)
  assert_eq(get_string_width("🙎🏻‍♀"), 3)
  assert_eq(get_string_width("🙎🏼‍♀️"), 2)
  assert_eq(get_string_width("🙎🏼‍♀"), 3)
  assert_eq(get_string_width("🙎🏽‍♀️"), 2)
  assert_eq(get_string_width("🙎🏽‍♀"), 3)
  assert_eq(get_string_width("🙎🏾‍♀️"), 2)
  assert_eq(get_string_width("🙎🏾‍♀"), 3)
  assert_eq(get_string_width("🙎🏿‍♀️"), 2)
  assert_eq(get_string_width("🙎🏿‍♀"), 3)
  assert_eq(get_string_width("🙅"), 2)
  assert_eq(get_string_width("🙅🏻"), 2)
  assert_eq(get_string_width("🙅🏼"), 2)
  assert_eq(get_string_width("🙅🏽"), 2)
  assert_eq(get_string_width("🙅🏾"), 2)
  assert_eq(get_string_width("🙅🏿"), 2)
  assert_eq(get_string_width("🙅‍♂️"), 2)
  assert_eq(get_string_width("🙅‍♂"), 3)
  assert_eq(get_string_width("🙅🏻‍♂️"), 2)
  assert_eq(get_string_width("🙅🏻‍♂"), 3)
  assert_eq(get_string_width("🙅🏼‍♂️"), 2)
  assert_eq(get_string_width("🙅🏼‍♂"), 3)
  assert_eq(get_string_width("🙅🏽‍♂️"), 2)
  assert_eq(get_string_width("🙅🏽‍♂"), 3)
  assert_eq(get_string_width("🙅🏾‍♂️"), 2)
  assert_eq(get_string_width("🙅🏾‍♂"), 3)
  assert_eq(get_string_width("🙅🏿‍♂️"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 10" {
  assert_eq(get_string_width("🙅🏿‍♂"), 3)
  assert_eq(get_string_width("🙅‍♀️"), 2)
  assert_eq(get_string_width("🙅‍♀"), 3)
  assert_eq(get_string_width("🙅🏻‍♀️"), 2)
  assert_eq(get_string_width("🙅🏻‍♀"), 3)
  assert_eq(get_string_width("🙅🏼‍♀️"), 2)
  assert_eq(get_string_width("🙅🏼‍♀"), 3)
  assert_eq(get_string_width("🙅🏽‍♀️"), 2)
  assert_eq(get_string_width("🙅🏽‍♀"), 3)
  assert_eq(get_string_width("🙅🏾‍♀️"), 2)
  assert_eq(get_string_width("🙅🏾‍♀"), 3)
  assert_eq(get_string_width("🙅🏿‍♀️"), 2)
  assert_eq(get_string_width("🙅🏿‍♀"), 3)
  assert_eq(get_string_width("🙆"), 2)
  assert_eq(get_string_width("🙆🏻"), 2)
  assert_eq(get_string_width("🙆🏼"), 2)
  assert_eq(get_string_width("🙆🏽"), 2)
  assert_eq(get_string_width("🙆🏾"), 2)
  assert_eq(get_string_width("🙆🏿"), 2)
  assert_eq(get_string_width("🙆‍♂️"), 2)
  assert_eq(get_string_width("🙆‍♂"), 3)
  assert_eq(get_string_width("🙆🏻‍♂️"), 2)
  assert_eq(get_string_width("🙆🏻‍♂"), 3)
  assert_eq(get_string_width("🙆🏼‍♂️"), 2)
  assert_eq(get_string_width("🙆🏼‍♂"), 3)
  assert_eq(get_string_width("🙆🏽‍♂️"), 2)
  assert_eq(get_string_width("🙆🏽‍♂"), 3)
  assert_eq(get_string_width("🙆🏾‍♂️"), 2)
  assert_eq(get_string_width("🙆🏾‍♂"), 3)
  assert_eq(get_string_width("🙆🏿‍♂️"), 2)
  assert_eq(get_string_width("🙆🏿‍♂"), 3)
  assert_eq(get_string_width("🙆‍♀️"), 2)
  assert_eq(get_string_width("🙆‍♀"), 3)
  assert_eq(get_string_width("🙆🏻‍♀️"), 2)
  assert_eq(get_string_width("🙆🏻‍♀"), 3)
  assert_eq(get_string_width("🙆🏼‍♀️"), 2)
  assert_eq(get_string_width("🙆🏼‍♀"), 3)
  assert_eq(get_string_width("🙆🏽‍♀️"), 2)
  assert_eq(get_string_width("🙆🏽‍♀"), 3)
  assert_eq(get_string_width("🙆🏾‍♀️"), 2)
  assert_eq(get_string_width("🙆🏾‍♀"), 3)
  assert_eq(get_string_width("🙆🏿‍♀️"), 2)
  assert_eq(get_string_width("🙆🏿‍♀"), 3)
  assert_eq(get_string_width("💁"), 2)
  assert_eq(get_string_width("💁🏻"), 2)
  assert_eq(get_string_width("💁🏼"), 2)
  assert_eq(get_string_width("💁🏽"), 2)
  assert_eq(get_string_width("💁🏾"), 2)
  assert_eq(get_string_width("💁🏿"), 2)
  assert_eq(get_string_width("💁‍♂️"), 2)
  assert_eq(get_string_width("💁‍♂"), 3)
  assert_eq(get_string_width("💁🏻‍♂️"), 2)
  assert_eq(get_string_width("💁🏻‍♂"), 3)
  assert_eq(get_string_width("💁🏼‍♂️"), 2)
  assert_eq(get_string_width("💁🏼‍♂"), 3)
  assert_eq(get_string_width("💁🏽‍♂️"), 2)
  assert_eq(get_string_width("💁🏽‍♂"), 3)
  assert_eq(get_string_width("💁🏾‍♂️"), 2)
  assert_eq(get_string_width("💁🏾‍♂"), 3)
  assert_eq(get_string_width("💁🏿‍♂️"), 2)
  assert_eq(get_string_width("💁🏿‍♂"), 3)
  assert_eq(get_string_width("💁‍♀️"), 2)
  assert_eq(get_string_width("💁‍♀"), 3)
  assert_eq(get_string_width("💁🏻‍♀️"), 2)
  assert_eq(get_string_width("💁🏻‍♀"), 3)
  assert_eq(get_string_width("💁🏼‍♀️"), 2)
  assert_eq(get_string_width("💁🏼‍♀"), 3)
  assert_eq(get_string_width("💁🏽‍♀️"), 2)
  assert_eq(get_string_width("💁🏽‍♀"), 3)
  assert_eq(get_string_width("💁🏾‍♀️"), 2)
  assert_eq(get_string_width("💁🏾‍♀"), 3)
  assert_eq(get_string_width("💁🏿‍♀️"), 2)
  assert_eq(get_string_width("💁🏿‍♀"), 3)
  assert_eq(get_string_width("🙋"), 2)
  assert_eq(get_string_width("🙋🏻"), 2)
  assert_eq(get_string_width("🙋🏼"), 2)
  assert_eq(get_string_width("🙋🏽"), 2)
  assert_eq(get_string_width("🙋🏾"), 2)
  assert_eq(get_string_width("🙋🏿"), 2)
  assert_eq(get_string_width("🙋‍♂️"), 2)
  assert_eq(get_string_width("🙋‍♂"), 3)
  assert_eq(get_string_width("🙋🏻‍♂️"), 2)
  assert_eq(get_string_width("🙋🏻‍♂"), 3)
  assert_eq(get_string_width("🙋🏼‍♂️"), 2)
  assert_eq(get_string_width("🙋🏼‍♂"), 3)
  assert_eq(get_string_width("🙋🏽‍♂️"), 2)
  assert_eq(get_string_width("🙋🏽‍♂"), 3)
  assert_eq(get_string_width("🙋🏾‍♂️"), 2)
  assert_eq(get_string_width("🙋🏾‍♂"), 3)
  assert_eq(get_string_width("🙋🏿‍♂️"), 2)
  assert_eq(get_string_width("🙋🏿‍♂"), 3)
  assert_eq(get_string_width("🙋‍♀️"), 2)
  assert_eq(get_string_width("🙋‍♀"), 3)
  assert_eq(get_string_width("🙋🏻‍♀️"), 2)
  assert_eq(get_string_width("🙋🏻‍♀"), 3)
  assert_eq(get_string_width("🙋🏼‍♀️"), 2)
  assert_eq(get_string_width("🙋🏼‍♀"), 3)
  assert_eq(get_string_width("🙋🏽‍♀️"), 2)
  assert_eq(get_string_width("🙋🏽‍♀"), 3)
  assert_eq(get_string_width("🙋🏾‍♀️"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 11" {
  assert_eq(get_string_width("🙋🏾‍♀"), 3)
  assert_eq(get_string_width("🙋🏿‍♀️"), 2)
  assert_eq(get_string_width("🙋🏿‍♀"), 3)
  assert_eq(get_string_width("🧏"), 2)
  assert_eq(get_string_width("🧏🏻"), 2)
  assert_eq(get_string_width("🧏🏼"), 2)
  assert_eq(get_string_width("🧏🏽"), 2)
  assert_eq(get_string_width("🧏🏾"), 2)
  assert_eq(get_string_width("🧏🏿"), 2)
  assert_eq(get_string_width("🧏‍♂️"), 2)
  assert_eq(get_string_width("🧏‍♂"), 3)
  assert_eq(get_string_width("🧏🏻‍♂️"), 2)
  assert_eq(get_string_width("🧏🏻‍♂"), 3)
  assert_eq(get_string_width("🧏🏼‍♂️"), 2)
  assert_eq(get_string_width("🧏🏼‍♂"), 3)
  assert_eq(get_string_width("🧏🏽‍♂️"), 2)
  assert_eq(get_string_width("🧏🏽‍♂"), 3)
  assert_eq(get_string_width("🧏🏾‍♂️"), 2)
  assert_eq(get_string_width("🧏🏾‍♂"), 3)
  assert_eq(get_string_width("🧏🏿‍♂️"), 2)
  assert_eq(get_string_width("🧏🏿‍♂"), 3)
  assert_eq(get_string_width("🧏‍♀️"), 2)
  assert_eq(get_string_width("🧏‍♀"), 3)
  assert_eq(get_string_width("🧏🏻‍♀️"), 2)
  assert_eq(get_string_width("🧏🏻‍♀"), 3)
  assert_eq(get_string_width("🧏🏼‍♀️"), 2)
  assert_eq(get_string_width("🧏🏼‍♀"), 3)
  assert_eq(get_string_width("🧏🏽‍♀️"), 2)
  assert_eq(get_string_width("🧏🏽‍♀"), 3)
  assert_eq(get_string_width("🧏🏾‍♀️"), 2)
  assert_eq(get_string_width("🧏🏾‍♀"), 3)
  assert_eq(get_string_width("🧏🏿‍♀️"), 2)
  assert_eq(get_string_width("🧏🏿‍♀"), 3)
  assert_eq(get_string_width("🙇"), 2)
  assert_eq(get_string_width("🙇🏻"), 2)
  assert_eq(get_string_width("🙇🏼"), 2)
  assert_eq(get_string_width("🙇🏽"), 2)
  assert_eq(get_string_width("🙇🏾"), 2)
  assert_eq(get_string_width("🙇🏿"), 2)
  assert_eq(get_string_width("🙇‍♂️"), 2)
  assert_eq(get_string_width("🙇‍♂"), 3)
  assert_eq(get_string_width("🙇🏻‍♂️"), 2)
  assert_eq(get_string_width("🙇🏻‍♂"), 3)
  assert_eq(get_string_width("🙇🏼‍♂️"), 2)
  assert_eq(get_string_width("🙇🏼‍♂"), 3)
  assert_eq(get_string_width("🙇🏽‍♂️"), 2)
  assert_eq(get_string_width("🙇🏽‍♂"), 3)
  assert_eq(get_string_width("🙇🏾‍♂️"), 2)
  assert_eq(get_string_width("🙇🏾‍♂"), 3)
  assert_eq(get_string_width("🙇🏿‍♂️"), 2)
  assert_eq(get_string_width("🙇🏿‍♂"), 3)
  assert_eq(get_string_width("🙇‍♀️"), 2)
  assert_eq(get_string_width("🙇‍♀"), 3)
  assert_eq(get_string_width("🙇🏻‍♀️"), 2)
  assert_eq(get_string_width("🙇🏻‍♀"), 3)
  assert_eq(get_string_width("🙇🏼‍♀️"), 2)
  assert_eq(get_string_width("🙇🏼‍♀"), 3)
  assert_eq(get_string_width("🙇🏽‍♀️"), 2)
  assert_eq(get_string_width("🙇🏽‍♀"), 3)
  assert_eq(get_string_width("🙇🏾‍♀️"), 2)
  assert_eq(get_string_width("🙇🏾‍♀"), 3)
  assert_eq(get_string_width("🙇🏿‍♀️"), 2)
  assert_eq(get_string_width("🙇🏿‍♀"), 3)
  assert_eq(get_string_width("🤦"), 2)
  assert_eq(get_string_width("🤦🏻"), 2)
  assert_eq(get_string_width("🤦🏼"), 2)
  assert_eq(get_string_width("🤦🏽"), 2)
  assert_eq(get_string_width("🤦🏾"), 2)
  assert_eq(get_string_width("🤦🏿"), 2)
  assert_eq(get_string_width("🤦‍♂️"), 2)
  assert_eq(get_string_width("🤦‍♂"), 3)
  assert_eq(get_string_width("🤦🏻‍♂️"), 2)
  assert_eq(get_string_width("🤦🏻‍♂"), 3)
  assert_eq(get_string_width("🤦🏼‍♂️"), 2)
  assert_eq(get_string_width("🤦🏼‍♂"), 3)
  assert_eq(get_string_width("🤦🏽‍♂️"), 2)
  assert_eq(get_string_width("🤦🏽‍♂"), 3)
  assert_eq(get_string_width("🤦🏾‍♂️"), 2)
  assert_eq(get_string_width("🤦🏾‍♂"), 3)
  assert_eq(get_string_width("🤦🏿‍♂️"), 2)
  assert_eq(get_string_width("🤦🏿‍♂"), 3)
  assert_eq(get_string_width("🤦‍♀️"), 2)
  assert_eq(get_string_width("🤦‍♀"), 3)
  assert_eq(get_string_width("🤦🏻‍♀️"), 2)
  assert_eq(get_string_width("🤦🏻‍♀"), 3)
  assert_eq(get_string_width("🤦🏼‍♀️"), 2)
  assert_eq(get_string_width("🤦🏼‍♀"), 3)
  assert_eq(get_string_width("🤦🏽‍♀️"), 2)
  assert_eq(get_string_width("🤦🏽‍♀"), 3)
  assert_eq(get_string_width("🤦🏾‍♀️"), 2)
  assert_eq(get_string_width("🤦🏾‍♀"), 3)
  assert_eq(get_string_width("🤦🏿‍♀️"), 2)
  assert_eq(get_string_width("🤦🏿‍♀"), 3)
  assert_eq(get_string_width("🤷"), 2)
  assert_eq(get_string_width("🤷🏻"), 2)
  assert_eq(get_string_width("🤷🏼"), 2)
  assert_eq(get_string_width("🤷🏽"), 2)
  assert_eq(get_string_width("🤷🏾"), 2)
  assert_eq(get_string_width("🤷🏿"), 2)
  assert_eq(get_string_width("🤷‍♂️"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 12" {
  assert_eq(get_string_width("🤷‍♂"), 3)
  assert_eq(get_string_width("🤷🏻‍♂️"), 2)
  assert_eq(get_string_width("🤷🏻‍♂"), 3)
  assert_eq(get_string_width("🤷🏼‍♂️"), 2)
  assert_eq(get_string_width("🤷🏼‍♂"), 3)
  assert_eq(get_string_width("🤷🏽‍♂️"), 2)
  assert_eq(get_string_width("🤷🏽‍♂"), 3)
  assert_eq(get_string_width("🤷🏾‍♂️"), 2)
  assert_eq(get_string_width("🤷🏾‍♂"), 3)
  assert_eq(get_string_width("🤷🏿‍♂️"), 2)
  assert_eq(get_string_width("🤷🏿‍♂"), 3)
  assert_eq(get_string_width("🤷‍♀️"), 2)
  assert_eq(get_string_width("🤷‍♀"), 3)
  assert_eq(get_string_width("🤷🏻‍♀️"), 2)
  assert_eq(get_string_width("🤷🏻‍♀"), 3)
  assert_eq(get_string_width("🤷🏼‍♀️"), 2)
  assert_eq(get_string_width("🤷🏼‍♀"), 3)
  assert_eq(get_string_width("🤷🏽‍♀️"), 2)
  assert_eq(get_string_width("🤷🏽‍♀"), 3)
  assert_eq(get_string_width("🤷🏾‍♀️"), 2)
  assert_eq(get_string_width("🤷🏾‍♀"), 3)
  assert_eq(get_string_width("🤷🏿‍♀️"), 2)
  assert_eq(get_string_width("🤷🏿‍♀"), 3)
  assert_eq(get_string_width("🧑‍⚕️"), 2)
  assert_eq(get_string_width("🧑‍⚕"), 3)
  assert_eq(get_string_width("🧑🏻‍⚕️"), 2)
  assert_eq(get_string_width("🧑🏻‍⚕"), 3)
  assert_eq(get_string_width("🧑🏼‍⚕️"), 2)
  assert_eq(get_string_width("🧑🏼‍⚕"), 3)
  assert_eq(get_string_width("🧑🏽‍⚕️"), 2)
  assert_eq(get_string_width("🧑🏽‍⚕"), 3)
  assert_eq(get_string_width("🧑🏾‍⚕️"), 2)
  assert_eq(get_string_width("🧑🏾‍⚕"), 3)
  assert_eq(get_string_width("🧑🏿‍⚕️"), 2)
  assert_eq(get_string_width("🧑🏿‍⚕"), 3)
  assert_eq(get_string_width("👨‍⚕️"), 2)
  assert_eq(get_string_width("👨‍⚕"), 3)
  assert_eq(get_string_width("👨🏻‍⚕️"), 2)
  assert_eq(get_string_width("👨🏻‍⚕"), 3)
  assert_eq(get_string_width("👨🏼‍⚕️"), 2)
  assert_eq(get_string_width("👨🏼‍⚕"), 3)
  assert_eq(get_string_width("👨🏽‍⚕️"), 2)
  assert_eq(get_string_width("👨🏽‍⚕"), 3)
  assert_eq(get_string_width("👨🏾‍⚕️"), 2)
  assert_eq(get_string_width("👨🏾‍⚕"), 3)
  assert_eq(get_string_width("👨🏿‍⚕️"), 2)
  assert_eq(get_string_width("👨🏿‍⚕"), 3)
  assert_eq(get_string_width("👩‍⚕️"), 2)
  assert_eq(get_string_width("👩‍⚕"), 3)
  assert_eq(get_string_width("👩🏻‍⚕️"), 2)
  assert_eq(get_string_width("👩🏻‍⚕"), 3)
  assert_eq(get_string_width("👩🏼‍⚕️"), 2)
  assert_eq(get_string_width("👩🏼‍⚕"), 3)
  assert_eq(get_string_width("👩🏽‍⚕️"), 2)
  assert_eq(get_string_width("👩🏽‍⚕"), 3)
  assert_eq(get_string_width("👩🏾‍⚕️"), 2)
  assert_eq(get_string_width("👩🏾‍⚕"), 3)
  assert_eq(get_string_width("👩🏿‍⚕️"), 2)
  assert_eq(get_string_width("👩🏿‍⚕"), 3)
  assert_eq(get_string_width("🧑‍🎓"), 2)
  assert_eq(get_string_width("🧑🏻‍🎓"), 2)
  assert_eq(get_string_width("🧑🏼‍🎓"), 2)
  assert_eq(get_string_width("🧑🏽‍🎓"), 2)
  assert_eq(get_string_width("🧑🏾‍🎓"), 2)
  assert_eq(get_string_width("🧑🏿‍🎓"), 2)
  assert_eq(get_string_width("👨‍🎓"), 2)
  assert_eq(get_string_width("👨🏻‍🎓"), 2)
  assert_eq(get_string_width("👨🏼‍🎓"), 2)
  assert_eq(get_string_width("👨🏽‍🎓"), 2)
  assert_eq(get_string_width("👨🏾‍🎓"), 2)
  assert_eq(get_string_width("👨🏿‍🎓"), 2)
  assert_eq(get_string_width("👩‍🎓"), 2)
  assert_eq(get_string_width("👩🏻‍🎓"), 2)
  assert_eq(get_string_width("👩🏼‍🎓"), 2)
  assert_eq(get_string_width("👩🏽‍🎓"), 2)
  assert_eq(get_string_width("👩🏾‍🎓"), 2)
  assert_eq(get_string_width("👩🏿‍🎓"), 2)
  assert_eq(get_string_width("🧑‍🏫"), 2)
  assert_eq(get_string_width("🧑🏻‍🏫"), 2)
  assert_eq(get_string_width("🧑🏼‍🏫"), 2)
  assert_eq(get_string_width("🧑🏽‍🏫"), 2)
  assert_eq(get_string_width("🧑🏾‍🏫"), 2)
  assert_eq(get_string_width("🧑🏿‍🏫"), 2)
  assert_eq(get_string_width("👨‍🏫"), 2)
  assert_eq(get_string_width("👨🏻‍🏫"), 2)
  assert_eq(get_string_width("👨🏼‍🏫"), 2)
  assert_eq(get_string_width("👨🏽‍🏫"), 2)
  assert_eq(get_string_width("👨🏾‍🏫"), 2)
  assert_eq(get_string_width("👨🏿‍🏫"), 2)
  assert_eq(get_string_width("👩‍🏫"), 2)
  assert_eq(get_string_width("👩🏻‍🏫"), 2)
  assert_eq(get_string_width("👩🏼‍🏫"), 2)
  assert_eq(get_string_width("👩🏽‍🏫"), 2)
  assert_eq(get_string_width("👩🏾‍🏫"), 2)
  assert_eq(get_string_width("👩🏿‍🏫"), 2)
  assert_eq(get_string_width("🧑‍⚖️"), 2)
  assert_eq(get_string_width("🧑‍⚖"), 3)
  assert_eq(get_string_width("🧑🏻‍⚖️"), 2)
  assert_eq(get_string_width("🧑🏻‍⚖"), 3)
  assert_eq(get_string_width("🧑🏼‍⚖️"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 13" {
  assert_eq(get_string_width("🧑🏼‍⚖"), 3)
  assert_eq(get_string_width("🧑🏽‍⚖️"), 2)
  assert_eq(get_string_width("🧑🏽‍⚖"), 3)
  assert_eq(get_string_width("🧑🏾‍⚖️"), 2)
  assert_eq(get_string_width("🧑🏾‍⚖"), 3)
  assert_eq(get_string_width("🧑🏿‍⚖️"), 2)
  assert_eq(get_string_width("🧑🏿‍⚖"), 3)
  assert_eq(get_string_width("👨‍⚖️"), 2)
  assert_eq(get_string_width("👨‍⚖"), 3)
  assert_eq(get_string_width("👨🏻‍⚖️"), 2)
  assert_eq(get_string_width("👨🏻‍⚖"), 3)
  assert_eq(get_string_width("👨🏼‍⚖️"), 2)
  assert_eq(get_string_width("👨🏼‍⚖"), 3)
  assert_eq(get_string_width("👨🏽‍⚖️"), 2)
  assert_eq(get_string_width("👨🏽‍⚖"), 3)
  assert_eq(get_string_width("👨🏾‍⚖️"), 2)
  assert_eq(get_string_width("👨🏾‍⚖"), 3)
  assert_eq(get_string_width("👨🏿‍⚖️"), 2)
  assert_eq(get_string_width("👨🏿‍⚖"), 3)
  assert_eq(get_string_width("👩‍⚖️"), 2)
  assert_eq(get_string_width("👩‍⚖"), 3)
  assert_eq(get_string_width("👩🏻‍⚖️"), 2)
  assert_eq(get_string_width("👩🏻‍⚖"), 3)
  assert_eq(get_string_width("👩🏼‍⚖️"), 2)
  assert_eq(get_string_width("👩🏼‍⚖"), 3)
  assert_eq(get_string_width("👩🏽‍⚖️"), 2)
  assert_eq(get_string_width("👩🏽‍⚖"), 3)
  assert_eq(get_string_width("👩🏾‍⚖️"), 2)
  assert_eq(get_string_width("👩🏾‍⚖"), 3)
  assert_eq(get_string_width("👩🏿‍⚖️"), 2)
  assert_eq(get_string_width("👩🏿‍⚖"), 3)
  assert_eq(get_string_width("🧑‍🌾"), 2)
  assert_eq(get_string_width("🧑🏻‍🌾"), 2)
  assert_eq(get_string_width("🧑🏼‍🌾"), 2)
  assert_eq(get_string_width("🧑🏽‍🌾"), 2)
  assert_eq(get_string_width("🧑🏾‍🌾"), 2)
  assert_eq(get_string_width("🧑🏿‍🌾"), 2)
  assert_eq(get_string_width("👨‍🌾"), 2)
  assert_eq(get_string_width("👨🏻‍🌾"), 2)
  assert_eq(get_string_width("👨🏼‍🌾"), 2)
  assert_eq(get_string_width("👨🏽‍🌾"), 2)
  assert_eq(get_string_width("👨🏾‍🌾"), 2)
  assert_eq(get_string_width("👨🏿‍🌾"), 2)
  assert_eq(get_string_width("👩‍🌾"), 2)
  assert_eq(get_string_width("👩🏻‍🌾"), 2)
  assert_eq(get_string_width("👩🏼‍🌾"), 2)
  assert_eq(get_string_width("👩🏽‍🌾"), 2)
  assert_eq(get_string_width("👩🏾‍🌾"), 2)
  assert_eq(get_string_width("👩🏿‍🌾"), 2)
  assert_eq(get_string_width("🧑‍🍳"), 2)
  assert_eq(get_string_width("🧑🏻‍🍳"), 2)
  assert_eq(get_string_width("🧑🏼‍🍳"), 2)
  assert_eq(get_string_width("🧑🏽‍🍳"), 2)
  assert_eq(get_string_width("🧑🏾‍🍳"), 2)
  assert_eq(get_string_width("🧑🏿‍🍳"), 2)
  assert_eq(get_string_width("👨‍🍳"), 2)
  assert_eq(get_string_width("👨🏻‍🍳"), 2)
  assert_eq(get_string_width("👨🏼‍🍳"), 2)
  assert_eq(get_string_width("👨🏽‍🍳"), 2)
  assert_eq(get_string_width("👨🏾‍🍳"), 2)
  assert_eq(get_string_width("👨🏿‍🍳"), 2)
  assert_eq(get_string_width("👩‍🍳"), 2)
  assert_eq(get_string_width("👩🏻‍🍳"), 2)
  assert_eq(get_string_width("👩🏼‍🍳"), 2)
  assert_eq(get_string_width("👩🏽‍🍳"), 2)
  assert_eq(get_string_width("👩🏾‍🍳"), 2)
  assert_eq(get_string_width("👩🏿‍🍳"), 2)
  assert_eq(get_string_width("🧑‍🔧"), 2)
  assert_eq(get_string_width("🧑🏻‍🔧"), 2)
  assert_eq(get_string_width("🧑🏼‍🔧"), 2)
  assert_eq(get_string_width("🧑🏽‍🔧"), 2)
  assert_eq(get_string_width("🧑🏾‍🔧"), 2)
  assert_eq(get_string_width("🧑🏿‍🔧"), 2)
  assert_eq(get_string_width("👨‍🔧"), 2)
  assert_eq(get_string_width("👨🏻‍🔧"), 2)
  assert_eq(get_string_width("👨🏼‍🔧"), 2)
  assert_eq(get_string_width("👨🏽‍🔧"), 2)
  assert_eq(get_string_width("👨🏾‍🔧"), 2)
  assert_eq(get_string_width("👨🏿‍🔧"), 2)
  assert_eq(get_string_width("👩‍🔧"), 2)
  assert_eq(get_string_width("👩🏻‍🔧"), 2)
  assert_eq(get_string_width("👩🏼‍🔧"), 2)
  assert_eq(get_string_width("👩🏽‍🔧"), 2)
  assert_eq(get_string_width("👩🏾‍🔧"), 2)
  assert_eq(get_string_width("👩🏿‍🔧"), 2)
  assert_eq(get_string_width("🧑‍🏭"), 2)
  assert_eq(get_string_width("🧑🏻‍🏭"), 2)
  assert_eq(get_string_width("🧑🏼‍🏭"), 2)
  assert_eq(get_string_width("🧑🏽‍🏭"), 2)
  assert_eq(get_string_width("🧑🏾‍🏭"), 2)
  assert_eq(get_string_width("🧑🏿‍🏭"), 2)
  assert_eq(get_string_width("👨‍🏭"), 2)
  assert_eq(get_string_width("👨🏻‍🏭"), 2)
  assert_eq(get_string_width("👨🏼‍🏭"), 2)
  assert_eq(get_string_width("👨🏽‍🏭"), 2)
  assert_eq(get_string_width("👨🏾‍🏭"), 2)
  assert_eq(get_string_width("👨🏿‍🏭"), 2)
  assert_eq(get_string_width("👩‍🏭"), 2)
  assert_eq(get_string_width("👩🏻‍🏭"), 2)
  assert_eq(get_string_width("👩🏼‍🏭"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 14" {
  assert_eq(get_string_width("👩🏽‍🏭"), 2)
  assert_eq(get_string_width("👩🏾‍🏭"), 2)
  assert_eq(get_string_width("👩🏿‍🏭"), 2)
  assert_eq(get_string_width("🧑‍💼"), 2)
  assert_eq(get_string_width("🧑🏻‍💼"), 2)
  assert_eq(get_string_width("🧑🏼‍💼"), 2)
  assert_eq(get_string_width("🧑🏽‍💼"), 2)
  assert_eq(get_string_width("🧑🏾‍💼"), 2)
  assert_eq(get_string_width("🧑🏿‍💼"), 2)
  assert_eq(get_string_width("👨‍💼"), 2)
  assert_eq(get_string_width("👨🏻‍💼"), 2)
  assert_eq(get_string_width("👨🏼‍💼"), 2)
  assert_eq(get_string_width("👨🏽‍💼"), 2)
  assert_eq(get_string_width("👨🏾‍💼"), 2)
  assert_eq(get_string_width("👨🏿‍💼"), 2)
  assert_eq(get_string_width("👩‍💼"), 2)
  assert_eq(get_string_width("👩🏻‍💼"), 2)
  assert_eq(get_string_width("👩🏼‍💼"), 2)
  assert_eq(get_string_width("👩🏽‍💼"), 2)
  assert_eq(get_string_width("👩🏾‍💼"), 2)
  assert_eq(get_string_width("👩🏿‍💼"), 2)
  assert_eq(get_string_width("🧑‍🔬"), 2)
  assert_eq(get_string_width("🧑🏻‍🔬"), 2)
  assert_eq(get_string_width("🧑🏼‍🔬"), 2)
  assert_eq(get_string_width("🧑🏽‍🔬"), 2)
  assert_eq(get_string_width("🧑🏾‍🔬"), 2)
  assert_eq(get_string_width("🧑🏿‍🔬"), 2)
  assert_eq(get_string_width("👨‍🔬"), 2)
  assert_eq(get_string_width("👨🏻‍🔬"), 2)
  assert_eq(get_string_width("👨🏼‍🔬"), 2)
  assert_eq(get_string_width("👨🏽‍🔬"), 2)
  assert_eq(get_string_width("👨🏾‍🔬"), 2)
  assert_eq(get_string_width("👨🏿‍🔬"), 2)
  assert_eq(get_string_width("👩🏻‍🔬"), 2)
  assert_eq(get_string_width("👩🏼‍🔬"), 2)
  assert_eq(get_string_width("👩🏽‍🔬"), 2)
  assert_eq(get_string_width("👩🏾‍🔬"), 2)
  assert_eq(get_string_width("👩🏿‍🔬"), 2)
  assert_eq(get_string_width("🧑‍💻"), 2)
  assert_eq(get_string_width("🧑🏻‍💻"), 2)
  assert_eq(get_string_width("🧑🏼‍💻"), 2)
  assert_eq(get_string_width("🧑🏽‍💻"), 2)
  assert_eq(get_string_width("🧑🏾‍💻"), 2)
  assert_eq(get_string_width("🧑🏿‍💻"), 2)
  assert_eq(get_string_width("👨‍💻"), 2)
  assert_eq(get_string_width("👨🏻‍💻"), 2)
  assert_eq(get_string_width("👨🏼‍💻"), 2)
  assert_eq(get_string_width("👨🏽‍💻"), 2)
  assert_eq(get_string_width("👨🏾‍💻"), 2)
  assert_eq(get_string_width("👨🏿‍💻"), 2)
  assert_eq(get_string_width("👩‍💻"), 2)
  assert_eq(get_string_width("👩🏻‍💻"), 2)
  assert_eq(get_string_width("👩🏼‍💻"), 2)
  assert_eq(get_string_width("👩🏽‍💻"), 2)
  assert_eq(get_string_width("👩🏾‍💻"), 2)
  assert_eq(get_string_width("👩🏿‍💻"), 2)
  assert_eq(get_string_width("🧑‍🎤"), 2)
  assert_eq(get_string_width("🧑🏻‍🎤"), 2)
  assert_eq(get_string_width("🧑🏼‍🎤"), 2)
  assert_eq(get_string_width("🧑🏽‍🎤"), 2)
  assert_eq(get_string_width("🧑🏾‍🎤"), 2)
  assert_eq(get_string_width("🧑🏿‍🎤"), 2)
  assert_eq(get_string_width("👨‍🎤"), 2)
  assert_eq(get_string_width("👨🏻‍🎤"), 2)
  assert_eq(get_string_width("👨🏼‍🎤"), 2)
  assert_eq(get_string_width("👨🏽‍🎤"), 2)
  assert_eq(get_string_width("👨🏾‍🎤"), 2)
  assert_eq(get_string_width("👨🏿‍🎤"), 2)
  assert_eq(get_string_width("👩‍🎤"), 2)
  assert_eq(get_string_width("👩🏻‍🎤"), 2)
  assert_eq(get_string_width("👩🏼‍🎤"), 2)
  assert_eq(get_string_width("👩🏽‍🎤"), 2)
  assert_eq(get_string_width("👩🏾‍🎤"), 2)
  assert_eq(get_string_width("👩🏿‍🎤"), 2)
  assert_eq(get_string_width("🧑‍🎨"), 2)
  assert_eq(get_string_width("🧑🏻‍🎨"), 2)
  assert_eq(get_string_width("🧑🏼‍🎨"), 2)
  assert_eq(get_string_width("🧑🏽‍🎨"), 2)
  assert_eq(get_string_width("🧑🏾‍🎨"), 2)
  assert_eq(get_string_width("🧑🏿‍🎨"), 2)
  assert_eq(get_string_width("👨‍🎨"), 2)
  assert_eq(get_string_width("👨🏻‍🎨"), 2)
  assert_eq(get_string_width("👨🏼‍🎨"), 2)
  assert_eq(get_string_width("👨🏽‍🎨"), 2)
  assert_eq(get_string_width("👨🏾‍🎨"), 2)
  assert_eq(get_string_width("👨🏿‍🎨"), 2)
  assert_eq(get_string_width("👩‍🎨"), 2)
  assert_eq(get_string_width("👩🏻‍🎨"), 2)
  assert_eq(get_string_width("👩🏼‍🎨"), 2)
  assert_eq(get_string_width("👩🏽‍🎨"), 2)
  assert_eq(get_string_width("👩🏾‍🎨"), 2)
  assert_eq(get_string_width("👩🏿‍🎨"), 2)
  assert_eq(get_string_width("🧑‍✈️"), 2)
  assert_eq(get_string_width("🧑‍✈"), 3)
  assert_eq(get_string_width("🧑🏻‍✈️"), 2)
  assert_eq(get_string_width("🧑🏻‍✈"), 3)
  assert_eq(get_string_width("🧑🏼‍✈️"), 2)
  assert_eq(get_string_width("🧑🏼‍✈"), 3)
  assert_eq(get_string_width("🧑🏽‍✈️"), 2)
  assert_eq(get_string_width("🧑🏽‍✈"), 3)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 15" {
  assert_eq(get_string_width("🧑🏾‍✈️"), 2)
  assert_eq(get_string_width("🧑🏾‍✈"), 3)
  assert_eq(get_string_width("🧑🏿‍✈️"), 2)
  assert_eq(get_string_width("🧑🏿‍✈"), 3)
  assert_eq(get_string_width("👨‍✈️"), 2)
  assert_eq(get_string_width("👨‍✈"), 3)
  assert_eq(get_string_width("👨🏻‍✈️"), 2)
  assert_eq(get_string_width("👨🏻‍✈"), 3)
  assert_eq(get_string_width("👨🏼‍✈️"), 2)
  assert_eq(get_string_width("👨🏼‍✈"), 3)
  assert_eq(get_string_width("👨🏽‍✈️"), 2)
  assert_eq(get_string_width("👨🏽‍✈"), 3)
  assert_eq(get_string_width("👨🏾‍✈️"), 2)
  assert_eq(get_string_width("👨🏾‍✈"), 3)
  assert_eq(get_string_width("👨🏿‍✈️"), 2)
  assert_eq(get_string_width("👨🏿‍✈"), 3)
  assert_eq(get_string_width("👩‍✈️"), 2)
  assert_eq(get_string_width("👩‍✈"), 3)
  assert_eq(get_string_width("👩🏻‍✈️"), 2)
  assert_eq(get_string_width("👩🏻‍✈"), 3)
  assert_eq(get_string_width("👩🏼‍✈️"), 2)
  assert_eq(get_string_width("👩🏼‍✈"), 3)
  assert_eq(get_string_width("👩🏽‍✈️"), 2)
  assert_eq(get_string_width("👩🏽‍✈"), 3)
  assert_eq(get_string_width("👩🏾‍✈️"), 2)
  assert_eq(get_string_width("👩🏾‍✈"), 3)
  assert_eq(get_string_width("👩🏿‍✈️"), 2)
  assert_eq(get_string_width("👩🏿‍✈"), 3)
  assert_eq(get_string_width("🧑‍🚀"), 2)
  assert_eq(get_string_width("🧑🏻‍🚀"), 2)
  assert_eq(get_string_width("🧑🏼‍🚀"), 2)
  assert_eq(get_string_width("🧑🏽‍🚀"), 2)
  assert_eq(get_string_width("🧑🏾‍🚀"), 2)
  assert_eq(get_string_width("🧑🏿‍🚀"), 2)
  assert_eq(get_string_width("👨‍🚀"), 2)
  assert_eq(get_string_width("👨🏻‍🚀"), 2)
  assert_eq(get_string_width("👨🏼‍🚀"), 2)
  assert_eq(get_string_width("👨🏽‍🚀"), 2)
  assert_eq(get_string_width("👨🏾‍🚀"), 2)
  assert_eq(get_string_width("👨🏿‍🚀"), 2)
  assert_eq(get_string_width("👩‍🚀"), 2)
  assert_eq(get_string_width("👩🏻‍🚀"), 2)
  assert_eq(get_string_width("👩🏼‍🚀"), 2)
  assert_eq(get_string_width("👩🏽‍🚀"), 2)
  assert_eq(get_string_width("👩🏾‍🚀"), 2)
  assert_eq(get_string_width("👩🏿‍🚀"), 2)
  assert_eq(get_string_width("🧑‍🚒"), 2)
  assert_eq(get_string_width("🧑🏻‍🚒"), 2)
  assert_eq(get_string_width("🧑🏼‍🚒"), 2)
  assert_eq(get_string_width("🧑🏽‍🚒"), 2)
  assert_eq(get_string_width("🧑🏾‍🚒"), 2)
  assert_eq(get_string_width("🧑🏿‍🚒"), 2)
  assert_eq(get_string_width("👨‍🚒"), 2)
  assert_eq(get_string_width("👨🏻‍🚒"), 2)
  assert_eq(get_string_width("👨🏼‍🚒"), 2)
  assert_eq(get_string_width("👨🏽‍🚒"), 2)
  assert_eq(get_string_width("👨🏾‍🚒"), 2)
  assert_eq(get_string_width("👨🏿‍🚒"), 2)
  assert_eq(get_string_width("👩‍🚒"), 2)
  assert_eq(get_string_width("👩🏻‍🚒"), 2)
  assert_eq(get_string_width("👩🏼‍🚒"), 2)
  assert_eq(get_string_width("👩🏽‍🚒"), 2)
  assert_eq(get_string_width("👩🏾‍🚒"), 2)
  assert_eq(get_string_width("👩🏿‍🚒"), 2)
  assert_eq(get_string_width("👮"), 2)
  assert_eq(get_string_width("👮🏻"), 2)
  assert_eq(get_string_width("👮🏼"), 2)
  assert_eq(get_string_width("👮🏽"), 2)
  assert_eq(get_string_width("👮🏾"), 2)
  assert_eq(get_string_width("👮🏿"), 2)
  assert_eq(get_string_width("👮‍♂️"), 2)
  assert_eq(get_string_width("👮‍♂"), 3)
  assert_eq(get_string_width("👮🏻‍♂️"), 2)
  assert_eq(get_string_width("👮🏻‍♂"), 3)
  assert_eq(get_string_width("👮🏼‍♂️"), 2)
  assert_eq(get_string_width("👮🏼‍♂"), 3)
  assert_eq(get_string_width("👮🏽‍♂️"), 2)
  assert_eq(get_string_width("👮🏽‍♂"), 3)
  assert_eq(get_string_width("👮🏾‍♂️"), 2)
  assert_eq(get_string_width("👮🏾‍♂"), 3)
  assert_eq(get_string_width("👮🏿‍♂️"), 2)
  assert_eq(get_string_width("👮🏿‍♂"), 3)
  assert_eq(get_string_width("👮‍♀️"), 2)
  assert_eq(get_string_width("👮‍♀"), 3)
  assert_eq(get_string_width("👮🏻‍♀️"), 2)
  assert_eq(get_string_width("👮🏻‍♀"), 3)
  assert_eq(get_string_width("👮🏼‍♀️"), 2)
  assert_eq(get_string_width("👮🏼‍♀"), 3)
  assert_eq(get_string_width("👮🏽‍♀️"), 2)
  assert_eq(get_string_width("👮🏽‍♀"), 3)
  assert_eq(get_string_width("👮🏾‍♀️"), 2)
  assert_eq(get_string_width("👮🏾‍♀"), 3)
  assert_eq(get_string_width("👮🏿‍♀️"), 2)
  assert_eq(get_string_width("👮🏿‍♀"), 3)
  assert_eq(get_string_width("🕵️"), 2)
  assert_eq(get_string_width("🕵"), 1)
  assert_eq(get_string_width("🕵🏻"), 2)
  assert_eq(get_string_width("🕵🏼"), 2)
  assert_eq(get_string_width("🕵🏽"), 2)
  assert_eq(get_string_width("🕵🏾"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 16" {
  assert_eq(get_string_width("🕵🏿"), 2)
  assert_eq(get_string_width("🕵️‍♂️"), 2)
  assert_eq(get_string_width("🕵‍♂️"), 3)
  assert_eq(get_string_width("🕵️‍♂"), 3)
  assert_eq(get_string_width("🕵‍♂"), 2)
  assert_eq(get_string_width("🕵🏻‍♂️"), 2)
  assert_eq(get_string_width("🕵🏻‍♂"), 3)
  assert_eq(get_string_width("🕵🏼‍♂️"), 2)
  assert_eq(get_string_width("🕵🏼‍♂"), 3)
  assert_eq(get_string_width("🕵🏽‍♂️"), 2)
  assert_eq(get_string_width("🕵🏽‍♂"), 3)
  assert_eq(get_string_width("🕵🏾‍♂️"), 2)
  assert_eq(get_string_width("🕵🏾‍♂"), 3)
  assert_eq(get_string_width("🕵🏿‍♂️"), 2)
  assert_eq(get_string_width("🕵🏿‍♂"), 3)
  assert_eq(get_string_width("🕵️‍♀️"), 2)
  assert_eq(get_string_width("🕵‍♀️"), 3)
  assert_eq(get_string_width("🕵️‍♀"), 3)
  assert_eq(get_string_width("🕵‍♀"), 2)
  assert_eq(get_string_width("🕵🏻‍♀️"), 2)
  assert_eq(get_string_width("🕵🏻‍♀"), 3)
  assert_eq(get_string_width("🕵🏼‍♀️"), 2)
  assert_eq(get_string_width("🕵🏼‍♀"), 3)
  assert_eq(get_string_width("🕵🏽‍♀️"), 2)
  assert_eq(get_string_width("🕵🏽‍♀"), 3)
  assert_eq(get_string_width("🕵🏾‍♀️"), 2)
  assert_eq(get_string_width("🕵🏾‍♀"), 3)
  assert_eq(get_string_width("🕵🏿‍♀️"), 2)
  assert_eq(get_string_width("🕵🏿‍♀"), 3)
  assert_eq(get_string_width("💂"), 2)
  assert_eq(get_string_width("💂🏻"), 2)
  assert_eq(get_string_width("💂🏼"), 2)
  assert_eq(get_string_width("💂🏽"), 2)
  assert_eq(get_string_width("💂🏾"), 2)
  assert_eq(get_string_width("💂🏿"), 2)
  assert_eq(get_string_width("💂‍♂️"), 2)
  assert_eq(get_string_width("💂‍♂"), 3)
  assert_eq(get_string_width("💂🏻‍♂️"), 2)
  assert_eq(get_string_width("💂🏻‍♂"), 3)
  assert_eq(get_string_width("💂🏼‍♂️"), 2)
  assert_eq(get_string_width("💂🏼‍♂"), 3)
  assert_eq(get_string_width("💂🏽‍♂️"), 2)
  assert_eq(get_string_width("💂🏽‍♂"), 3)
  assert_eq(get_string_width("💂🏾‍♂️"), 2)
  assert_eq(get_string_width("💂🏾‍♂"), 3)
  assert_eq(get_string_width("💂🏿‍♂️"), 2)
  assert_eq(get_string_width("💂🏿‍♂"), 3)
  assert_eq(get_string_width("💂‍♀️"), 2)
  assert_eq(get_string_width("💂‍♀"), 3)
  assert_eq(get_string_width("💂🏻‍♀️"), 2)
  assert_eq(get_string_width("💂🏻‍♀"), 3)
  assert_eq(get_string_width("💂🏼‍♀️"), 2)
  assert_eq(get_string_width("💂🏼‍♀"), 3)
  assert_eq(get_string_width("💂🏽‍♀️"), 2)
  assert_eq(get_string_width("💂🏽‍♀"), 3)
  assert_eq(get_string_width("💂🏾‍♀️"), 2)
  assert_eq(get_string_width("💂🏾‍♀"), 3)
  assert_eq(get_string_width("💂🏿‍♀️"), 2)
  assert_eq(get_string_width("💂🏿‍♀"), 3)
  assert_eq(get_string_width("🥷"), 2)
  assert_eq(get_string_width("🥷🏻"), 2)
  assert_eq(get_string_width("🥷🏼"), 2)
  assert_eq(get_string_width("🥷🏽"), 2)
  assert_eq(get_string_width("🥷🏾"), 2)
  assert_eq(get_string_width("🥷🏿"), 2)
  assert_eq(get_string_width("👷"), 2)
  assert_eq(get_string_width("👷🏻"), 2)
  assert_eq(get_string_width("👷🏼"), 2)
  assert_eq(get_string_width("👷🏽"), 2)
  assert_eq(get_string_width("👷🏾"), 2)
  assert_eq(get_string_width("👷🏿"), 2)
  assert_eq(get_string_width("👷‍♂️"), 2)
  assert_eq(get_string_width("👷‍♂"), 3)
  assert_eq(get_string_width("👷🏻‍♂️"), 2)
  assert_eq(get_string_width("👷🏻‍♂"), 3)
  assert_eq(get_string_width("👷🏼‍♂️"), 2)
  assert_eq(get_string_width("👷🏼‍♂"), 3)
  assert_eq(get_string_width("👷🏽‍♂️"), 2)
  assert_eq(get_string_width("👷🏽‍♂"), 3)
  assert_eq(get_string_width("👷🏾‍♂️"), 2)
  assert_eq(get_string_width("👷🏾‍♂"), 3)
  assert_eq(get_string_width("👷🏿‍♂️"), 2)
  assert_eq(get_string_width("👷🏿‍♂"), 3)
  assert_eq(get_string_width("👷‍♀️"), 2)
  assert_eq(get_string_width("👷‍♀"), 3)
  assert_eq(get_string_width("👷🏻‍♀️"), 2)
  assert_eq(get_string_width("👷🏻‍♀"), 3)
  assert_eq(get_string_width("👷🏼‍♀️"), 2)
  assert_eq(get_string_width("👷🏼‍♀"), 3)
  assert_eq(get_string_width("👷🏽‍♀️"), 2)
  assert_eq(get_string_width("👷🏽‍♀"), 3)
  assert_eq(get_string_width("👷🏾‍♀️"), 2)
  assert_eq(get_string_width("👷🏾‍♀"), 3)
  assert_eq(get_string_width("👷🏿‍♀️"), 2)
  assert_eq(get_string_width("👷🏿‍♀"), 3)
  assert_eq(get_string_width("🫅"), 2)
  assert_eq(get_string_width("🫅🏻"), 2)
  assert_eq(get_string_width("🫅🏼"), 2)
  assert_eq(get_string_width("🫅🏽"), 2)
  assert_eq(get_string_width("🫅🏾"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 17" {
  assert_eq(get_string_width("🫅🏿"), 2)
  assert_eq(get_string_width("🤴"), 2)
  assert_eq(get_string_width("🤴🏻"), 2)
  assert_eq(get_string_width("🤴🏼"), 2)
  assert_eq(get_string_width("🤴🏽"), 2)
  assert_eq(get_string_width("🤴🏾"), 2)
  assert_eq(get_string_width("🤴🏿"), 2)
  assert_eq(get_string_width("👸"), 2)
  assert_eq(get_string_width("👸🏻"), 2)
  assert_eq(get_string_width("👸🏼"), 2)
  assert_eq(get_string_width("👸🏽"), 2)
  assert_eq(get_string_width("👸🏾"), 2)
  assert_eq(get_string_width("👸🏿"), 2)
  assert_eq(get_string_width("👳"), 2)
  assert_eq(get_string_width("👳🏻"), 2)
  assert_eq(get_string_width("👳🏼"), 2)
  assert_eq(get_string_width("👳🏽"), 2)
  assert_eq(get_string_width("👳🏾"), 2)
  assert_eq(get_string_width("👳🏿"), 2)
  assert_eq(get_string_width("👳‍♂️"), 2)
  assert_eq(get_string_width("👳‍♂"), 3)
  assert_eq(get_string_width("👳🏻‍♂️"), 2)
  assert_eq(get_string_width("👳🏻‍♂"), 3)
  assert_eq(get_string_width("👳🏼‍♂️"), 2)
  assert_eq(get_string_width("👳🏼‍♂"), 3)
  assert_eq(get_string_width("👳🏽‍♂️"), 2)
  assert_eq(get_string_width("👳🏽‍♂"), 3)
  assert_eq(get_string_width("👳🏾‍♂️"), 2)
  assert_eq(get_string_width("👳🏾‍♂"), 3)
  assert_eq(get_string_width("👳🏿‍♂️"), 2)
  assert_eq(get_string_width("👳🏿‍♂"), 3)
  assert_eq(get_string_width("👳‍♀️"), 2)
  assert_eq(get_string_width("👳‍♀"), 3)
  assert_eq(get_string_width("👳🏻‍♀️"), 2)
  assert_eq(get_string_width("👳🏻‍♀"), 3)
  assert_eq(get_string_width("👳🏼‍♀️"), 2)
  assert_eq(get_string_width("👳🏼‍♀"), 3)
  assert_eq(get_string_width("👳🏽‍♀️"), 2)
  assert_eq(get_string_width("👳🏽‍♀"), 3)
  assert_eq(get_string_width("👳🏾‍♀️"), 2)
  assert_eq(get_string_width("👳🏾‍♀"), 3)
  assert_eq(get_string_width("👳🏿‍♀️"), 2)
  assert_eq(get_string_width("👳🏿‍♀"), 3)
  assert_eq(get_string_width("👲"), 2)
  assert_eq(get_string_width("👲🏻"), 2)
  assert_eq(get_string_width("👲🏼"), 2)
  assert_eq(get_string_width("👲🏽"), 2)
  assert_eq(get_string_width("👲🏾"), 2)
  assert_eq(get_string_width("👲🏿"), 2)
  assert_eq(get_string_width("🧕"), 2)
  assert_eq(get_string_width("🧕🏻"), 2)
  assert_eq(get_string_width("🧕🏼"), 2)
  assert_eq(get_string_width("🧕🏽"), 2)
  assert_eq(get_string_width("🧕🏾"), 2)
  assert_eq(get_string_width("🧕🏿"), 2)
  assert_eq(get_string_width("🤵"), 2)
  assert_eq(get_string_width("🤵🏻"), 2)
  assert_eq(get_string_width("🤵🏼"), 2)
  assert_eq(get_string_width("🤵🏽"), 2)
  assert_eq(get_string_width("🤵🏾"), 2)
  assert_eq(get_string_width("🤵🏿"), 2)
  assert_eq(get_string_width("🤵‍♂️"), 2)
  assert_eq(get_string_width("🤵‍♂"), 3)
  assert_eq(get_string_width("🤵🏻‍♂️"), 2)
  assert_eq(get_string_width("🤵🏻‍♂"), 3)
  assert_eq(get_string_width("🤵🏼‍♂️"), 2)
  assert_eq(get_string_width("🤵🏼‍♂"), 3)
  assert_eq(get_string_width("🤵🏽‍♂️"), 2)
  assert_eq(get_string_width("🤵🏽‍♂"), 3)
  assert_eq(get_string_width("🤵🏾‍♂️"), 2)
  assert_eq(get_string_width("🤵🏾‍♂"), 3)
  assert_eq(get_string_width("🤵🏿‍♂️"), 2)
  assert_eq(get_string_width("🤵🏿‍♂"), 3)
  assert_eq(get_string_width("🤵‍♀️"), 2)
  assert_eq(get_string_width("🤵‍♀"), 3)
  assert_eq(get_string_width("🤵🏻‍♀️"), 2)
  assert_eq(get_string_width("🤵🏻‍♀"), 3)
  assert_eq(get_string_width("🤵🏼‍♀️"), 2)
  assert_eq(get_string_width("🤵🏼‍♀"), 3)
  assert_eq(get_string_width("🤵🏽‍♀️"), 2)
  assert_eq(get_string_width("🤵🏽‍♀"), 3)
  assert_eq(get_string_width("🤵🏾‍♀️"), 2)
  assert_eq(get_string_width("🤵🏾‍♀"), 3)
  assert_eq(get_string_width("🤵🏿‍♀️"), 2)
  assert_eq(get_string_width("🤵🏿‍♀"), 3)
  assert_eq(get_string_width("👰"), 2)
  assert_eq(get_string_width("👰🏻"), 2)
  assert_eq(get_string_width("👰🏼"), 2)
  assert_eq(get_string_width("👰🏽"), 2)
  assert_eq(get_string_width("👰🏾"), 2)
  assert_eq(get_string_width("👰🏿"), 2)
  assert_eq(get_string_width("👰‍♂️"), 2)
  assert_eq(get_string_width("👰‍♂"), 3)
  assert_eq(get_string_width("👰🏻‍♂️"), 2)
  assert_eq(get_string_width("👰🏻‍♂"), 3)
  assert_eq(get_string_width("👰🏼‍♂️"), 2)
  assert_eq(get_string_width("👰🏼‍♂"), 3)
  assert_eq(get_string_width("👰🏽‍♂️"), 2)
  assert_eq(get_string_width("👰🏽‍♂"), 3)
  assert_eq(get_string_width("👰🏾‍♂️"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 18" {
  assert_eq(get_string_width("👰🏾‍♂"), 3)
  assert_eq(get_string_width("👰🏿‍♂️"), 2)
  assert_eq(get_string_width("👰🏿‍♂"), 3)
  assert_eq(get_string_width("👰‍♀️"), 2)
  assert_eq(get_string_width("👰‍♀"), 3)
  assert_eq(get_string_width("👰🏻‍♀️"), 2)
  assert_eq(get_string_width("👰🏻‍♀"), 3)
  assert_eq(get_string_width("👰🏼‍♀️"), 2)
  assert_eq(get_string_width("👰🏼‍♀"), 3)
  assert_eq(get_string_width("👰🏽‍♀️"), 2)
  assert_eq(get_string_width("👰🏽‍♀"), 3)
  assert_eq(get_string_width("👰🏾‍♀️"), 2)
  assert_eq(get_string_width("👰🏾‍♀"), 3)
  assert_eq(get_string_width("👰🏿‍♀️"), 2)
  assert_eq(get_string_width("👰🏿‍♀"), 3)
  assert_eq(get_string_width("🤰"), 2)
  assert_eq(get_string_width("🤰🏻"), 2)
  assert_eq(get_string_width("🤰🏼"), 2)
  assert_eq(get_string_width("🤰🏽"), 2)
  assert_eq(get_string_width("🤰🏾"), 2)
  assert_eq(get_string_width("🤰🏿"), 2)
  assert_eq(get_string_width("🫃"), 2)
  assert_eq(get_string_width("🫃🏻"), 2)
  assert_eq(get_string_width("🫃🏼"), 2)
  assert_eq(get_string_width("🫃🏽"), 2)
  assert_eq(get_string_width("🫃🏾"), 2)
  assert_eq(get_string_width("🫃🏿"), 2)
  assert_eq(get_string_width("🫄"), 2)
  assert_eq(get_string_width("🫄🏻"), 2)
  assert_eq(get_string_width("🫄🏼"), 2)
  assert_eq(get_string_width("🫄🏽"), 2)
  assert_eq(get_string_width("🫄🏾"), 2)
  assert_eq(get_string_width("🫄🏿"), 2)
  assert_eq(get_string_width("🤱"), 2)
  assert_eq(get_string_width("🤱🏻"), 2)
  assert_eq(get_string_width("🤱🏼"), 2)
  assert_eq(get_string_width("🤱🏽"), 2)
  assert_eq(get_string_width("🤱🏾"), 2)
  assert_eq(get_string_width("🤱🏿"), 2)
  assert_eq(get_string_width("👩‍🍼"), 2)
  assert_eq(get_string_width("👩🏻‍🍼"), 2)
  assert_eq(get_string_width("👩🏼‍🍼"), 2)
  assert_eq(get_string_width("👩🏽‍🍼"), 2)
  assert_eq(get_string_width("👩🏾‍🍼"), 2)
  assert_eq(get_string_width("👩🏿‍🍼"), 2)
  assert_eq(get_string_width("👨‍🍼"), 2)
  assert_eq(get_string_width("👨🏻‍🍼"), 2)
  assert_eq(get_string_width("👨🏼‍🍼"), 2)
  assert_eq(get_string_width("👨🏽‍🍼"), 2)
  assert_eq(get_string_width("👨🏾‍🍼"), 2)
  assert_eq(get_string_width("👨🏿‍🍼"), 2)
  assert_eq(get_string_width("🧑‍🍼"), 2)
  assert_eq(get_string_width("🧑🏻‍🍼"), 2)
  assert_eq(get_string_width("🧑🏼‍🍼"), 2)
  assert_eq(get_string_width("🧑🏽‍🍼"), 2)
  assert_eq(get_string_width("🧑🏾‍🍼"), 2)
  assert_eq(get_string_width("🧑🏿‍🍼"), 2)
  assert_eq(get_string_width("👼"), 2)
  assert_eq(get_string_width("👼🏻"), 2)
  assert_eq(get_string_width("👼🏼"), 2)
  assert_eq(get_string_width("👼🏽"), 2)
  assert_eq(get_string_width("👼🏾"), 2)
  assert_eq(get_string_width("👼🏿"), 2)
  assert_eq(get_string_width("🎅"), 2)
  assert_eq(get_string_width("🎅🏻"), 2)
  assert_eq(get_string_width("🎅🏼"), 2)
  assert_eq(get_string_width("🎅🏽"), 2)
  assert_eq(get_string_width("🎅🏾"), 2)
  assert_eq(get_string_width("🎅🏿"), 2)
  assert_eq(get_string_width("🤶"), 2)
  assert_eq(get_string_width("🤶🏻"), 2)
  assert_eq(get_string_width("🤶🏼"), 2)
  assert_eq(get_string_width("🤶🏽"), 2)
  assert_eq(get_string_width("🤶🏾"), 2)
  assert_eq(get_string_width("🤶🏿"), 2)
  assert_eq(get_string_width("🧑‍🎄"), 2)
  assert_eq(get_string_width("🧑🏻‍🎄"), 2)
  assert_eq(get_string_width("🧑🏼‍🎄"), 2)
  assert_eq(get_string_width("🧑🏽‍🎄"), 2)
  assert_eq(get_string_width("🧑🏾‍🎄"), 2)
  assert_eq(get_string_width("🧑🏿‍🎄"), 2)
  assert_eq(get_string_width("🦸"), 2)
  assert_eq(get_string_width("🦸🏻"), 2)
  assert_eq(get_string_width("🦸🏼"), 2)
  assert_eq(get_string_width("🦸🏽"), 2)
  assert_eq(get_string_width("🦸🏾"), 2)
  assert_eq(get_string_width("🦸🏿"), 2)
  assert_eq(get_string_width("🦸‍♂️"), 2)
  assert_eq(get_string_width("🦸‍♂"), 3)
  assert_eq(get_string_width("🦸🏻‍♂️"), 2)
  assert_eq(get_string_width("🦸🏻‍♂"), 3)
  assert_eq(get_string_width("🦸🏼‍♂️"), 2)
  assert_eq(get_string_width("🦸🏼‍♂"), 3)
  assert_eq(get_string_width("🦸🏽‍♂️"), 2)
  assert_eq(get_string_width("🦸🏽‍♂"), 3)
  assert_eq(get_string_width("🦸🏾‍♂️"), 2)
  assert_eq(get_string_width("🦸🏾‍♂"), 3)
  assert_eq(get_string_width("🦸🏿‍♂️"), 2)
  assert_eq(get_string_width("🦸🏿‍♂"), 3)
  assert_eq(get_string_width("🦸‍♀️"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 19" {
  assert_eq(get_string_width("🦸‍♀"), 3)
  assert_eq(get_string_width("🦸🏻‍♀️"), 2)
  assert_eq(get_string_width("🦸🏻‍♀"), 3)
  assert_eq(get_string_width("🦸🏼‍♀️"), 2)
  assert_eq(get_string_width("🦸🏼‍♀"), 3)
  assert_eq(get_string_width("🦸🏽‍♀️"), 2)
  assert_eq(get_string_width("🦸🏽‍♀"), 3)
  assert_eq(get_string_width("🦸🏾‍♀️"), 2)
  assert_eq(get_string_width("🦸🏾‍♀"), 3)
  assert_eq(get_string_width("🦸🏿‍♀️"), 2)
  assert_eq(get_string_width("🦸🏿‍♀"), 3)
  assert_eq(get_string_width("🦹"), 2)
  assert_eq(get_string_width("🦹🏻"), 2)
  assert_eq(get_string_width("🦹🏼"), 2)
  assert_eq(get_string_width("🦹🏽"), 2)
  assert_eq(get_string_width("🦹🏾"), 2)
  assert_eq(get_string_width("🦹🏿"), 2)
  assert_eq(get_string_width("🦹‍♂️"), 2)
  assert_eq(get_string_width("🦹‍♂"), 3)
  assert_eq(get_string_width("🦹🏻‍♂️"), 2)
  assert_eq(get_string_width("🦹🏻‍♂"), 3)
  assert_eq(get_string_width("🦹🏼‍♂️"), 2)
  assert_eq(get_string_width("🦹🏼‍♂"), 3)
  assert_eq(get_string_width("🦹🏽‍♂️"), 2)
  assert_eq(get_string_width("🦹🏽‍♂"), 3)
  assert_eq(get_string_width("🦹🏾‍♂️"), 2)
  assert_eq(get_string_width("🦹🏾‍♂"), 3)
  assert_eq(get_string_width("🦹🏿‍♂️"), 2)
  assert_eq(get_string_width("🦹🏿‍♂"), 3)
  assert_eq(get_string_width("🦹‍♀️"), 2)
  assert_eq(get_string_width("🦹‍♀"), 3)
  assert_eq(get_string_width("🦹🏻‍♀️"), 2)
  assert_eq(get_string_width("🦹🏻‍♀"), 3)
  assert_eq(get_string_width("🦹🏼‍♀️"), 2)
  assert_eq(get_string_width("🦹🏼‍♀"), 3)
  assert_eq(get_string_width("🦹🏽‍♀️"), 2)
  assert_eq(get_string_width("🦹🏽‍♀"), 3)
  assert_eq(get_string_width("🦹🏾‍♀️"), 2)
  assert_eq(get_string_width("🦹🏾‍♀"), 3)
  assert_eq(get_string_width("🦹🏿‍♀️"), 2)
  assert_eq(get_string_width("🦹🏿‍♀"), 3)
  assert_eq(get_string_width("🧙"), 2)
  assert_eq(get_string_width("🧙🏻"), 2)
  assert_eq(get_string_width("🧙🏼"), 2)
  assert_eq(get_string_width("🧙🏽"), 2)
  assert_eq(get_string_width("🧙🏾"), 2)
  assert_eq(get_string_width("🧙🏿"), 2)
  assert_eq(get_string_width("🧙‍♂️"), 2)
  assert_eq(get_string_width("🧙‍♂"), 3)
  assert_eq(get_string_width("🧙🏻‍♂️"), 2)
  assert_eq(get_string_width("🧙🏻‍♂"), 3)
  assert_eq(get_string_width("🧙🏼‍♂️"), 2)
  assert_eq(get_string_width("🧙🏼‍♂"), 3)
  assert_eq(get_string_width("🧙🏽‍♂️"), 2)
  assert_eq(get_string_width("🧙🏽‍♂"), 3)
  assert_eq(get_string_width("🧙🏾‍♂️"), 2)
  assert_eq(get_string_width("🧙🏾‍♂"), 3)
  assert_eq(get_string_width("🧙🏿‍♂️"), 2)
  assert_eq(get_string_width("🧙🏿‍♂"), 3)
  assert_eq(get_string_width("🧙‍♀️"), 2)
  assert_eq(get_string_width("🧙‍♀"), 3)
  assert_eq(get_string_width("🧙🏻‍♀️"), 2)
  assert_eq(get_string_width("🧙🏻‍♀"), 3)
  assert_eq(get_string_width("🧙🏼‍♀️"), 2)
  assert_eq(get_string_width("🧙🏼‍♀"), 3)
  assert_eq(get_string_width("🧙🏽‍♀️"), 2)
  assert_eq(get_string_width("🧙🏽‍♀"), 3)
  assert_eq(get_string_width("🧙🏾‍♀️"), 2)
  assert_eq(get_string_width("🧙🏾‍♀"), 3)
  assert_eq(get_string_width("🧙🏿‍♀️"), 2)
  assert_eq(get_string_width("🧙🏿‍♀"), 3)
  assert_eq(get_string_width("🧚"), 2)
  assert_eq(get_string_width("🧚🏻"), 2)
  assert_eq(get_string_width("🧚🏼"), 2)
  assert_eq(get_string_width("🧚🏽"), 2)
  assert_eq(get_string_width("🧚🏾"), 2)
  assert_eq(get_string_width("🧚🏿"), 2)
  assert_eq(get_string_width("🧚‍♂️"), 2)
  assert_eq(get_string_width("🧚‍♂"), 3)
  assert_eq(get_string_width("🧚🏻‍♂️"), 2)
  assert_eq(get_string_width("🧚🏻‍♂"), 3)
  assert_eq(get_string_width("🧚🏼‍♂️"), 2)
  assert_eq(get_string_width("🧚🏼‍♂"), 3)
  assert_eq(get_string_width("🧚🏽‍♂️"), 2)
  assert_eq(get_string_width("🧚🏽‍♂"), 3)
  assert_eq(get_string_width("🧚🏾‍♂️"), 2)
  assert_eq(get_string_width("🧚🏾‍♂"), 3)
  assert_eq(get_string_width("🧚🏿‍♂️"), 2)
  assert_eq(get_string_width("🧚🏿‍♂"), 3)
  assert_eq(get_string_width("🧚‍♀️"), 2)
  assert_eq(get_string_width("🧚‍♀"), 3)
  assert_eq(get_string_width("🧚🏻‍♀️"), 2)
  assert_eq(get_string_width("🧚🏻‍♀"), 3)
  assert_eq(get_string_width("🧚🏼‍♀️"), 2)
  assert_eq(get_string_width("🧚🏼‍♀"), 3)
  assert_eq(get_string_width("🧚🏽‍♀️"), 2)
  assert_eq(get_string_width("🧚🏽‍♀"), 3)
  assert_eq(get_string_width("🧚🏾‍♀️"), 2)
  assert_eq(get_string_width("🧚🏾‍♀"), 3)
  assert_eq(get_string_width("🧚🏿‍♀️"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 20" {
  assert_eq(get_string_width("🧚🏿‍♀"), 3)
  assert_eq(get_string_width("🧛"), 2)
  assert_eq(get_string_width("🧛🏻"), 2)
  assert_eq(get_string_width("🧛🏼"), 2)
  assert_eq(get_string_width("🧛🏽"), 2)
  assert_eq(get_string_width("🧛🏾"), 2)
  assert_eq(get_string_width("🧛🏿"), 2)
  assert_eq(get_string_width("🧛‍♂️"), 2)
  assert_eq(get_string_width("🧛‍♂"), 3)
  assert_eq(get_string_width("🧛🏻‍♂️"), 2)
  assert_eq(get_string_width("🧛🏻‍♂"), 3)
  assert_eq(get_string_width("🧛🏼‍♂️"), 2)
  assert_eq(get_string_width("🧛🏼‍♂"), 3)
  assert_eq(get_string_width("🧛🏽‍♂️"), 2)
  assert_eq(get_string_width("🧛🏽‍♂"), 3)
  assert_eq(get_string_width("🧛🏾‍♂️"), 2)
  assert_eq(get_string_width("🧛🏾‍♂"), 3)
  assert_eq(get_string_width("🧛🏿‍♂️"), 2)
  assert_eq(get_string_width("🧛🏿‍♂"), 3)
  assert_eq(get_string_width("🧛‍♀️"), 2)
  assert_eq(get_string_width("🧛‍♀"), 3)
  assert_eq(get_string_width("🧛🏻‍♀️"), 2)
  assert_eq(get_string_width("🧛🏻‍♀"), 3)
  assert_eq(get_string_width("🧛🏼‍♀️"), 2)
  assert_eq(get_string_width("🧛🏼‍♀"), 3)
  assert_eq(get_string_width("🧛🏽‍♀️"), 2)
  assert_eq(get_string_width("🧛🏽‍♀"), 3)
  assert_eq(get_string_width("🧛🏾‍♀️"), 2)
  assert_eq(get_string_width("🧛🏾‍♀"), 3)
  assert_eq(get_string_width("🧛🏿‍♀️"), 2)
  assert_eq(get_string_width("🧛🏿‍♀"), 3)
  assert_eq(get_string_width("🧜"), 2)
  assert_eq(get_string_width("🧜🏻"), 2)
  assert_eq(get_string_width("🧜🏼"), 2)
  assert_eq(get_string_width("🧜🏽"), 2)
  assert_eq(get_string_width("🧜🏾"), 2)
  assert_eq(get_string_width("🧜🏿"), 2)
  assert_eq(get_string_width("🧜‍♂️"), 2)
  assert_eq(get_string_width("🧜‍♂"), 3)
  assert_eq(get_string_width("🧜🏻‍♂️"), 2)
  assert_eq(get_string_width("🧜🏻‍♂"), 3)
  assert_eq(get_string_width("🧜🏼‍♂️"), 2)
  assert_eq(get_string_width("🧜🏼‍♂"), 3)
  assert_eq(get_string_width("🧜🏽‍♂️"), 2)
  assert_eq(get_string_width("🧜🏽‍♂"), 3)
  assert_eq(get_string_width("🧜🏾‍♂️"), 2)
  assert_eq(get_string_width("🧜🏾‍♂"), 3)
  assert_eq(get_string_width("🧜🏿‍♂️"), 2)
  assert_eq(get_string_width("🧜🏿‍♂"), 3)
  assert_eq(get_string_width("🧜‍♀️"), 2)
  assert_eq(get_string_width("🧜‍♀"), 3)
  assert_eq(get_string_width("🧜🏻‍♀️"), 2)
  assert_eq(get_string_width("🧜🏻‍♀"), 3)
  assert_eq(get_string_width("🧜🏼‍♀️"), 2)
  assert_eq(get_string_width("🧜🏼‍♀"), 3)
  assert_eq(get_string_width("🧜🏽‍♀️"), 2)
  assert_eq(get_string_width("🧜🏽‍♀"), 3)
  assert_eq(get_string_width("🧜🏾‍♀️"), 2)
  assert_eq(get_string_width("🧜🏾‍♀"), 3)
  assert_eq(get_string_width("🧜🏿‍♀️"), 2)
  assert_eq(get_string_width("🧜🏿‍♀"), 3)
  assert_eq(get_string_width("🧝"), 2)
  assert_eq(get_string_width("🧝🏻"), 2)
  assert_eq(get_string_width("🧝🏼"), 2)
  assert_eq(get_string_width("🧝🏽"), 2)
  assert_eq(get_string_width("🧝🏾"), 2)
  assert_eq(get_string_width("🧝🏿"), 2)
  assert_eq(get_string_width("🧝‍♂️"), 2)
  assert_eq(get_string_width("🧝‍♂"), 3)
  assert_eq(get_string_width("🧝🏻‍♂️"), 2)
  assert_eq(get_string_width("🧝🏻‍♂"), 3)
  assert_eq(get_string_width("🧝🏼‍♂️"), 2)
  assert_eq(get_string_width("🧝🏼‍♂"), 3)
  assert_eq(get_string_width("🧝🏽‍♂️"), 2)
  assert_eq(get_string_width("🧝🏽‍♂"), 3)
  assert_eq(get_string_width("🧝🏾‍♂️"), 2)
  assert_eq(get_string_width("🧝🏾‍♂"), 3)
  assert_eq(get_string_width("🧝🏿‍♂️"), 2)
  assert_eq(get_string_width("🧝🏿‍♂"), 3)
  assert_eq(get_string_width("🧝‍♀️"), 2)
  assert_eq(get_string_width("🧝‍♀"), 3)
  assert_eq(get_string_width("🧝🏻‍♀️"), 2)
  assert_eq(get_string_width("🧝🏻‍♀"), 3)
  assert_eq(get_string_width("🧝🏼‍♀️"), 2)
  assert_eq(get_string_width("🧝🏼‍♀"), 3)
  assert_eq(get_string_width("🧝🏽‍♀️"), 2)
  assert_eq(get_string_width("🧝🏽‍♀"), 3)
  assert_eq(get_string_width("🧝🏾‍♀️"), 2)
  assert_eq(get_string_width("🧝🏾‍♀"), 3)
  assert_eq(get_string_width("🧝🏿‍♀️"), 2)
  assert_eq(get_string_width("🧝🏿‍♀"), 3)
  assert_eq(get_string_width("🧞"), 2)
  assert_eq(get_string_width("🧞‍♂️"), 2)
  assert_eq(get_string_width("🧞‍♂"), 3)
  assert_eq(get_string_width("🧞‍♀️"), 2)
  assert_eq(get_string_width("🧞‍♀"), 3)
  assert_eq(get_string_width("🧟"), 2)
  assert_eq(get_string_width("🧟‍♂️"), 2)
  assert_eq(get_string_width("🧟‍♂"), 3)
  assert_eq(get_string_width("🧟‍♀️"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 21" {
  assert_eq(get_string_width("🧟‍♀"), 3)
  assert_eq(get_string_width("🧌"), 2)
  assert_eq(get_string_width("🫈"), 2)
  assert_eq(get_string_width("💆"), 2)
  assert_eq(get_string_width("💆🏻"), 2)
  assert_eq(get_string_width("💆🏼"), 2)
  assert_eq(get_string_width("💆🏽"), 2)
  assert_eq(get_string_width("💆🏾"), 2)
  assert_eq(get_string_width("💆🏿"), 2)
  assert_eq(get_string_width("💆‍♂️"), 2)
  assert_eq(get_string_width("💆‍♂"), 3)
  assert_eq(get_string_width("💆🏻‍♂️"), 2)
  assert_eq(get_string_width("💆🏻‍♂"), 3)
  assert_eq(get_string_width("💆🏼‍♂️"), 2)
  assert_eq(get_string_width("💆🏼‍♂"), 3)
  assert_eq(get_string_width("💆🏽‍♂️"), 2)
  assert_eq(get_string_width("💆🏽‍♂"), 3)
  assert_eq(get_string_width("💆🏾‍♂️"), 2)
  assert_eq(get_string_width("💆🏾‍♂"), 3)
  assert_eq(get_string_width("💆🏿‍♂️"), 2)
  assert_eq(get_string_width("💆🏿‍♂"), 3)
  assert_eq(get_string_width("💆‍♀️"), 2)
  assert_eq(get_string_width("💆‍♀"), 3)
  assert_eq(get_string_width("💆🏻‍♀️"), 2)
  assert_eq(get_string_width("💆🏻‍♀"), 3)
  assert_eq(get_string_width("💆🏼‍♀️"), 2)
  assert_eq(get_string_width("💆🏼‍♀"), 3)
  assert_eq(get_string_width("💆🏽‍♀️"), 2)
  assert_eq(get_string_width("💆🏽‍♀"), 3)
  assert_eq(get_string_width("💆🏾‍♀️"), 2)
  assert_eq(get_string_width("💆🏾‍♀"), 3)
  assert_eq(get_string_width("💆🏿‍♀️"), 2)
  assert_eq(get_string_width("💆🏿‍♀"), 3)
  assert_eq(get_string_width("💇"), 2)
  assert_eq(get_string_width("💇🏻"), 2)
  assert_eq(get_string_width("💇🏼"), 2)
  assert_eq(get_string_width("💇🏽"), 2)
  assert_eq(get_string_width("💇🏾"), 2)
  assert_eq(get_string_width("💇🏿"), 2)
  assert_eq(get_string_width("💇‍♂️"), 2)
  assert_eq(get_string_width("💇‍♂"), 3)
  assert_eq(get_string_width("💇🏻‍♂️"), 2)
  assert_eq(get_string_width("💇🏻‍♂"), 3)
  assert_eq(get_string_width("💇🏼‍♂️"), 2)
  assert_eq(get_string_width("💇🏼‍♂"), 3)
  assert_eq(get_string_width("💇🏽‍♂️"), 2)
  assert_eq(get_string_width("💇🏽‍♂"), 3)
  assert_eq(get_string_width("💇🏾‍♂️"), 2)
  assert_eq(get_string_width("💇🏾‍♂"), 3)
  assert_eq(get_string_width("💇🏿‍♂️"), 2)
  assert_eq(get_string_width("💇🏿‍♂"), 3)
  assert_eq(get_string_width("💇‍♀️"), 2)
  assert_eq(get_string_width("💇‍♀"), 3)
  assert_eq(get_string_width("💇🏻‍♀️"), 2)
  assert_eq(get_string_width("💇🏻‍♀"), 3)
  assert_eq(get_string_width("💇🏼‍♀️"), 2)
  assert_eq(get_string_width("💇🏼‍♀"), 3)
  assert_eq(get_string_width("💇🏽‍♀️"), 2)
  assert_eq(get_string_width("💇🏽‍♀"), 3)
  assert_eq(get_string_width("💇🏾‍♀️"), 2)
  assert_eq(get_string_width("💇🏾‍♀"), 3)
  assert_eq(get_string_width("💇🏿‍♀️"), 2)
  assert_eq(get_string_width("💇🏿‍♀"), 3)
  assert_eq(get_string_width("🚶"), 2)
  assert_eq(get_string_width("🚶🏻"), 2)
  assert_eq(get_string_width("🚶🏼"), 2)
  assert_eq(get_string_width("🚶🏽"), 2)
  assert_eq(get_string_width("🚶🏾"), 2)
  assert_eq(get_string_width("🚶🏿"), 2)
  assert_eq(get_string_width("🚶‍♂️"), 2)
  assert_eq(get_string_width("🚶‍♂"), 3)
  assert_eq(get_string_width("🚶🏻‍♂️"), 2)
  assert_eq(get_string_width("🚶🏻‍♂"), 3)
  assert_eq(get_string_width("🚶🏼‍♂️"), 2)
  assert_eq(get_string_width("🚶🏼‍♂"), 3)
  assert_eq(get_string_width("🚶🏽‍♂️"), 2)
  assert_eq(get_string_width("🚶🏽‍♂"), 3)
  assert_eq(get_string_width("🚶🏾‍♂️"), 2)
  assert_eq(get_string_width("🚶🏾‍♂"), 3)
  assert_eq(get_string_width("🚶🏿‍♂️"), 2)
  assert_eq(get_string_width("🚶🏿‍♂"), 3)
  assert_eq(get_string_width("🚶‍♀️"), 2)
  assert_eq(get_string_width("🚶‍♀"), 3)
  assert_eq(get_string_width("🚶🏻‍♀️"), 2)
  assert_eq(get_string_width("🚶🏻‍♀"), 3)
  assert_eq(get_string_width("🚶🏼‍♀️"), 2)
  assert_eq(get_string_width("🚶🏼‍♀"), 3)
  assert_eq(get_string_width("🚶🏽‍♀️"), 2)
  assert_eq(get_string_width("🚶🏽‍♀"), 3)
  assert_eq(get_string_width("🚶🏾‍♀️"), 2)
  assert_eq(get_string_width("🚶🏾‍♀"), 3)
  assert_eq(get_string_width("🚶🏿‍♀️"), 2)
  assert_eq(get_string_width("🚶🏿‍♀"), 3)
  assert_eq(get_string_width("🚶‍➡️"), 2)
  assert_eq(get_string_width("🚶‍➡"), 3)
  assert_eq(get_string_width("🚶🏻‍➡️"), 2)
  assert_eq(get_string_width("🚶🏻‍➡"), 3)
  assert_eq(get_string_width("🚶🏼‍➡️"), 2)
  assert_eq(get_string_width("🚶🏼‍➡"), 3)
  assert_eq(get_string_width("🚶🏽‍➡️"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 22" {
  assert_eq(get_string_width("🚶🏽‍➡"), 3)
  assert_eq(get_string_width("🚶🏾‍➡️"), 2)
  assert_eq(get_string_width("🚶🏾‍➡"), 3)
  assert_eq(get_string_width("🚶🏿‍➡️"), 2)
  assert_eq(get_string_width("🚶🏿‍➡"), 3)
  assert_eq(get_string_width("🚶‍♀️‍➡️"), 2)
  assert_eq(get_string_width("🚶‍♀‍➡️"), 5)
  assert_eq(get_string_width("🚶‍♀️‍➡"), 3)
  assert_eq(get_string_width("🚶‍♀‍➡"), 4)
  assert_eq(get_string_width("🚶🏻‍♀️‍➡️"), 2)
  assert_eq(get_string_width("🚶🏻‍♀‍➡️"), 5)
  assert_eq(get_string_width("🚶🏻‍♀️‍➡"), 3)
  assert_eq(get_string_width("🚶🏻‍♀‍➡"), 4)
  assert_eq(get_string_width("🚶🏼‍♀️‍➡️"), 2)
  assert_eq(get_string_width("🚶🏼‍♀‍➡️"), 5)
  assert_eq(get_string_width("🚶🏼‍♀️‍➡"), 3)
  assert_eq(get_string_width("🚶🏼‍♀‍➡"), 4)
  assert_eq(get_string_width("🚶🏽‍♀️‍➡️"), 2)
  assert_eq(get_string_width("🚶🏽‍♀‍➡️"), 5)
  assert_eq(get_string_width("🚶🏽‍♀️‍➡"), 3)
  assert_eq(get_string_width("🚶🏽‍♀‍➡"), 4)
  assert_eq(get_string_width("🚶🏾‍♀️‍➡️"), 2)
  assert_eq(get_string_width("🚶🏾‍♀‍➡️"), 5)
  assert_eq(get_string_width("🚶🏾‍♀️‍➡"), 3)
  assert_eq(get_string_width("🚶🏾‍♀‍➡"), 4)
  assert_eq(get_string_width("🚶🏿‍♀️‍➡️"), 2)
  assert_eq(get_string_width("🚶🏿‍♀‍➡️"), 5)
  assert_eq(get_string_width("🚶🏿‍♀️‍➡"), 3)
  assert_eq(get_string_width("🚶🏿‍♀‍➡"), 4)
  assert_eq(get_string_width("🚶‍♂️‍➡️"), 2)
  assert_eq(get_string_width("🚶‍♂‍➡️"), 5)
  assert_eq(get_string_width("🚶‍♂️‍➡"), 3)
  assert_eq(get_string_width("🚶‍♂‍➡"), 4)
  assert_eq(get_string_width("🚶🏻‍♂️‍➡️"), 2)
  assert_eq(get_string_width("🚶🏻‍♂‍➡️"), 5)
  assert_eq(get_string_width("🚶🏻‍♂️‍➡"), 3)
  assert_eq(get_string_width("🚶🏻‍♂‍➡"), 4)
  assert_eq(get_string_width("🚶🏼‍♂️‍➡️"), 2)
  assert_eq(get_string_width("🚶🏼‍♂‍➡️"), 5)
  assert_eq(get_string_width("🚶🏼‍♂️‍➡"), 3)
  assert_eq(get_string_width("🚶🏼‍♂‍➡"), 4)
  assert_eq(get_string_width("🚶🏽‍♂️‍➡️"), 2)
  assert_eq(get_string_width("🚶🏽‍♂‍➡️"), 5)
  assert_eq(get_string_width("🚶🏽‍♂️‍➡"), 3)
  assert_eq(get_string_width("🚶🏽‍♂‍➡"), 4)
  assert_eq(get_string_width("🚶🏾‍♂️‍➡️"), 2)
  assert_eq(get_string_width("🚶🏾‍♂‍➡️"), 5)
  assert_eq(get_string_width("🚶🏾‍♂️‍➡"), 3)
  assert_eq(get_string_width("🚶🏾‍♂‍➡"), 4)
  assert_eq(get_string_width("🚶🏿‍♂️‍➡️"), 2)
  assert_eq(get_string_width("🚶🏿‍♂‍➡️"), 5)
  assert_eq(get_string_width("🚶🏿‍♂️‍➡"), 3)
  assert_eq(get_string_width("🚶🏿‍♂‍➡"), 4)
  assert_eq(get_string_width("🧍"), 2)
  assert_eq(get_string_width("🧍🏻"), 2)
  assert_eq(get_string_width("🧍🏼"), 2)
  assert_eq(get_string_width("🧍🏽"), 2)
  assert_eq(get_string_width("🧍🏾"), 2)
  assert_eq(get_string_width("🧍🏿"), 2)
  assert_eq(get_string_width("🧍‍♂️"), 2)
  assert_eq(get_string_width("🧍‍♂"), 3)
  assert_eq(get_string_width("🧍🏻‍♂️"), 2)
  assert_eq(get_string_width("🧍🏻‍♂"), 3)
  assert_eq(get_string_width("🧍🏼‍♂️"), 2)
  assert_eq(get_string_width("🧍🏼‍♂"), 3)
  assert_eq(get_string_width("🧍🏽‍♂️"), 2)
  assert_eq(get_string_width("🧍🏽‍♂"), 3)
  assert_eq(get_string_width("🧍🏾‍♂️"), 2)
  assert_eq(get_string_width("🧍🏾‍♂"), 3)
  assert_eq(get_string_width("🧍🏿‍♂️"), 2)
  assert_eq(get_string_width("🧍🏿‍♂"), 3)
  assert_eq(get_string_width("🧍‍♀️"), 2)
  assert_eq(get_string_width("🧍‍♀"), 3)
  assert_eq(get_string_width("🧍🏻‍♀️"), 2)
  assert_eq(get_string_width("🧍🏻‍♀"), 3)
  assert_eq(get_string_width("🧍🏼‍♀️"), 2)
  assert_eq(get_string_width("🧍🏼‍♀"), 3)
  assert_eq(get_string_width("🧍🏽‍♀️"), 2)
  assert_eq(get_string_width("🧍🏽‍♀"), 3)
  assert_eq(get_string_width("🧍🏾‍♀️"), 2)
  assert_eq(get_string_width("🧍🏾‍♀"), 3)
  assert_eq(get_string_width("🧍🏿‍♀️"), 2)
  assert_eq(get_string_width("🧍🏿‍♀"), 3)
  assert_eq(get_string_width("🧎"), 2)
  assert_eq(get_string_width("🧎🏻"), 2)
  assert_eq(get_string_width("🧎🏼"), 2)
  assert_eq(get_string_width("🧎🏽"), 2)
  assert_eq(get_string_width("🧎🏾"), 2)
  assert_eq(get_string_width("🧎🏿"), 2)
  assert_eq(get_string_width("🧎‍♂️"), 2)
  assert_eq(get_string_width("🧎‍♂"), 3)
  assert_eq(get_string_width("🧎🏻‍♂️"), 2)
  assert_eq(get_string_width("🧎🏻‍♂"), 3)
  assert_eq(get_string_width("🧎🏼‍♂️"), 2)
  assert_eq(get_string_width("🧎🏼‍♂"), 3)
  assert_eq(get_string_width("🧎🏽‍♂️"), 2)
  assert_eq(get_string_width("🧎🏽‍♂"), 3)
  assert_eq(get_string_width("🧎🏾‍♂️"), 2)
  assert_eq(get_string_width("🧎🏾‍♂"), 3)
  assert_eq(get_string_width("🧎🏿‍♂️"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 23" {
  assert_eq(get_string_width("🧎🏿‍♂"), 3)
  assert_eq(get_string_width("🧎‍♀️"), 2)
  assert_eq(get_string_width("🧎‍♀"), 3)
  assert_eq(get_string_width("🧎🏻‍♀️"), 2)
  assert_eq(get_string_width("🧎🏻‍♀"), 3)
  assert_eq(get_string_width("🧎🏼‍♀️"), 2)
  assert_eq(get_string_width("🧎🏼‍♀"), 3)
  assert_eq(get_string_width("🧎🏽‍♀️"), 2)
  assert_eq(get_string_width("🧎🏽‍♀"), 3)
  assert_eq(get_string_width("🧎🏾‍♀️"), 2)
  assert_eq(get_string_width("🧎🏾‍♀"), 3)
  assert_eq(get_string_width("🧎🏿‍♀️"), 2)
  assert_eq(get_string_width("🧎🏿‍♀"), 3)
  assert_eq(get_string_width("🧎‍➡️"), 2)
  assert_eq(get_string_width("🧎‍➡"), 3)
  assert_eq(get_string_width("🧎🏻‍➡️"), 2)
  assert_eq(get_string_width("🧎🏻‍➡"), 3)
  assert_eq(get_string_width("🧎🏼‍➡️"), 2)
  assert_eq(get_string_width("🧎🏼‍➡"), 3)
  assert_eq(get_string_width("🧎🏽‍➡️"), 2)
  assert_eq(get_string_width("🧎🏽‍➡"), 3)
  assert_eq(get_string_width("🧎🏾‍➡️"), 2)
  assert_eq(get_string_width("🧎🏾‍➡"), 3)
  assert_eq(get_string_width("🧎🏿‍➡️"), 2)
  assert_eq(get_string_width("🧎🏿‍➡"), 3)
  assert_eq(get_string_width("🧎‍♀️‍➡️"), 2)
  assert_eq(get_string_width("🧎‍♀‍➡️"), 5)
  assert_eq(get_string_width("🧎‍♀️‍➡"), 3)
  assert_eq(get_string_width("🧎‍♀‍➡"), 4)
  assert_eq(get_string_width("🧎🏻‍♀️‍➡️"), 2)
  assert_eq(get_string_width("🧎🏻‍♀‍➡️"), 5)
  assert_eq(get_string_width("🧎🏻‍♀️‍➡"), 3)
  assert_eq(get_string_width("🧎🏻‍♀‍➡"), 4)
  assert_eq(get_string_width("🧎🏼‍♀️‍➡️"), 2)
  assert_eq(get_string_width("🧎🏼‍♀‍➡️"), 5)
  assert_eq(get_string_width("🧎🏼‍♀️‍➡"), 3)
  assert_eq(get_string_width("🧎🏼‍♀‍➡"), 4)
  assert_eq(get_string_width("🧎🏽‍♀️‍➡️"), 2)
  assert_eq(get_string_width("🧎🏽‍♀‍➡️"), 5)
  assert_eq(get_string_width("🧎🏽‍♀️‍➡"), 3)
  assert_eq(get_string_width("🧎🏽‍♀‍➡"), 4)
  assert_eq(get_string_width("🧎🏾‍♀️‍➡️"), 2)
  assert_eq(get_string_width("🧎🏾‍♀‍➡️"), 5)
  assert_eq(get_string_width("🧎🏾‍♀️‍➡"), 3)
  assert_eq(get_string_width("🧎🏾‍♀‍➡"), 4)
  assert_eq(get_string_width("🧎🏿‍♀️‍➡️"), 2)
  assert_eq(get_string_width("🧎🏿‍♀‍➡️"), 5)
  assert_eq(get_string_width("🧎🏿‍♀️‍➡"), 3)
  assert_eq(get_string_width("🧎🏿‍♀‍➡"), 4)
  assert_eq(get_string_width("🧎‍♂️‍➡️"), 2)
  assert_eq(get_string_width("🧎‍♂‍➡️"), 5)
  assert_eq(get_string_width("🧎‍♂️‍➡"), 3)
  assert_eq(get_string_width("🧎‍♂‍➡"), 4)
  assert_eq(get_string_width("🧎🏻‍♂️‍➡️"), 2)
  assert_eq(get_string_width("🧎🏻‍♂‍➡️"), 5)
  assert_eq(get_string_width("🧎🏻‍♂️‍➡"), 3)
  assert_eq(get_string_width("🧎🏻‍♂‍➡"), 4)
  assert_eq(get_string_width("🧎🏼‍♂️‍➡️"), 2)
  assert_eq(get_string_width("🧎🏼‍♂‍➡️"), 5)
  assert_eq(get_string_width("🧎🏼‍♂️‍➡"), 3)
  assert_eq(get_string_width("🧎🏼‍♂‍➡"), 4)
  assert_eq(get_string_width("🧎🏽‍♂️‍➡️"), 2)
  assert_eq(get_string_width("🧎🏽‍♂‍➡️"), 5)
  assert_eq(get_string_width("🧎🏽‍♂️‍➡"), 3)
  assert_eq(get_string_width("🧎🏽‍♂‍➡"), 4)
  assert_eq(get_string_width("🧎🏾‍♂️‍➡️"), 2)
  assert_eq(get_string_width("🧎🏾‍♂‍➡️"), 5)
  assert_eq(get_string_width("🧎🏾‍♂️‍➡"), 3)
  assert_eq(get_string_width("🧎🏾‍♂‍➡"), 4)
  assert_eq(get_string_width("🧎🏿‍♂️‍➡️"), 2)
  assert_eq(get_string_width("🧎🏿‍♂‍➡️"), 5)
  assert_eq(get_string_width("🧎🏿‍♂️‍➡"), 3)
  assert_eq(get_string_width("🧎🏿‍♂‍➡"), 4)
  assert_eq(get_string_width("🧑‍🦯"), 2)
  assert_eq(get_string_width("🧑🏻‍🦯"), 2)
  assert_eq(get_string_width("🧑🏼‍🦯"), 2)
  assert_eq(get_string_width("🧑🏽‍🦯"), 2)
  assert_eq(get_string_width("🧑🏾‍🦯"), 2)
  assert_eq(get_string_width("🧑🏿‍🦯"), 2)
  assert_eq(get_string_width("🧑‍🦯‍➡️"), 2)
  assert_eq(get_string_width("🧑‍🦯‍➡"), 3)
  assert_eq(get_string_width("🧑🏻‍🦯‍➡️"), 2)
  assert_eq(get_string_width("🧑🏻‍🦯‍➡"), 3)
  assert_eq(get_string_width("🧑🏼‍🦯‍➡️"), 2)
  assert_eq(get_string_width("🧑🏼‍🦯‍➡"), 3)
  assert_eq(get_string_width("🧑🏽‍🦯‍➡️"), 2)
  assert_eq(get_string_width("🧑🏽‍🦯‍➡"), 3)
  assert_eq(get_string_width("🧑🏾‍🦯‍➡️"), 2)
  assert_eq(get_string_width("🧑🏾‍🦯‍➡"), 3)
  assert_eq(get_string_width("🧑🏿‍🦯‍➡️"), 2)
  assert_eq(get_string_width("🧑🏿‍🦯‍➡"), 3)
  assert_eq(get_string_width("👨‍🦯"), 2)
  assert_eq(get_string_width("👨🏻‍🦯"), 2)
  assert_eq(get_string_width("👨🏼‍🦯"), 2)
  assert_eq(get_string_width("👨🏽‍🦯"), 2)
  assert_eq(get_string_width("👨🏾‍🦯"), 2)
  assert_eq(get_string_width("👨🏿‍🦯"), 2)
  assert_eq(get_string_width("👨‍🦯‍➡️"), 2)
  assert_eq(get_string_width("👨‍🦯‍➡"), 3)
  assert_eq(get_string_width("👨🏻‍🦯‍➡️"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 24" {
  assert_eq(get_string_width("👨🏻‍🦯‍➡"), 3)
  assert_eq(get_string_width("👨🏼‍🦯‍➡️"), 2)
  assert_eq(get_string_width("👨🏼‍🦯‍➡"), 3)
  assert_eq(get_string_width("👨🏽‍🦯‍➡️"), 2)
  assert_eq(get_string_width("👨🏽‍🦯‍➡"), 3)
  assert_eq(get_string_width("👨🏾‍🦯‍➡️"), 2)
  assert_eq(get_string_width("👨🏾‍🦯‍➡"), 3)
  assert_eq(get_string_width("👨🏿‍🦯‍➡️"), 2)
  assert_eq(get_string_width("👨🏿‍🦯‍➡"), 3)
  assert_eq(get_string_width("👩‍🦯"), 2)
  assert_eq(get_string_width("👩🏻‍🦯"), 2)
  assert_eq(get_string_width("👩🏼‍🦯"), 2)
  assert_eq(get_string_width("👩🏽‍🦯"), 2)
  assert_eq(get_string_width("👩🏾‍🦯"), 2)
  assert_eq(get_string_width("👩🏿‍🦯"), 2)
  assert_eq(get_string_width("👩‍🦯‍➡️"), 2)
  assert_eq(get_string_width("👩‍🦯‍➡"), 3)
  assert_eq(get_string_width("👩🏻‍🦯‍➡️"), 2)
  assert_eq(get_string_width("👩🏻‍🦯‍➡"), 3)
  assert_eq(get_string_width("👩🏼‍🦯‍➡️"), 2)
  assert_eq(get_string_width("👩🏼‍🦯‍➡"), 3)
  assert_eq(get_string_width("👩🏽‍🦯‍➡️"), 2)
  assert_eq(get_string_width("👩🏽‍🦯‍➡"), 3)
  assert_eq(get_string_width("👩🏾‍🦯‍➡️"), 2)
  assert_eq(get_string_width("👩🏾‍🦯‍➡"), 3)
  assert_eq(get_string_width("👩🏿‍🦯‍➡️"), 2)
  assert_eq(get_string_width("👩🏿‍🦯‍➡"), 3)
  assert_eq(get_string_width("🧑‍🦼"), 2)
  assert_eq(get_string_width("🧑🏻‍🦼"), 2)
  assert_eq(get_string_width("🧑🏼‍🦼"), 2)
  assert_eq(get_string_width("🧑🏽‍🦼"), 2)
  assert_eq(get_string_width("🧑🏾‍🦼"), 2)
  assert_eq(get_string_width("🧑🏿‍🦼"), 2)
  assert_eq(get_string_width("🧑‍🦼‍➡️"), 2)
  assert_eq(get_string_width("🧑‍🦼‍➡"), 3)
  assert_eq(get_string_width("🧑🏻‍🦼‍➡️"), 2)
  assert_eq(get_string_width("🧑🏻‍🦼‍➡"), 3)
  assert_eq(get_string_width("🧑🏼‍🦼‍➡️"), 2)
  assert_eq(get_string_width("🧑🏼‍🦼‍➡"), 3)
  assert_eq(get_string_width("🧑🏽‍🦼‍➡️"), 2)
  assert_eq(get_string_width("🧑🏽‍🦼‍➡"), 3)
  assert_eq(get_string_width("🧑🏾‍🦼‍➡️"), 2)
  assert_eq(get_string_width("🧑🏾‍🦼‍➡"), 3)
  assert_eq(get_string_width("🧑🏿‍🦼‍➡️"), 2)
  assert_eq(get_string_width("🧑🏿‍🦼‍➡"), 3)
  assert_eq(get_string_width("👨‍🦼"), 2)
  assert_eq(get_string_width("👨🏻‍🦼"), 2)
  assert_eq(get_string_width("👨🏼‍🦼"), 2)
  assert_eq(get_string_width("👨🏽‍🦼"), 2)
  assert_eq(get_string_width("👨🏾‍🦼"), 2)
  assert_eq(get_string_width("👨🏿‍🦼"), 2)
  assert_eq(get_string_width("👨‍🦼‍➡️"), 2)
  assert_eq(get_string_width("👨‍🦼‍➡"), 3)
  assert_eq(get_string_width("👨🏻‍🦼‍➡️"), 2)
  assert_eq(get_string_width("👨🏻‍🦼‍➡"), 3)
  assert_eq(get_string_width("👨🏼‍🦼‍➡️"), 2)
  assert_eq(get_string_width("👨🏼‍🦼‍➡"), 3)
  assert_eq(get_string_width("👨🏽‍🦼‍➡️"), 2)
  assert_eq(get_string_width("👨🏽‍🦼‍➡"), 3)
  assert_eq(get_string_width("👨🏾‍🦼‍➡️"), 2)
  assert_eq(get_string_width("👨🏾‍🦼‍➡"), 3)
  assert_eq(get_string_width("👨🏿‍🦼‍➡️"), 2)
  assert_eq(get_string_width("👨🏿‍🦼‍➡"), 3)
  assert_eq(get_string_width("👩‍🦼"), 2)
  assert_eq(get_string_width("👩🏻‍🦼"), 2)
  assert_eq(get_string_width("👩🏼‍🦼"), 2)
  assert_eq(get_string_width("👩🏽‍🦼"), 2)
  assert_eq(get_string_width("👩🏾‍🦼"), 2)
  assert_eq(get_string_width("👩🏿‍🦼"), 2)
  assert_eq(get_string_width("👩‍🦼‍➡️"), 2)
  assert_eq(get_string_width("👩‍🦼‍➡"), 3)
  assert_eq(get_string_width("👩🏻‍🦼‍➡️"), 2)
  assert_eq(get_string_width("👩🏻‍🦼‍➡"), 3)
  assert_eq(get_string_width("👩🏼‍🦼‍➡️"), 2)
  assert_eq(get_string_width("👩🏼‍🦼‍➡"), 3)
  assert_eq(get_string_width("👩🏽‍🦼‍➡️"), 2)
  assert_eq(get_string_width("👩🏽‍🦼‍➡"), 3)
  assert_eq(get_string_width("👩🏾‍🦼‍➡️"), 2)
  assert_eq(get_string_width("👩🏾‍🦼‍➡"), 3)
  assert_eq(get_string_width("👩🏿‍🦼‍➡️"), 2)
  assert_eq(get_string_width("👩🏿‍🦼‍➡"), 3)
  assert_eq(get_string_width("🧑‍🦽"), 2)
  assert_eq(get_string_width("🧑🏻‍🦽"), 2)
  assert_eq(get_string_width("🧑🏼‍🦽"), 2)
  assert_eq(get_string_width("🧑🏽‍🦽"), 2)
  assert_eq(get_string_width("🧑🏾‍🦽"), 2)
  assert_eq(get_string_width("🧑🏿‍🦽"), 2)
  assert_eq(get_string_width("🧑‍🦽‍➡️"), 2)
  assert_eq(get_string_width("🧑‍🦽‍➡"), 3)
  assert_eq(get_string_width("🧑🏻‍🦽‍➡️"), 2)
  assert_eq(get_string_width("🧑🏻‍🦽‍➡"), 3)
  assert_eq(get_string_width("🧑🏼‍🦽‍➡️"), 2)
  assert_eq(get_string_width("🧑🏼‍🦽‍➡"), 3)
  assert_eq(get_string_width("🧑🏽‍🦽‍➡️"), 2)
  assert_eq(get_string_width("🧑🏽‍🦽‍➡"), 3)
  assert_eq(get_string_width("🧑🏾‍🦽‍➡️"), 2)
  assert_eq(get_string_width("🧑🏾‍🦽‍➡"), 3)
  assert_eq(get_string_width("🧑🏿‍🦽‍➡️"), 2)
  assert_eq(get_string_width("🧑🏿‍🦽‍➡"), 3)
  assert_eq(get_string_width("👨‍🦽"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 25" {
  assert_eq(get_string_width("👨🏻‍🦽"), 2)
  assert_eq(get_string_width("👨🏼‍🦽"), 2)
  assert_eq(get_string_width("👨🏽‍🦽"), 2)
  assert_eq(get_string_width("👨🏾‍🦽"), 2)
  assert_eq(get_string_width("👨🏿‍🦽"), 2)
  assert_eq(get_string_width("👨‍🦽‍➡️"), 2)
  assert_eq(get_string_width("👨‍🦽‍➡"), 3)
  assert_eq(get_string_width("👨🏻‍🦽‍➡️"), 2)
  assert_eq(get_string_width("👨🏻‍🦽‍➡"), 3)
  assert_eq(get_string_width("👨🏼‍🦽‍➡️"), 2)
  assert_eq(get_string_width("👨🏼‍🦽‍➡"), 3)
  assert_eq(get_string_width("👨🏽‍🦽‍➡️"), 2)
  assert_eq(get_string_width("👨🏽‍🦽‍➡"), 3)
  assert_eq(get_string_width("👨🏾‍🦽‍➡️"), 2)
  assert_eq(get_string_width("👨🏾‍🦽‍➡"), 3)
  assert_eq(get_string_width("👨🏿‍🦽‍➡️"), 2)
  assert_eq(get_string_width("👨🏿‍🦽‍➡"), 3)
  assert_eq(get_string_width("👩‍🦽"), 2)
  assert_eq(get_string_width("👩🏻‍🦽"), 2)
  assert_eq(get_string_width("👩🏼‍🦽"), 2)
  assert_eq(get_string_width("👩🏽‍🦽"), 2)
  assert_eq(get_string_width("👩🏾‍🦽"), 2)
  assert_eq(get_string_width("👩🏿‍🦽"), 2)
  assert_eq(get_string_width("👩‍🦽‍➡️"), 2)
  assert_eq(get_string_width("👩‍🦽‍➡"), 3)
  assert_eq(get_string_width("👩🏻‍🦽‍➡️"), 2)
  assert_eq(get_string_width("👩🏻‍🦽‍➡"), 3)
  assert_eq(get_string_width("👩🏼‍🦽‍➡️"), 2)
  assert_eq(get_string_width("👩🏼‍🦽‍➡"), 3)
  assert_eq(get_string_width("👩🏽‍🦽‍➡️"), 2)
  assert_eq(get_string_width("👩🏽‍🦽‍➡"), 3)
  assert_eq(get_string_width("👩🏾‍🦽‍➡️"), 2)
  assert_eq(get_string_width("👩🏾‍🦽‍➡"), 3)
  assert_eq(get_string_width("👩🏿‍🦽‍➡️"), 2)
  assert_eq(get_string_width("👩🏿‍🦽‍➡"), 3)
  assert_eq(get_string_width("🏃"), 2)
  assert_eq(get_string_width("🏃🏻"), 2)
  assert_eq(get_string_width("🏃🏼"), 2)
  assert_eq(get_string_width("🏃🏽"), 2)
  assert_eq(get_string_width("🏃🏾"), 2)
  assert_eq(get_string_width("🏃🏿"), 2)
  assert_eq(get_string_width("🏃‍♂️"), 2)
  assert_eq(get_string_width("🏃‍♂"), 3)
  assert_eq(get_string_width("🏃🏻‍♂️"), 2)
  assert_eq(get_string_width("🏃🏻‍♂"), 3)
  assert_eq(get_string_width("🏃🏼‍♂️"), 2)
  assert_eq(get_string_width("🏃🏼‍♂"), 3)
  assert_eq(get_string_width("🏃🏽‍♂️"), 2)
  assert_eq(get_string_width("🏃🏽‍♂"), 3)
  assert_eq(get_string_width("🏃🏾‍♂️"), 2)
  assert_eq(get_string_width("🏃🏾‍♂"), 3)
  assert_eq(get_string_width("🏃🏿‍♂️"), 2)
  assert_eq(get_string_width("🏃🏿‍♂"), 3)
  assert_eq(get_string_width("🏃‍♀️"), 2)
  assert_eq(get_string_width("🏃‍♀"), 3)
  assert_eq(get_string_width("🏃🏻‍♀️"), 2)
  assert_eq(get_string_width("🏃🏻‍♀"), 3)
  assert_eq(get_string_width("🏃🏼‍♀️"), 2)
  assert_eq(get_string_width("🏃🏼‍♀"), 3)
  assert_eq(get_string_width("🏃🏽‍♀️"), 2)
  assert_eq(get_string_width("🏃🏽‍♀"), 3)
  assert_eq(get_string_width("🏃🏾‍♀️"), 2)
  assert_eq(get_string_width("🏃🏾‍♀"), 3)
  assert_eq(get_string_width("🏃🏿‍♀️"), 2)
  assert_eq(get_string_width("🏃🏿‍♀"), 3)
  assert_eq(get_string_width("🏃‍➡️"), 2)
  assert_eq(get_string_width("🏃‍➡"), 3)
  assert_eq(get_string_width("🏃🏻‍➡️"), 2)
  assert_eq(get_string_width("🏃🏻‍➡"), 3)
  assert_eq(get_string_width("🏃🏼‍➡️"), 2)
  assert_eq(get_string_width("🏃🏼‍➡"), 3)
  assert_eq(get_string_width("🏃🏽‍➡️"), 2)
  assert_eq(get_string_width("🏃🏽‍➡"), 3)
  assert_eq(get_string_width("🏃🏾‍➡️"), 2)
  assert_eq(get_string_width("🏃🏾‍➡"), 3)
  assert_eq(get_string_width("🏃🏿‍➡️"), 2)
  assert_eq(get_string_width("🏃🏿‍➡"), 3)
  assert_eq(get_string_width("🏃‍♀️‍➡️"), 2)
  assert_eq(get_string_width("🏃‍♀‍➡️"), 5)
  assert_eq(get_string_width("🏃‍♀️‍➡"), 3)
  assert_eq(get_string_width("🏃‍♀‍➡"), 4)
  assert_eq(get_string_width("🏃🏻‍♀️‍➡️"), 2)
  assert_eq(get_string_width("🏃🏻‍♀‍➡️"), 5)
  assert_eq(get_string_width("🏃🏻‍♀️‍➡"), 3)
  assert_eq(get_string_width("🏃🏻‍♀‍➡"), 4)
  assert_eq(get_string_width("🏃🏼‍♀️‍➡️"), 2)
  assert_eq(get_string_width("🏃🏼‍♀‍➡️"), 5)
  assert_eq(get_string_width("🏃🏼‍♀️‍➡"), 3)
  assert_eq(get_string_width("🏃🏼‍♀‍➡"), 4)
  assert_eq(get_string_width("🏃🏽‍♀️‍➡️"), 2)
  assert_eq(get_string_width("🏃🏽‍♀‍➡️"), 5)
  assert_eq(get_string_width("🏃🏽‍♀️‍➡"), 3)
  assert_eq(get_string_width("🏃🏽‍♀‍➡"), 4)
  assert_eq(get_string_width("🏃🏾‍♀️‍➡️"), 2)
  assert_eq(get_string_width("🏃🏾‍♀‍➡️"), 5)
  assert_eq(get_string_width("🏃🏾‍♀️‍➡"), 3)
  assert_eq(get_string_width("🏃🏾‍♀‍➡"), 4)
  assert_eq(get_string_width("🏃🏿‍♀️‍➡️"), 2)
  assert_eq(get_string_width("🏃🏿‍♀‍➡️"), 5)
  assert_eq(get_string_width("🏃🏿‍♀️‍➡"), 3)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 26" {
  assert_eq(get_string_width("🏃🏿‍♀‍➡"), 4)
  assert_eq(get_string_width("🏃‍♂️‍➡️"), 2)
  assert_eq(get_string_width("🏃‍♂‍➡️"), 5)
  assert_eq(get_string_width("🏃‍♂️‍➡"), 3)
  assert_eq(get_string_width("🏃‍♂‍➡"), 4)
  assert_eq(get_string_width("🏃🏻‍♂️‍➡️"), 2)
  assert_eq(get_string_width("🏃🏻‍♂‍➡️"), 5)
  assert_eq(get_string_width("🏃🏻‍♂️‍➡"), 3)
  assert_eq(get_string_width("🏃🏻‍♂‍➡"), 4)
  assert_eq(get_string_width("🏃🏼‍♂️‍➡️"), 2)
  assert_eq(get_string_width("🏃🏼‍♂‍➡️"), 5)
  assert_eq(get_string_width("🏃🏼‍♂️‍➡"), 3)
  assert_eq(get_string_width("🏃🏼‍♂‍➡"), 4)
  assert_eq(get_string_width("🏃🏽‍♂️‍➡️"), 2)
  assert_eq(get_string_width("🏃🏽‍♂‍➡️"), 5)
  assert_eq(get_string_width("🏃🏽‍♂️‍➡"), 3)
  assert_eq(get_string_width("🏃🏽‍♂‍➡"), 4)
  assert_eq(get_string_width("🏃🏾‍♂️‍➡️"), 2)
  assert_eq(get_string_width("🏃🏾‍♂‍➡️"), 5)
  assert_eq(get_string_width("🏃🏾‍♂️‍➡"), 3)
  assert_eq(get_string_width("🏃🏾‍♂‍➡"), 4)
  assert_eq(get_string_width("🏃🏿‍♂️‍➡️"), 2)
  assert_eq(get_string_width("🏃🏿‍♂‍➡️"), 5)
  assert_eq(get_string_width("🏃🏿‍♂️‍➡"), 3)
  assert_eq(get_string_width("🏃🏿‍♂‍➡"), 4)
  assert_eq(get_string_width("🧑‍🩰"), 2)
  assert_eq(get_string_width("🧑🏻‍🩰"), 2)
  assert_eq(get_string_width("🧑🏼‍🩰"), 2)
  assert_eq(get_string_width("🧑🏽‍🩰"), 2)
  assert_eq(get_string_width("🧑🏾‍🩰"), 2)
  assert_eq(get_string_width("🧑🏿‍🩰"), 2)
  assert_eq(get_string_width("💃"), 2)
  assert_eq(get_string_width("💃🏻"), 2)
  assert_eq(get_string_width("💃🏼"), 2)
  assert_eq(get_string_width("💃🏽"), 2)
  assert_eq(get_string_width("💃🏾"), 2)
  assert_eq(get_string_width("💃🏿"), 2)
  assert_eq(get_string_width("🕺"), 2)
  assert_eq(get_string_width("🕺🏻"), 2)
  assert_eq(get_string_width("🕺🏼"), 2)
  assert_eq(get_string_width("🕺🏽"), 2)
  assert_eq(get_string_width("🕺🏾"), 2)
  assert_eq(get_string_width("🕺🏿"), 2)
  assert_eq(get_string_width("🕴️"), 2)
  assert_eq(get_string_width("🕴"), 1)
  assert_eq(get_string_width("🕴🏻"), 2)
  assert_eq(get_string_width("🕴🏼"), 2)
  assert_eq(get_string_width("🕴🏽"), 2)
  assert_eq(get_string_width("🕴🏾"), 2)
  assert_eq(get_string_width("🕴🏿"), 2)
  assert_eq(get_string_width("👯"), 2)
  assert_eq(get_string_width("👯🏻"), 2)
  assert_eq(get_string_width("👯🏼"), 2)
  assert_eq(get_string_width("👯🏽"), 2)
  assert_eq(get_string_width("👯🏾"), 2)
  assert_eq(get_string_width("👯🏿"), 2)
  assert_eq(get_string_width("👯‍♂️"), 2)
  assert_eq(get_string_width("👯‍♂"), 3)
  assert_eq(get_string_width("👯🏻‍♂️"), 2)
  assert_eq(get_string_width("👯🏻‍♂"), 3)
  assert_eq(get_string_width("👯🏼‍♂️"), 2)
  assert_eq(get_string_width("👯🏼‍♂"), 3)
  assert_eq(get_string_width("👯🏽‍♂️"), 2)
  assert_eq(get_string_width("👯🏽‍♂"), 3)
  assert_eq(get_string_width("👯🏾‍♂️"), 2)
  assert_eq(get_string_width("👯🏾‍♂"), 3)
  assert_eq(get_string_width("👯🏿‍♂️"), 2)
  assert_eq(get_string_width("👯🏿‍♂"), 3)
  assert_eq(get_string_width("👯‍♀️"), 2)
  assert_eq(get_string_width("👯‍♀"), 3)
  assert_eq(get_string_width("👯🏻‍♀️"), 2)
  assert_eq(get_string_width("👯🏻‍♀"), 3)
  assert_eq(get_string_width("👯🏼‍♀️"), 2)
  assert_eq(get_string_width("👯🏼‍♀"), 3)
  assert_eq(get_string_width("👯🏽‍♀️"), 2)
  assert_eq(get_string_width("👯🏽‍♀"), 3)
  assert_eq(get_string_width("👯🏾‍♀️"), 2)
  assert_eq(get_string_width("👯🏾‍♀"), 3)
  assert_eq(get_string_width("👯🏿‍♀️"), 2)
  assert_eq(get_string_width("👯🏿‍♀"), 3)
  assert_eq(get_string_width("🧑🏻‍🐰‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏻‍🐰‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏻‍🐰‍🧑🏾"), 2)
  assert_eq(get_string_width("🧑🏻‍🐰‍🧑🏿"), 2)
  assert_eq(get_string_width("🧑🏼‍🐰‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏼‍🐰‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏼‍🐰‍🧑🏾"), 2)
  assert_eq(get_string_width("🧑🏼‍🐰‍🧑🏿"), 2)
  assert_eq(get_string_width("🧑🏽‍🐰‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏽‍🐰‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏽‍🐰‍🧑🏾"), 2)
  assert_eq(get_string_width("🧑🏽‍🐰‍🧑🏿"), 2)
  assert_eq(get_string_width("🧑🏾‍🐰‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏾‍🐰‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏾‍🐰‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏾‍🐰‍🧑🏿"), 2)
  assert_eq(get_string_width("🧑🏿‍🐰‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏿‍🐰‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏿‍🐰‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏿‍🐰‍🧑🏾"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 27" {
  assert_eq(get_string_width("👨🏻‍🐰‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏻‍🐰‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏻‍🐰‍👨🏾"), 2)
  assert_eq(get_string_width("👨🏻‍🐰‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏼‍🐰‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏼‍🐰‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏼‍🐰‍👨🏾"), 2)
  assert_eq(get_string_width("👨🏼‍🐰‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏽‍🐰‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏽‍🐰‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏽‍🐰‍👨🏾"), 2)
  assert_eq(get_string_width("👨🏽‍🐰‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏾‍🐰‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏾‍🐰‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏾‍🐰‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏾‍🐰‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏿‍🐰‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏿‍🐰‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏿‍🐰‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏿‍🐰‍👨🏾"), 2)
  assert_eq(get_string_width("👩🏻‍🐰‍👩🏼"), 2)
  assert_eq(get_string_width("👩🏻‍🐰‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏻‍🐰‍👩🏾"), 2)
  assert_eq(get_string_width("👩🏻‍🐰‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏼‍🐰‍👩🏻"), 2)
  assert_eq(get_string_width("👩🏼‍🐰‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏼‍🐰‍👩🏾"), 2)
  assert_eq(get_string_width("👩🏼‍🐰‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏽‍🐰‍👩🏻"), 2)
  assert_eq(get_string_width("👩🏽‍🐰‍👩🏼"), 2)
  assert_eq(get_string_width("👩🏽‍🐰‍👩🏾"), 2)
  assert_eq(get_string_width("👩🏽‍🐰‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏾‍🐰‍👩🏻"), 2)
  assert_eq(get_string_width("👩🏾‍🐰‍👩🏼"), 2)
  assert_eq(get_string_width("👩🏾‍🐰‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏾‍🐰‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏿‍🐰‍👩🏻"), 2)
  assert_eq(get_string_width("👩🏿‍🐰‍👩🏼"), 2)
  assert_eq(get_string_width("👩🏿‍🐰‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏿‍🐰‍👩🏾"), 2)
  assert_eq(get_string_width("🧖"), 2)
  assert_eq(get_string_width("🧖🏻"), 2)
  assert_eq(get_string_width("🧖🏼"), 2)
  assert_eq(get_string_width("🧖🏽"), 2)
  assert_eq(get_string_width("🧖🏾"), 2)
  assert_eq(get_string_width("🧖🏿"), 2)
  assert_eq(get_string_width("🧖‍♂️"), 2)
  assert_eq(get_string_width("🧖‍♂"), 3)
  assert_eq(get_string_width("🧖🏻‍♂️"), 2)
  assert_eq(get_string_width("🧖🏻‍♂"), 3)
  assert_eq(get_string_width("🧖🏼‍♂️"), 2)
  assert_eq(get_string_width("🧖🏼‍♂"), 3)
  assert_eq(get_string_width("🧖🏽‍♂️"), 2)
  assert_eq(get_string_width("🧖🏽‍♂"), 3)
  assert_eq(get_string_width("🧖🏾‍♂️"), 2)
  assert_eq(get_string_width("🧖🏾‍♂"), 3)
  assert_eq(get_string_width("🧖🏿‍♂️"), 2)
  assert_eq(get_string_width("🧖🏿‍♂"), 3)
  assert_eq(get_string_width("🧖‍♀️"), 2)
  assert_eq(get_string_width("🧖‍♀"), 3)
  assert_eq(get_string_width("🧖🏻‍♀️"), 2)
  assert_eq(get_string_width("🧖🏻‍♀"), 3)
  assert_eq(get_string_width("🧖🏼‍♀️"), 2)
  assert_eq(get_string_width("🧖🏼‍♀"), 3)
  assert_eq(get_string_width("🧖🏽‍♀️"), 2)
  assert_eq(get_string_width("🧖🏽‍♀"), 3)
  assert_eq(get_string_width("🧖🏾‍♀️"), 2)
  assert_eq(get_string_width("🧖🏾‍♀"), 3)
  assert_eq(get_string_width("🧖🏿‍♀️"), 2)
  assert_eq(get_string_width("🧖🏿‍♀"), 3)
  assert_eq(get_string_width("🧗"), 2)
  assert_eq(get_string_width("🧗🏻"), 2)
  assert_eq(get_string_width("🧗🏼"), 2)
  assert_eq(get_string_width("🧗🏽"), 2)
  assert_eq(get_string_width("🧗🏾"), 2)
  assert_eq(get_string_width("🧗🏿"), 2)
  assert_eq(get_string_width("🧗‍♂️"), 2)
  assert_eq(get_string_width("🧗‍♂"), 3)
  assert_eq(get_string_width("🧗🏻‍♂️"), 2)
  assert_eq(get_string_width("🧗🏻‍♂"), 3)
  assert_eq(get_string_width("🧗🏼‍♂️"), 2)
  assert_eq(get_string_width("🧗🏼‍♂"), 3)
  assert_eq(get_string_width("🧗🏽‍♂️"), 2)
  assert_eq(get_string_width("🧗🏽‍♂"), 3)
  assert_eq(get_string_width("🧗🏾‍♂️"), 2)
  assert_eq(get_string_width("🧗🏾‍♂"), 3)
  assert_eq(get_string_width("🧗🏿‍♂️"), 2)
  assert_eq(get_string_width("🧗🏿‍♂"), 3)
  assert_eq(get_string_width("🧗‍♀️"), 2)
  assert_eq(get_string_width("🧗‍♀"), 3)
  assert_eq(get_string_width("🧗🏻‍♀️"), 2)
  assert_eq(get_string_width("🧗🏻‍♀"), 3)
  assert_eq(get_string_width("🧗🏼‍♀️"), 2)
  assert_eq(get_string_width("🧗🏼‍♀"), 3)
  assert_eq(get_string_width("🧗🏽‍♀️"), 2)
  assert_eq(get_string_width("🧗🏽‍♀"), 3)
  assert_eq(get_string_width("🧗🏾‍♀️"), 2)
  assert_eq(get_string_width("🧗🏾‍♀"), 3)
  assert_eq(get_string_width("🧗🏿‍♀️"), 2)
  assert_eq(get_string_width("🧗🏿‍♀"), 3)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 28" {
  assert_eq(get_string_width("🤺"), 2)
  assert_eq(get_string_width("🏇"), 2)
  assert_eq(get_string_width("🏇🏻"), 2)
  assert_eq(get_string_width("🏇🏼"), 2)
  assert_eq(get_string_width("🏇🏽"), 2)
  assert_eq(get_string_width("🏇🏾"), 2)
  assert_eq(get_string_width("🏇🏿"), 2)
  assert_eq(get_string_width("⛷️"), 2)
  assert_eq(get_string_width("⛷"), 1)
  assert_eq(get_string_width("🏂"), 2)
  assert_eq(get_string_width("🏂🏻"), 2)
  assert_eq(get_string_width("🏂🏼"), 2)
  assert_eq(get_string_width("🏂🏽"), 2)
  assert_eq(get_string_width("🏂🏾"), 2)
  assert_eq(get_string_width("🏂🏿"), 2)
  assert_eq(get_string_width("🏌️"), 2)
  assert_eq(get_string_width("🏌"), 1)
  assert_eq(get_string_width("🏌🏻"), 2)
  assert_eq(get_string_width("🏌🏼"), 2)
  assert_eq(get_string_width("🏌🏽"), 2)
  assert_eq(get_string_width("🏌🏾"), 2)
  assert_eq(get_string_width("🏌🏿"), 2)
  assert_eq(get_string_width("🏌️‍♂️"), 2)
  assert_eq(get_string_width("🏌‍♂️"), 3)
  assert_eq(get_string_width("🏌️‍♂"), 3)
  assert_eq(get_string_width("🏌‍♂"), 2)
  assert_eq(get_string_width("🏌🏻‍♂️"), 2)
  assert_eq(get_string_width("🏌🏻‍♂"), 3)
  assert_eq(get_string_width("🏌🏼‍♂️"), 2)
  assert_eq(get_string_width("🏌🏼‍♂"), 3)
  assert_eq(get_string_width("🏌🏽‍♂️"), 2)
  assert_eq(get_string_width("🏌🏽‍♂"), 3)
  assert_eq(get_string_width("🏌🏾‍♂️"), 2)
  assert_eq(get_string_width("🏌🏾‍♂"), 3)
  assert_eq(get_string_width("🏌🏿‍♂️"), 2)
  assert_eq(get_string_width("🏌🏿‍♂"), 3)
  assert_eq(get_string_width("🏌️‍♀️"), 2)
  assert_eq(get_string_width("🏌‍♀️"), 3)
  assert_eq(get_string_width("🏌️‍♀"), 3)
  assert_eq(get_string_width("🏌‍♀"), 2)
  assert_eq(get_string_width("🏌🏻‍♀️"), 2)
  assert_eq(get_string_width("🏌🏻‍♀"), 3)
  assert_eq(get_string_width("🏌🏼‍♀️"), 2)
  assert_eq(get_string_width("🏌🏼‍♀"), 3)
  assert_eq(get_string_width("🏌🏽‍♀️"), 2)
  assert_eq(get_string_width("🏌🏽‍♀"), 3)
  assert_eq(get_string_width("🏌🏾‍♀️"), 2)
  assert_eq(get_string_width("🏌🏾‍♀"), 3)
  assert_eq(get_string_width("🏌🏿‍♀️"), 2)
  assert_eq(get_string_width("🏌🏿‍♀"), 3)
  assert_eq(get_string_width("🏄"), 2)
  assert_eq(get_string_width("🏄🏻"), 2)
  assert_eq(get_string_width("🏄🏼"), 2)
  assert_eq(get_string_width("🏄🏽"), 2)
  assert_eq(get_string_width("🏄🏾"), 2)
  assert_eq(get_string_width("🏄🏿"), 2)
  assert_eq(get_string_width("🏄‍♂️"), 2)
  assert_eq(get_string_width("🏄‍♂"), 3)
  assert_eq(get_string_width("🏄🏻‍♂️"), 2)
  assert_eq(get_string_width("🏄🏻‍♂"), 3)
  assert_eq(get_string_width("🏄🏼‍♂️"), 2)
  assert_eq(get_string_width("🏄🏼‍♂"), 3)
  assert_eq(get_string_width("🏄🏽‍♂️"), 2)
  assert_eq(get_string_width("🏄🏽‍♂"), 3)
  assert_eq(get_string_width("🏄🏾‍♂️"), 2)
  assert_eq(get_string_width("🏄🏾‍♂"), 3)
  assert_eq(get_string_width("🏄🏿‍♂️"), 2)
  assert_eq(get_string_width("🏄🏿‍♂"), 3)
  assert_eq(get_string_width("🏄‍♀️"), 2)
  assert_eq(get_string_width("🏄‍♀"), 3)
  assert_eq(get_string_width("🏄🏻‍♀️"), 2)
  assert_eq(get_string_width("🏄🏻‍♀"), 3)
  assert_eq(get_string_width("🏄🏼‍♀️"), 2)
  assert_eq(get_string_width("🏄🏼‍♀"), 3)
  assert_eq(get_string_width("🏄🏽‍♀️"), 2)
  assert_eq(get_string_width("🏄🏽‍♀"), 3)
  assert_eq(get_string_width("🏄🏾‍♀️"), 2)
  assert_eq(get_string_width("🏄🏾‍♀"), 3)
  assert_eq(get_string_width("🏄🏿‍♀️"), 2)
  assert_eq(get_string_width("🏄🏿‍♀"), 3)
  assert_eq(get_string_width("🚣"), 2)
  assert_eq(get_string_width("🚣🏻"), 2)
  assert_eq(get_string_width("🚣🏼"), 2)
  assert_eq(get_string_width("🚣🏽"), 2)
  assert_eq(get_string_width("🚣🏾"), 2)
  assert_eq(get_string_width("🚣🏿"), 2)
  assert_eq(get_string_width("🚣‍♂️"), 2)
  assert_eq(get_string_width("🚣‍♂"), 3)
  assert_eq(get_string_width("🚣🏻‍♂️"), 2)
  assert_eq(get_string_width("🚣🏻‍♂"), 3)
  assert_eq(get_string_width("🚣🏼‍♂️"), 2)
  assert_eq(get_string_width("🚣🏼‍♂"), 3)
  assert_eq(get_string_width("🚣🏽‍♂️"), 2)
  assert_eq(get_string_width("🚣🏽‍♂"), 3)
  assert_eq(get_string_width("🚣🏾‍♂️"), 2)
  assert_eq(get_string_width("🚣🏾‍♂"), 3)
  assert_eq(get_string_width("🚣🏿‍♂️"), 2)
  assert_eq(get_string_width("🚣🏿‍♂"), 3)
  assert_eq(get_string_width("🚣‍♀️"), 2)
  assert_eq(get_string_width("🚣‍♀"), 3)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 29" {
  assert_eq(get_string_width("🚣🏻‍♀️"), 2)
  assert_eq(get_string_width("🚣🏻‍♀"), 3)
  assert_eq(get_string_width("🚣🏼‍♀️"), 2)
  assert_eq(get_string_width("🚣🏼‍♀"), 3)
  assert_eq(get_string_width("🚣🏽‍♀️"), 2)
  assert_eq(get_string_width("🚣🏽‍♀"), 3)
  assert_eq(get_string_width("🚣🏾‍♀️"), 2)
  assert_eq(get_string_width("🚣🏾‍♀"), 3)
  assert_eq(get_string_width("🚣🏿‍♀️"), 2)
  assert_eq(get_string_width("🚣🏿‍♀"), 3)
  assert_eq(get_string_width("🏊"), 2)
  assert_eq(get_string_width("🏊🏻"), 2)
  assert_eq(get_string_width("🏊🏼"), 2)
  assert_eq(get_string_width("🏊🏽"), 2)
  assert_eq(get_string_width("🏊🏾"), 2)
  assert_eq(get_string_width("🏊🏿"), 2)
  assert_eq(get_string_width("🏊‍♂️"), 2)
  assert_eq(get_string_width("🏊‍♂"), 3)
  assert_eq(get_string_width("🏊🏻‍♂️"), 2)
  assert_eq(get_string_width("🏊🏻‍♂"), 3)
  assert_eq(get_string_width("🏊🏼‍♂️"), 2)
  assert_eq(get_string_width("🏊🏼‍♂"), 3)
  assert_eq(get_string_width("🏊🏽‍♂️"), 2)
  assert_eq(get_string_width("🏊🏽‍♂"), 3)
  assert_eq(get_string_width("🏊🏾‍♂️"), 2)
  assert_eq(get_string_width("🏊🏾‍♂"), 3)
  assert_eq(get_string_width("🏊🏿‍♂️"), 2)
  assert_eq(get_string_width("🏊🏿‍♂"), 3)
  assert_eq(get_string_width("🏊‍♀️"), 2)
  assert_eq(get_string_width("🏊‍♀"), 3)
  assert_eq(get_string_width("🏊🏻‍♀️"), 2)
  assert_eq(get_string_width("🏊🏻‍♀"), 3)
  assert_eq(get_string_width("🏊🏼‍♀️"), 2)
  assert_eq(get_string_width("🏊🏼‍♀"), 3)
  assert_eq(get_string_width("🏊🏽‍♀️"), 2)
  assert_eq(get_string_width("🏊🏽‍♀"), 3)
  assert_eq(get_string_width("🏊🏾‍♀️"), 2)
  assert_eq(get_string_width("🏊🏾‍♀"), 3)
  assert_eq(get_string_width("🏊🏿‍♀️"), 2)
  assert_eq(get_string_width("🏊🏿‍♀"), 3)
  assert_eq(get_string_width("⛹️"), 2)
  assert_eq(get_string_width("⛹"), 1)
  assert_eq(get_string_width("⛹🏻"), 2)
  assert_eq(get_string_width("⛹🏼"), 2)
  assert_eq(get_string_width("⛹🏽"), 2)
  assert_eq(get_string_width("⛹🏾"), 2)
  assert_eq(get_string_width("⛹🏿"), 2)
  assert_eq(get_string_width("⛹️‍♂️"), 2)
  assert_eq(get_string_width("⛹‍♂️"), 3)
  assert_eq(get_string_width("⛹️‍♂"), 3)
  assert_eq(get_string_width("⛹‍♂"), 2)
  assert_eq(get_string_width("⛹🏻‍♂️"), 2)
  assert_eq(get_string_width("⛹🏻‍♂"), 3)
  assert_eq(get_string_width("⛹🏼‍♂️"), 2)
  assert_eq(get_string_width("⛹🏼‍♂"), 3)
  assert_eq(get_string_width("⛹🏽‍♂️"), 2)
  assert_eq(get_string_width("⛹🏽‍♂"), 3)
  assert_eq(get_string_width("⛹🏾‍♂️"), 2)
  assert_eq(get_string_width("⛹🏾‍♂"), 3)
  assert_eq(get_string_width("⛹🏿‍♂️"), 2)
  assert_eq(get_string_width("⛹🏿‍♂"), 3)
  assert_eq(get_string_width("⛹️‍♀️"), 2)
  assert_eq(get_string_width("⛹‍♀️"), 3)
  assert_eq(get_string_width("⛹️‍♀"), 3)
  assert_eq(get_string_width("⛹‍♀"), 2)
  assert_eq(get_string_width("⛹🏻‍♀️"), 2)
  assert_eq(get_string_width("⛹🏻‍♀"), 3)
  assert_eq(get_string_width("⛹🏼‍♀️"), 2)
  assert_eq(get_string_width("⛹🏼‍♀"), 3)
  assert_eq(get_string_width("⛹🏽‍♀️"), 2)
  assert_eq(get_string_width("⛹🏽‍♀"), 3)
  assert_eq(get_string_width("⛹🏾‍♀️"), 2)
  assert_eq(get_string_width("⛹🏾‍♀"), 3)
  assert_eq(get_string_width("⛹🏿‍♀️"), 2)
  assert_eq(get_string_width("⛹🏿‍♀"), 3)
  assert_eq(get_string_width("🏋️"), 2)
  assert_eq(get_string_width("🏋"), 1)
  assert_eq(get_string_width("🏋🏻"), 2)
  assert_eq(get_string_width("🏋🏼"), 2)
  assert_eq(get_string_width("🏋🏽"), 2)
  assert_eq(get_string_width("🏋🏾"), 2)
  assert_eq(get_string_width("🏋🏿"), 2)
  assert_eq(get_string_width("🏋️‍♂️"), 2)
  assert_eq(get_string_width("🏋‍♂️"), 3)
  assert_eq(get_string_width("🏋️‍♂"), 3)
  assert_eq(get_string_width("🏋‍♂"), 2)
  assert_eq(get_string_width("🏋🏻‍♂️"), 2)
  assert_eq(get_string_width("🏋🏻‍♂"), 3)
  assert_eq(get_string_width("🏋🏼‍♂️"), 2)
  assert_eq(get_string_width("🏋🏼‍♂"), 3)
  assert_eq(get_string_width("🏋🏽‍♂️"), 2)
  assert_eq(get_string_width("🏋🏽‍♂"), 3)
  assert_eq(get_string_width("🏋🏾‍♂️"), 2)
  assert_eq(get_string_width("🏋🏾‍♂"), 3)
  assert_eq(get_string_width("🏋🏿‍♂️"), 2)
  assert_eq(get_string_width("🏋🏿‍♂"), 3)
  assert_eq(get_string_width("🏋️‍♀️"), 2)
  assert_eq(get_string_width("🏋‍♀️"), 3)
  assert_eq(get_string_width("🏋️‍♀"), 3)
  assert_eq(get_string_width("🏋‍♀"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 30" {
  assert_eq(get_string_width("🏋🏻‍♀️"), 2)
  assert_eq(get_string_width("🏋🏻‍♀"), 3)
  assert_eq(get_string_width("🏋🏼‍♀️"), 2)
  assert_eq(get_string_width("🏋🏼‍♀"), 3)
  assert_eq(get_string_width("🏋🏽‍♀️"), 2)
  assert_eq(get_string_width("🏋🏽‍♀"), 3)
  assert_eq(get_string_width("🏋🏾‍♀️"), 2)
  assert_eq(get_string_width("🏋🏾‍♀"), 3)
  assert_eq(get_string_width("🏋🏿‍♀️"), 2)
  assert_eq(get_string_width("🏋🏿‍♀"), 3)
  assert_eq(get_string_width("🚴"), 2)
  assert_eq(get_string_width("🚴🏻"), 2)
  assert_eq(get_string_width("🚴🏼"), 2)
  assert_eq(get_string_width("🚴🏽"), 2)
  assert_eq(get_string_width("🚴🏾"), 2)
  assert_eq(get_string_width("🚴🏿"), 2)
  assert_eq(get_string_width("🚴‍♂️"), 2)
  assert_eq(get_string_width("🚴‍♂"), 3)
  assert_eq(get_string_width("🚴🏻‍♂️"), 2)
  assert_eq(get_string_width("🚴🏻‍♂"), 3)
  assert_eq(get_string_width("🚴🏼‍♂️"), 2)
  assert_eq(get_string_width("🚴🏼‍♂"), 3)
  assert_eq(get_string_width("🚴🏽‍♂️"), 2)
  assert_eq(get_string_width("🚴🏽‍♂"), 3)
  assert_eq(get_string_width("🚴🏾‍♂️"), 2)
  assert_eq(get_string_width("🚴🏾‍♂"), 3)
  assert_eq(get_string_width("🚴🏿‍♂️"), 2)
  assert_eq(get_string_width("🚴🏿‍♂"), 3)
  assert_eq(get_string_width("🚴‍♀️"), 2)
  assert_eq(get_string_width("🚴‍♀"), 3)
  assert_eq(get_string_width("🚴🏻‍♀️"), 2)
  assert_eq(get_string_width("🚴🏻‍♀"), 3)
  assert_eq(get_string_width("🚴🏼‍♀️"), 2)
  assert_eq(get_string_width("🚴🏼‍♀"), 3)
  assert_eq(get_string_width("🚴🏽‍♀️"), 2)
  assert_eq(get_string_width("🚴🏽‍♀"), 3)
  assert_eq(get_string_width("🚴🏾‍♀️"), 2)
  assert_eq(get_string_width("🚴🏾‍♀"), 3)
  assert_eq(get_string_width("🚴🏿‍♀️"), 2)
  assert_eq(get_string_width("🚴🏿‍♀"), 3)
  assert_eq(get_string_width("🚵"), 2)
  assert_eq(get_string_width("🚵🏻"), 2)
  assert_eq(get_string_width("🚵🏼"), 2)
  assert_eq(get_string_width("🚵🏽"), 2)
  assert_eq(get_string_width("🚵🏾"), 2)
  assert_eq(get_string_width("🚵🏿"), 2)
  assert_eq(get_string_width("🚵‍♂️"), 2)
  assert_eq(get_string_width("🚵‍♂"), 3)
  assert_eq(get_string_width("🚵🏻‍♂️"), 2)
  assert_eq(get_string_width("🚵🏻‍♂"), 3)
  assert_eq(get_string_width("🚵🏼‍♂️"), 2)
  assert_eq(get_string_width("🚵🏼‍♂"), 3)
  assert_eq(get_string_width("🚵🏽‍♂️"), 2)
  assert_eq(get_string_width("🚵🏽‍♂"), 3)
  assert_eq(get_string_width("🚵🏾‍♂️"), 2)
  assert_eq(get_string_width("🚵🏾‍♂"), 3)
  assert_eq(get_string_width("🚵🏿‍♂️"), 2)
  assert_eq(get_string_width("🚵🏿‍♂"), 3)
  assert_eq(get_string_width("🚵‍♀️"), 2)
  assert_eq(get_string_width("🚵‍♀"), 3)
  assert_eq(get_string_width("🚵🏻‍♀️"), 2)
  assert_eq(get_string_width("🚵🏻‍♀"), 3)
  assert_eq(get_string_width("🚵🏼‍♀️"), 2)
  assert_eq(get_string_width("🚵🏼‍♀"), 3)
  assert_eq(get_string_width("🚵🏽‍♀️"), 2)
  assert_eq(get_string_width("🚵🏽‍♀"), 3)
  assert_eq(get_string_width("🚵🏾‍♀️"), 2)
  assert_eq(get_string_width("🚵🏾‍♀"), 3)
  assert_eq(get_string_width("🚵🏿‍♀️"), 2)
  assert_eq(get_string_width("🚵🏿‍♀"), 3)
  assert_eq(get_string_width("🤸"), 2)
  assert_eq(get_string_width("🤸🏻"), 2)
  assert_eq(get_string_width("🤸🏼"), 2)
  assert_eq(get_string_width("🤸🏽"), 2)
  assert_eq(get_string_width("🤸🏾"), 2)
  assert_eq(get_string_width("🤸🏿"), 2)
  assert_eq(get_string_width("🤸‍♂️"), 2)
  assert_eq(get_string_width("🤸‍♂"), 3)
  assert_eq(get_string_width("🤸🏻‍♂️"), 2)
  assert_eq(get_string_width("🤸🏻‍♂"), 3)
  assert_eq(get_string_width("🤸🏼‍♂️"), 2)
  assert_eq(get_string_width("🤸🏼‍♂"), 3)
  assert_eq(get_string_width("🤸🏽‍♂️"), 2)
  assert_eq(get_string_width("🤸🏽‍♂"), 3)
  assert_eq(get_string_width("🤸🏾‍♂️"), 2)
  assert_eq(get_string_width("🤸🏾‍♂"), 3)
  assert_eq(get_string_width("🤸🏿‍♂️"), 2)
  assert_eq(get_string_width("🤸🏿‍♂"), 3)
  assert_eq(get_string_width("🤸‍♀️"), 2)
  assert_eq(get_string_width("🤸‍♀"), 3)
  assert_eq(get_string_width("🤸🏻‍♀️"), 2)
  assert_eq(get_string_width("🤸🏻‍♀"), 3)
  assert_eq(get_string_width("🤸🏼‍♀️"), 2)
  assert_eq(get_string_width("🤸🏼‍♀"), 3)
  assert_eq(get_string_width("🤸🏽‍♀️"), 2)
  assert_eq(get_string_width("🤸🏽‍♀"), 3)
  assert_eq(get_string_width("🤸🏾‍♀️"), 2)
  assert_eq(get_string_width("🤸🏾‍♀"), 3)
  assert_eq(get_string_width("🤸🏿‍♀️"), 2)
  assert_eq(get_string_width("🤸🏿‍♀"), 3)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 31" {
  assert_eq(get_string_width("🤼"), 2)
  assert_eq(get_string_width("🤼🏻"), 2)
  assert_eq(get_string_width("🤼🏼"), 2)
  assert_eq(get_string_width("🤼🏽"), 2)
  assert_eq(get_string_width("🤼🏾"), 2)
  assert_eq(get_string_width("🤼🏿"), 2)
  assert_eq(get_string_width("🤼‍♂️"), 2)
  assert_eq(get_string_width("🤼‍♂"), 3)
  assert_eq(get_string_width("🤼🏻‍♂️"), 2)
  assert_eq(get_string_width("🤼🏻‍♂"), 3)
  assert_eq(get_string_width("🤼🏼‍♂️"), 2)
  assert_eq(get_string_width("🤼🏼‍♂"), 3)
  assert_eq(get_string_width("🤼🏽‍♂️"), 2)
  assert_eq(get_string_width("🤼🏽‍♂"), 3)
  assert_eq(get_string_width("🤼🏾‍♂️"), 2)
  assert_eq(get_string_width("🤼🏾‍♂"), 3)
  assert_eq(get_string_width("🤼🏿‍♂️"), 2)
  assert_eq(get_string_width("🤼🏿‍♂"), 3)
  assert_eq(get_string_width("🤼‍♀️"), 2)
  assert_eq(get_string_width("🤼‍♀"), 3)
  assert_eq(get_string_width("🤼🏻‍♀️"), 2)
  assert_eq(get_string_width("🤼🏻‍♀"), 3)
  assert_eq(get_string_width("🤼🏼‍♀️"), 2)
  assert_eq(get_string_width("🤼🏼‍♀"), 3)
  assert_eq(get_string_width("🤼🏽‍♀️"), 2)
  assert_eq(get_string_width("🤼🏽‍♀"), 3)
  assert_eq(get_string_width("🤼🏾‍♀️"), 2)
  assert_eq(get_string_width("🤼🏾‍♀"), 3)
  assert_eq(get_string_width("🤼🏿‍♀️"), 2)
  assert_eq(get_string_width("🤼🏿‍♀"), 3)
  assert_eq(get_string_width("🧑🏻‍🫯‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏻‍🫯‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏻‍🫯‍🧑🏾"), 2)
  assert_eq(get_string_width("🧑🏻‍🫯‍🧑🏿"), 2)
  assert_eq(get_string_width("🧑🏼‍🫯‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏼‍🫯‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏼‍🫯‍🧑🏾"), 2)
  assert_eq(get_string_width("🧑🏼‍🫯‍🧑🏿"), 2)
  assert_eq(get_string_width("🧑🏽‍🫯‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏽‍🫯‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏽‍🫯‍🧑🏾"), 2)
  assert_eq(get_string_width("🧑🏽‍🫯‍🧑🏿"), 2)
  assert_eq(get_string_width("🧑🏾‍🫯‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏾‍🫯‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏾‍🫯‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏾‍🫯‍🧑🏿"), 2)
  assert_eq(get_string_width("🧑🏿‍🫯‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏿‍🫯‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏿‍🫯‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏿‍🫯‍🧑🏾"), 2)
  assert_eq(get_string_width("👨🏻‍🫯‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏻‍🫯‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏻‍🫯‍👨🏾"), 2)
  assert_eq(get_string_width("👨🏻‍🫯‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏼‍🫯‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏼‍🫯‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏼‍🫯‍👨🏾"), 2)
  assert_eq(get_string_width("👨🏼‍🫯‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏽‍🫯‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏽‍🫯‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏽‍🫯‍👨🏾"), 2)
  assert_eq(get_string_width("👨🏽‍🫯‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏾‍🫯‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏾‍🫯‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏾‍🫯‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏾‍🫯‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏿‍🫯‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏿‍🫯‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏿‍🫯‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏿‍🫯‍👨🏾"), 2)
  assert_eq(get_string_width("👩🏻‍🫯‍👩🏼"), 2)
  assert_eq(get_string_width("👩🏻‍🫯‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏻‍🫯‍👩🏾"), 2)
  assert_eq(get_string_width("👩🏻‍🫯‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏼‍🫯‍👩🏻"), 2)
  assert_eq(get_string_width("👩🏼‍🫯‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏼‍🫯‍👩🏾"), 2)
  assert_eq(get_string_width("👩🏼‍🫯‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏽‍🫯‍👩🏻"), 2)
  assert_eq(get_string_width("👩🏽‍🫯‍👩🏼"), 2)
  assert_eq(get_string_width("👩🏽‍🫯‍👩🏾"), 2)
  assert_eq(get_string_width("👩🏽‍🫯‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏾‍🫯‍👩🏻"), 2)
  assert_eq(get_string_width("👩🏾‍🫯‍👩🏼"), 2)
  assert_eq(get_string_width("👩🏾‍🫯‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏾‍🫯‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏿‍🫯‍👩🏻"), 2)
  assert_eq(get_string_width("👩🏿‍🫯‍👩🏼"), 2)
  assert_eq(get_string_width("👩🏿‍🫯‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏿‍🫯‍👩🏾"), 2)
  assert_eq(get_string_width("🤽"), 2)
  assert_eq(get_string_width("🤽🏻"), 2)
  assert_eq(get_string_width("🤽🏼"), 2)
  assert_eq(get_string_width("🤽🏽"), 2)
  assert_eq(get_string_width("🤽🏾"), 2)
  assert_eq(get_string_width("🤽🏿"), 2)
  assert_eq(get_string_width("🤽‍♂️"), 2)
  assert_eq(get_string_width("🤽‍♂"), 3)
  assert_eq(get_string_width("🤽🏻‍♂️"), 2)
  assert_eq(get_string_width("🤽🏻‍♂"), 3)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 32" {
  assert_eq(get_string_width("🤽🏼‍♂️"), 2)
  assert_eq(get_string_width("🤽🏼‍♂"), 3)
  assert_eq(get_string_width("🤽🏽‍♂️"), 2)
  assert_eq(get_string_width("🤽🏽‍♂"), 3)
  assert_eq(get_string_width("🤽🏾‍♂️"), 2)
  assert_eq(get_string_width("🤽🏾‍♂"), 3)
  assert_eq(get_string_width("🤽🏿‍♂️"), 2)
  assert_eq(get_string_width("🤽🏿‍♂"), 3)
  assert_eq(get_string_width("🤽‍♀️"), 2)
  assert_eq(get_string_width("🤽‍♀"), 3)
  assert_eq(get_string_width("🤽🏻‍♀️"), 2)
  assert_eq(get_string_width("🤽🏻‍♀"), 3)
  assert_eq(get_string_width("🤽🏼‍♀️"), 2)
  assert_eq(get_string_width("🤽🏼‍♀"), 3)
  assert_eq(get_string_width("🤽🏽‍♀️"), 2)
  assert_eq(get_string_width("🤽🏽‍♀"), 3)
  assert_eq(get_string_width("🤽🏾‍♀️"), 2)
  assert_eq(get_string_width("🤽🏾‍♀"), 3)
  assert_eq(get_string_width("🤽🏿‍♀️"), 2)
  assert_eq(get_string_width("🤽🏿‍♀"), 3)
  assert_eq(get_string_width("🤾"), 2)
  assert_eq(get_string_width("🤾🏻"), 2)
  assert_eq(get_string_width("🤾🏼"), 2)
  assert_eq(get_string_width("🤾🏽"), 2)
  assert_eq(get_string_width("🤾🏾"), 2)
  assert_eq(get_string_width("🤾🏿"), 2)
  assert_eq(get_string_width("🤾‍♂️"), 2)
  assert_eq(get_string_width("🤾‍♂"), 3)
  assert_eq(get_string_width("🤾🏻‍♂️"), 2)
  assert_eq(get_string_width("🤾🏻‍♂"), 3)
  assert_eq(get_string_width("🤾🏼‍♂️"), 2)
  assert_eq(get_string_width("🤾🏼‍♂"), 3)
  assert_eq(get_string_width("🤾🏽‍♂️"), 2)
  assert_eq(get_string_width("🤾🏽‍♂"), 3)
  assert_eq(get_string_width("🤾🏾‍♂️"), 2)
  assert_eq(get_string_width("🤾🏾‍♂"), 3)
  assert_eq(get_string_width("🤾🏿‍♂️"), 2)
  assert_eq(get_string_width("🤾🏿‍♂"), 3)
  assert_eq(get_string_width("🤾‍♀️"), 2)
  assert_eq(get_string_width("🤾‍♀"), 3)
  assert_eq(get_string_width("🤾🏻‍♀️"), 2)
  assert_eq(get_string_width("🤾🏻‍♀"), 3)
  assert_eq(get_string_width("🤾🏼‍♀️"), 2)
  assert_eq(get_string_width("🤾🏼‍♀"), 3)
  assert_eq(get_string_width("🤾🏽‍♀️"), 2)
  assert_eq(get_string_width("🤾🏽‍♀"), 3)
  assert_eq(get_string_width("🤾🏾‍♀️"), 2)
  assert_eq(get_string_width("🤾🏾‍♀"), 3)
  assert_eq(get_string_width("🤾🏿‍♀️"), 2)
  assert_eq(get_string_width("🤾🏿‍♀"), 3)
  assert_eq(get_string_width("🤹"), 2)
  assert_eq(get_string_width("🤹🏻"), 2)
  assert_eq(get_string_width("🤹🏼"), 2)
  assert_eq(get_string_width("🤹🏽"), 2)
  assert_eq(get_string_width("🤹🏾"), 2)
  assert_eq(get_string_width("🤹🏿"), 2)
  assert_eq(get_string_width("🤹‍♂️"), 2)
  assert_eq(get_string_width("🤹‍♂"), 3)
  assert_eq(get_string_width("🤹🏻‍♂️"), 2)
  assert_eq(get_string_width("🤹🏻‍♂"), 3)
  assert_eq(get_string_width("🤹🏼‍♂️"), 2)
  assert_eq(get_string_width("🤹🏼‍♂"), 3)
  assert_eq(get_string_width("🤹🏽‍♂️"), 2)
  assert_eq(get_string_width("🤹🏽‍♂"), 3)
  assert_eq(get_string_width("🤹🏾‍♂️"), 2)
  assert_eq(get_string_width("🤹🏾‍♂"), 3)
  assert_eq(get_string_width("🤹🏿‍♂️"), 2)
  assert_eq(get_string_width("🤹🏿‍♂"), 3)
  assert_eq(get_string_width("🤹‍♀️"), 2)
  assert_eq(get_string_width("🤹‍♀"), 3)
  assert_eq(get_string_width("🤹🏻‍♀️"), 2)
  assert_eq(get_string_width("🤹🏻‍♀"), 3)
  assert_eq(get_string_width("🤹🏼‍♀️"), 2)
  assert_eq(get_string_width("🤹🏼‍♀"), 3)
  assert_eq(get_string_width("🤹🏽‍♀️"), 2)
  assert_eq(get_string_width("🤹🏽‍♀"), 3)
  assert_eq(get_string_width("🤹🏾‍♀️"), 2)
  assert_eq(get_string_width("🤹🏾‍♀"), 3)
  assert_eq(get_string_width("🤹🏿‍♀️"), 2)
  assert_eq(get_string_width("🤹🏿‍♀"), 3)
  assert_eq(get_string_width("🧘"), 2)
  assert_eq(get_string_width("🧘🏻"), 2)
  assert_eq(get_string_width("🧘🏼"), 2)
  assert_eq(get_string_width("🧘🏽"), 2)
  assert_eq(get_string_width("🧘🏾"), 2)
  assert_eq(get_string_width("🧘🏿"), 2)
  assert_eq(get_string_width("🧘‍♂️"), 2)
  assert_eq(get_string_width("🧘‍♂"), 3)
  assert_eq(get_string_width("🧘🏻‍♂️"), 2)
  assert_eq(get_string_width("🧘🏻‍♂"), 3)
  assert_eq(get_string_width("🧘🏼‍♂️"), 2)
  assert_eq(get_string_width("🧘🏼‍♂"), 3)
  assert_eq(get_string_width("🧘🏽‍♂️"), 2)
  assert_eq(get_string_width("🧘🏽‍♂"), 3)
  assert_eq(get_string_width("🧘🏾‍♂️"), 2)
  assert_eq(get_string_width("🧘🏾‍♂"), 3)
  assert_eq(get_string_width("🧘🏿‍♂️"), 2)
  assert_eq(get_string_width("🧘🏿‍♂"), 3)
  assert_eq(get_string_width("🧘‍♀️"), 2)
  assert_eq(get_string_width("🧘‍♀"), 3)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 33" {
  assert_eq(get_string_width("🧘🏻‍♀️"), 2)
  assert_eq(get_string_width("🧘🏻‍♀"), 3)
  assert_eq(get_string_width("🧘🏼‍♀️"), 2)
  assert_eq(get_string_width("🧘🏼‍♀"), 3)
  assert_eq(get_string_width("🧘🏽‍♀️"), 2)
  assert_eq(get_string_width("🧘🏽‍♀"), 3)
  assert_eq(get_string_width("🧘🏾‍♀️"), 2)
  assert_eq(get_string_width("🧘🏾‍♀"), 3)
  assert_eq(get_string_width("🧘🏿‍♀️"), 2)
  assert_eq(get_string_width("🧘🏿‍♀"), 3)
  assert_eq(get_string_width("🛀"), 2)
  assert_eq(get_string_width("🛀🏻"), 2)
  assert_eq(get_string_width("🛀🏼"), 2)
  assert_eq(get_string_width("🛀🏽"), 2)
  assert_eq(get_string_width("🛀🏾"), 2)
  assert_eq(get_string_width("🛀🏿"), 2)
  assert_eq(get_string_width("🛌"), 2)
  assert_eq(get_string_width("🛌🏻"), 2)
  assert_eq(get_string_width("🛌🏼"), 2)
  assert_eq(get_string_width("🛌🏽"), 2)
  assert_eq(get_string_width("🛌🏾"), 2)
  assert_eq(get_string_width("🛌🏿"), 2)
  assert_eq(get_string_width("🧑🏻‍🤝‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏻‍🤝‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏻‍🤝‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏻‍🤝‍🧑🏾"), 2)
  assert_eq(get_string_width("🧑🏻‍🤝‍🧑🏿"), 2)
  assert_eq(get_string_width("🧑🏼‍🤝‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏼‍🤝‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏼‍🤝‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏼‍🤝‍🧑🏾"), 2)
  assert_eq(get_string_width("🧑🏼‍🤝‍🧑🏿"), 2)
  assert_eq(get_string_width("🧑🏽‍🤝‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏽‍🤝‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏽‍🤝‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏽‍🤝‍🧑🏾"), 2)
  assert_eq(get_string_width("🧑🏽‍🤝‍🧑🏿"), 2)
  assert_eq(get_string_width("🧑🏾‍🤝‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏾‍🤝‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏾‍🤝‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏾‍🤝‍🧑🏾"), 2)
  assert_eq(get_string_width("🧑🏾‍🤝‍🧑🏿"), 2)
  assert_eq(get_string_width("🧑🏿‍🤝‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏿‍🤝‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏿‍🤝‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏿‍🤝‍🧑🏾"), 2)
  assert_eq(get_string_width("🧑🏿‍🤝‍🧑🏿"), 2)
  assert_eq(get_string_width("👭"), 2)
  assert_eq(get_string_width("👭🏻"), 2)
  assert_eq(get_string_width("👩🏻‍🤝‍👩🏼"), 2)
  assert_eq(get_string_width("👩🏻‍🤝‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏻‍🤝‍👩🏾"), 2)
  assert_eq(get_string_width("👩🏻‍🤝‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏼‍🤝‍👩🏻"), 2)
  assert_eq(get_string_width("👭🏼"), 2)
  assert_eq(get_string_width("👩🏼‍🤝‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏼‍🤝‍👩🏾"), 2)
  assert_eq(get_string_width("👩🏼‍🤝‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏽‍🤝‍👩🏻"), 2)
  assert_eq(get_string_width("👩🏽‍🤝‍👩🏼"), 2)
  assert_eq(get_string_width("👭🏽"), 2)
  assert_eq(get_string_width("👩🏽‍🤝‍👩🏾"), 2)
  assert_eq(get_string_width("👩🏽‍🤝‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏾‍🤝‍👩🏻"), 2)
  assert_eq(get_string_width("👩🏾‍🤝‍👩🏼"), 2)
  assert_eq(get_string_width("👩🏾‍🤝‍👩🏽"), 2)
  assert_eq(get_string_width("👭🏾"), 2)
  assert_eq(get_string_width("👩🏾‍🤝‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏿‍🤝‍👩🏻"), 2)
  assert_eq(get_string_width("👩🏿‍🤝‍👩🏼"), 2)
  assert_eq(get_string_width("👩🏿‍🤝‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏿‍🤝‍👩🏾"), 2)
  assert_eq(get_string_width("👭🏿"), 2)
  assert_eq(get_string_width("👫"), 2)
  assert_eq(get_string_width("👫🏻"), 2)
  assert_eq(get_string_width("👩🏻‍🤝‍👨🏼"), 2)
  assert_eq(get_string_width("👩🏻‍🤝‍👨🏽"), 2)
  assert_eq(get_string_width("👩🏻‍🤝‍👨🏾"), 2)
  assert_eq(get_string_width("👩🏻‍🤝‍👨🏿"), 2)
  assert_eq(get_string_width("👩🏼‍🤝‍👨🏻"), 2)
  assert_eq(get_string_width("👫🏼"), 2)
  assert_eq(get_string_width("👩🏼‍🤝‍👨🏽"), 2)
  assert_eq(get_string_width("👩🏼‍🤝‍👨🏾"), 2)
  assert_eq(get_string_width("👩🏼‍🤝‍👨🏿"), 2)
  assert_eq(get_string_width("👩🏽‍🤝‍👨🏻"), 2)
  assert_eq(get_string_width("👩🏽‍🤝‍👨🏼"), 2)
  assert_eq(get_string_width("👫🏽"), 2)
  assert_eq(get_string_width("👩🏽‍🤝‍👨🏾"), 2)
  assert_eq(get_string_width("👩🏽‍🤝‍👨🏿"), 2)
  assert_eq(get_string_width("👩🏾‍🤝‍👨🏻"), 2)
  assert_eq(get_string_width("👩🏾‍🤝‍👨🏼"), 2)
  assert_eq(get_string_width("👩🏾‍🤝‍👨🏽"), 2)
  assert_eq(get_string_width("👫🏾"), 2)
  assert_eq(get_string_width("👩🏾‍🤝‍👨🏿"), 2)
  assert_eq(get_string_width("👩🏿‍🤝‍👨🏻"), 2)
  assert_eq(get_string_width("👩🏿‍🤝‍👨🏼"), 2)
  assert_eq(get_string_width("👩🏿‍🤝‍👨🏽"), 2)
  assert_eq(get_string_width("👩🏿‍🤝‍👨🏾"), 2)
  assert_eq(get_string_width("👫🏿"), 2)
  assert_eq(get_string_width("👬"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 34" {
  assert_eq(get_string_width("👬🏻"), 2)
  assert_eq(get_string_width("👨🏻‍🤝‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏻‍🤝‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏻‍🤝‍👨🏾"), 2)
  assert_eq(get_string_width("👨🏻‍🤝‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏼‍🤝‍👨🏻"), 2)
  assert_eq(get_string_width("👬🏼"), 2)
  assert_eq(get_string_width("👨🏼‍🤝‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏼‍🤝‍👨🏾"), 2)
  assert_eq(get_string_width("👨🏼‍🤝‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏽‍🤝‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏽‍🤝‍👨🏼"), 2)
  assert_eq(get_string_width("👬🏽"), 2)
  assert_eq(get_string_width("👨🏽‍🤝‍👨🏾"), 2)
  assert_eq(get_string_width("👨🏽‍🤝‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏾‍🤝‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏾‍🤝‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏾‍🤝‍👨🏽"), 2)
  assert_eq(get_string_width("👬🏾"), 2)
  assert_eq(get_string_width("👨🏾‍🤝‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏿‍🤝‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏿‍🤝‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏿‍🤝‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏿‍🤝‍👨🏾"), 2)
  assert_eq(get_string_width("👬🏿"), 2)
  assert_eq(get_string_width("💏"), 2)
  assert_eq(get_string_width("💏🏻"), 2)
  assert_eq(get_string_width("💏🏼"), 2)
  assert_eq(get_string_width("💏🏽"), 2)
  assert_eq(get_string_width("💏🏾"), 2)
  assert_eq(get_string_width("💏🏿"), 2)
  assert_eq(get_string_width("🧑🏻‍❤️‍💋‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏻‍❤‍💋‍🧑🏼"), 5)
  assert_eq(get_string_width("🧑🏻‍❤️‍💋‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏻‍❤‍💋‍🧑🏽"), 5)
  assert_eq(get_string_width("🧑🏻‍❤️‍💋‍🧑🏾"), 2)
  assert_eq(get_string_width("🧑🏻‍❤‍💋‍🧑🏾"), 5)
  assert_eq(get_string_width("🧑🏻‍❤️‍💋‍🧑🏿"), 2)
  assert_eq(get_string_width("🧑🏻‍❤‍💋‍🧑🏿"), 5)
  assert_eq(get_string_width("🧑🏼‍❤️‍💋‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏼‍❤‍💋‍🧑🏻"), 5)
  assert_eq(get_string_width("🧑🏼‍❤️‍💋‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏼‍❤‍💋‍🧑🏽"), 5)
  assert_eq(get_string_width("🧑🏼‍❤️‍💋‍🧑🏾"), 2)
  assert_eq(get_string_width("🧑🏼‍❤‍💋‍🧑🏾"), 5)
  assert_eq(get_string_width("🧑🏼‍❤️‍💋‍🧑🏿"), 2)
  assert_eq(get_string_width("🧑🏼‍❤‍💋‍🧑🏿"), 5)
  assert_eq(get_string_width("🧑🏽‍❤️‍💋‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏽‍❤‍💋‍🧑🏻"), 5)
  assert_eq(get_string_width("🧑🏽‍❤️‍💋‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏽‍❤‍💋‍🧑🏼"), 5)
  assert_eq(get_string_width("🧑🏽‍❤️‍💋‍🧑🏾"), 2)
  assert_eq(get_string_width("🧑🏽‍❤‍💋‍🧑🏾"), 5)
  assert_eq(get_string_width("🧑🏽‍❤️‍💋‍🧑🏿"), 2)
  assert_eq(get_string_width("🧑🏽‍❤‍💋‍🧑🏿"), 5)
  assert_eq(get_string_width("🧑🏾‍❤️‍💋‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏾‍❤‍💋‍🧑🏻"), 5)
  assert_eq(get_string_width("🧑🏾‍❤️‍💋‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏾‍❤‍💋‍🧑🏼"), 5)
  assert_eq(get_string_width("🧑🏾‍❤️‍💋‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏾‍❤‍💋‍🧑🏽"), 5)
  assert_eq(get_string_width("🧑🏾‍❤️‍💋‍🧑🏿"), 2)
  assert_eq(get_string_width("🧑🏾‍❤‍💋‍🧑🏿"), 5)
  assert_eq(get_string_width("🧑🏿‍❤️‍💋‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏿‍❤‍💋‍🧑🏻"), 5)
  assert_eq(get_string_width("🧑🏿‍❤️‍💋‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏿‍❤‍💋‍🧑🏼"), 5)
  assert_eq(get_string_width("🧑🏿‍❤️‍💋‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏿‍❤‍💋‍🧑🏽"), 5)
  assert_eq(get_string_width("🧑🏿‍❤️‍💋‍🧑🏾"), 2)
  assert_eq(get_string_width("🧑🏿‍❤‍💋‍🧑🏾"), 5)
  assert_eq(get_string_width("👩‍❤️‍💋‍👨"), 2)
  assert_eq(get_string_width("👩‍❤‍💋‍👨"), 5)
  assert_eq(get_string_width("👩🏻‍❤️‍💋‍👨🏻"), 2)
  assert_eq(get_string_width("👩🏻‍❤‍💋‍👨🏻"), 5)
  assert_eq(get_string_width("👩🏻‍❤️‍💋‍👨🏼"), 2)
  assert_eq(get_string_width("👩🏻‍❤‍💋‍👨🏼"), 5)
  assert_eq(get_string_width("👩🏻‍❤️‍💋‍👨🏽"), 2)
  assert_eq(get_string_width("👩🏻‍❤‍💋‍👨🏽"), 5)
  assert_eq(get_string_width("👩🏻‍❤️‍💋‍👨🏾"), 2)
  assert_eq(get_string_width("👩🏻‍❤‍💋‍👨🏾"), 5)
  assert_eq(get_string_width("👩🏻‍❤️‍💋‍👨🏿"), 2)
  assert_eq(get_string_width("👩🏻‍❤‍💋‍👨🏿"), 5)
  assert_eq(get_string_width("👩🏼‍❤️‍💋‍👨🏻"), 2)
  assert_eq(get_string_width("👩🏼‍❤‍💋‍👨🏻"), 5)
  assert_eq(get_string_width("👩🏼‍❤️‍💋‍👨🏼"), 2)
  assert_eq(get_string_width("👩🏼‍❤‍💋‍👨🏼"), 5)
  assert_eq(get_string_width("👩🏼‍❤️‍💋‍👨🏽"), 2)
  assert_eq(get_string_width("👩🏼‍❤‍💋‍👨🏽"), 5)
  assert_eq(get_string_width("👩🏼‍❤️‍💋‍👨🏾"), 2)
  assert_eq(get_string_width("👩🏼‍❤‍💋‍👨🏾"), 5)
  assert_eq(get_string_width("👩🏼‍❤️‍💋‍👨🏿"), 2)
  assert_eq(get_string_width("👩🏼‍❤‍💋‍👨🏿"), 5)
  assert_eq(get_string_width("👩🏽‍❤️‍💋‍👨🏻"), 2)
  assert_eq(get_string_width("👩🏽‍❤‍💋‍👨🏻"), 5)
  assert_eq(get_string_width("👩🏽‍❤️‍💋‍👨🏼"), 2)
  assert_eq(get_string_width("👩🏽‍❤‍💋‍👨🏼"), 5)
  assert_eq(get_string_width("👩🏽‍❤️‍💋‍👨🏽"), 2)
  assert_eq(get_string_width("👩🏽‍❤‍💋‍👨🏽"), 5)
  assert_eq(get_string_width("👩🏽‍❤️‍💋‍👨🏾"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 35" {
  assert_eq(get_string_width("👩🏽‍❤‍💋‍👨🏾"), 5)
  assert_eq(get_string_width("👩🏽‍❤️‍💋‍👨🏿"), 2)
  assert_eq(get_string_width("👩🏽‍❤‍💋‍👨🏿"), 5)
  assert_eq(get_string_width("👩🏾‍❤️‍💋‍👨🏻"), 2)
  assert_eq(get_string_width("👩🏾‍❤‍💋‍👨🏻"), 5)
  assert_eq(get_string_width("👩🏾‍❤️‍💋‍👨🏼"), 2)
  assert_eq(get_string_width("👩🏾‍❤‍💋‍👨🏼"), 5)
  assert_eq(get_string_width("👩🏾‍❤️‍💋‍👨🏽"), 2)
  assert_eq(get_string_width("👩🏾‍❤‍💋‍👨🏽"), 5)
  assert_eq(get_string_width("👩🏾‍❤️‍💋‍👨🏾"), 2)
  assert_eq(get_string_width("👩🏾‍❤‍💋‍👨🏾"), 5)
  assert_eq(get_string_width("👩🏾‍❤️‍💋‍👨🏿"), 2)
  assert_eq(get_string_width("👩🏾‍❤‍💋‍👨🏿"), 5)
  assert_eq(get_string_width("👩🏿‍❤️‍💋‍👨🏻"), 2)
  assert_eq(get_string_width("👩🏿‍❤‍💋‍👨🏻"), 5)
  assert_eq(get_string_width("👩🏿‍❤️‍💋‍👨🏼"), 2)
  assert_eq(get_string_width("👩🏿‍❤‍💋‍👨🏼"), 5)
  assert_eq(get_string_width("👩🏿‍❤️‍💋‍👨🏽"), 2)
  assert_eq(get_string_width("👩🏿‍❤‍💋‍👨🏽"), 5)
  assert_eq(get_string_width("👩🏿‍❤️‍💋‍👨🏾"), 2)
  assert_eq(get_string_width("👩🏿‍❤‍💋‍👨🏾"), 5)
  assert_eq(get_string_width("👩🏿‍❤️‍💋‍👨🏿"), 2)
  assert_eq(get_string_width("👩🏿‍❤‍💋‍👨🏿"), 5)
  assert_eq(get_string_width("👨‍❤️‍💋‍👨"), 2)
  assert_eq(get_string_width("👨‍❤‍💋‍👨"), 5)
  assert_eq(get_string_width("👨🏻‍❤️‍💋‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏻‍❤‍💋‍👨🏻"), 5)
  assert_eq(get_string_width("👨🏻‍❤️‍💋‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏻‍❤‍💋‍👨🏼"), 5)
  assert_eq(get_string_width("👨🏻‍❤️‍💋‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏻‍❤‍💋‍👨🏽"), 5)
  assert_eq(get_string_width("👨🏻‍❤️‍💋‍👨🏾"), 2)
  assert_eq(get_string_width("👨🏻‍❤‍💋‍👨🏾"), 5)
  assert_eq(get_string_width("👨🏻‍❤️‍💋‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏻‍❤‍💋‍👨🏿"), 5)
  assert_eq(get_string_width("👨🏼‍❤️‍💋‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏼‍❤‍💋‍👨🏻"), 5)
  assert_eq(get_string_width("👨🏼‍❤️‍💋‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏼‍❤‍💋‍👨🏼"), 5)
  assert_eq(get_string_width("👨🏼‍❤️‍💋‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏼‍❤‍💋‍👨🏽"), 5)
  assert_eq(get_string_width("👨🏼‍❤️‍💋‍👨🏾"), 2)
  assert_eq(get_string_width("👨🏼‍❤‍💋‍👨🏾"), 5)
  assert_eq(get_string_width("👨🏼‍❤️‍💋‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏼‍❤‍💋‍👨🏿"), 5)
  assert_eq(get_string_width("👨🏽‍❤️‍💋‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏽‍❤‍💋‍👨🏻"), 5)
  assert_eq(get_string_width("👨🏽‍❤️‍💋‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏽‍❤‍💋‍👨🏼"), 5)
  assert_eq(get_string_width("👨🏽‍❤️‍💋‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏽‍❤‍💋‍👨🏽"), 5)
  assert_eq(get_string_width("👨🏽‍❤️‍💋‍👨🏾"), 2)
  assert_eq(get_string_width("👨🏽‍❤‍💋‍👨🏾"), 5)
  assert_eq(get_string_width("👨🏽‍❤️‍💋‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏽‍❤‍💋‍👨🏿"), 5)
  assert_eq(get_string_width("👨🏾‍❤️‍💋‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏾‍❤‍💋‍👨🏻"), 5)
  assert_eq(get_string_width("👨🏾‍❤️‍💋‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏾‍❤‍💋‍👨🏼"), 5)
  assert_eq(get_string_width("👨🏾‍❤️‍💋‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏾‍❤‍💋‍👨🏽"), 5)
  assert_eq(get_string_width("👨🏾‍❤️‍💋‍👨🏾"), 2)
  assert_eq(get_string_width("👨🏾‍❤‍💋‍👨🏾"), 5)
  assert_eq(get_string_width("👨🏾‍❤️‍💋‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏾‍❤‍💋‍👨🏿"), 5)
  assert_eq(get_string_width("👨🏿‍❤️‍💋‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏿‍❤‍💋‍👨🏻"), 5)
  assert_eq(get_string_width("👨🏿‍❤️‍💋‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏿‍❤‍💋‍👨🏼"), 5)
  assert_eq(get_string_width("👨🏿‍❤️‍💋‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏿‍❤‍💋‍👨🏽"), 5)
  assert_eq(get_string_width("👨🏿‍❤️‍💋‍👨🏾"), 2)
  assert_eq(get_string_width("👨🏿‍❤‍💋‍👨🏾"), 5)
  assert_eq(get_string_width("👨🏿‍❤️‍💋‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏿‍❤‍💋‍👨🏿"), 5)
  assert_eq(get_string_width("👩‍❤️‍💋‍👩"), 2)
  assert_eq(get_string_width("👩‍❤‍💋‍👩"), 5)
  assert_eq(get_string_width("👩🏻‍❤️‍💋‍👩🏻"), 2)
  assert_eq(get_string_width("👩🏻‍❤‍💋‍👩🏻"), 5)
  assert_eq(get_string_width("👩🏻‍❤️‍💋‍👩🏼"), 2)
  assert_eq(get_string_width("👩🏻‍❤‍💋‍👩🏼"), 5)
  assert_eq(get_string_width("👩🏻‍❤️‍💋‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏻‍❤‍💋‍👩🏽"), 5)
  assert_eq(get_string_width("👩🏻‍❤️‍💋‍👩🏾"), 2)
  assert_eq(get_string_width("👩🏻‍❤‍💋‍👩🏾"), 5)
  assert_eq(get_string_width("👩🏻‍❤️‍💋‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏻‍❤‍💋‍👩🏿"), 5)
  assert_eq(get_string_width("👩🏼‍❤️‍💋‍👩🏻"), 2)
  assert_eq(get_string_width("👩🏼‍❤‍💋‍👩🏻"), 5)
  assert_eq(get_string_width("👩🏼‍❤️‍💋‍👩🏼"), 2)
  assert_eq(get_string_width("👩🏼‍❤‍💋‍👩🏼"), 5)
  assert_eq(get_string_width("👩🏼‍❤️‍💋‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏼‍❤‍💋‍👩🏽"), 5)
  assert_eq(get_string_width("👩🏼‍❤️‍💋‍👩🏾"), 2)
  assert_eq(get_string_width("👩🏼‍❤‍💋‍👩🏾"), 5)
  assert_eq(get_string_width("👩🏼‍❤️‍💋‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏼‍❤‍💋‍👩🏿"), 5)
  assert_eq(get_string_width("👩🏽‍❤️‍💋‍👩🏻"), 2)
  assert_eq(get_string_width("👩🏽‍❤‍💋‍👩🏻"), 5)
  assert_eq(get_string_width("👩🏽‍❤️‍💋‍👩🏼"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 36" {
  assert_eq(get_string_width("👩🏽‍❤‍💋‍👩🏼"), 5)
  assert_eq(get_string_width("👩🏽‍❤️‍💋‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏽‍❤‍💋‍👩🏽"), 5)
  assert_eq(get_string_width("👩🏽‍❤️‍💋‍👩🏾"), 2)
  assert_eq(get_string_width("👩🏽‍❤‍💋‍👩🏾"), 5)
  assert_eq(get_string_width("👩🏽‍❤️‍💋‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏽‍❤‍💋‍👩🏿"), 5)
  assert_eq(get_string_width("👩🏾‍❤️‍💋‍👩🏻"), 2)
  assert_eq(get_string_width("👩🏾‍❤‍💋‍👩🏻"), 5)
  assert_eq(get_string_width("👩🏾‍❤️‍💋‍👩🏼"), 2)
  assert_eq(get_string_width("👩🏾‍❤‍💋‍👩🏼"), 5)
  assert_eq(get_string_width("👩🏾‍❤️‍💋‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏾‍❤‍💋‍👩🏽"), 5)
  assert_eq(get_string_width("👩🏾‍❤️‍💋‍👩🏾"), 2)
  assert_eq(get_string_width("👩🏾‍❤‍💋‍👩🏾"), 5)
  assert_eq(get_string_width("👩🏾‍❤️‍💋‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏾‍❤‍💋‍👩🏿"), 5)
  assert_eq(get_string_width("👩🏿‍❤️‍💋‍👩🏻"), 2)
  assert_eq(get_string_width("👩🏿‍❤‍💋‍👩🏻"), 5)
  assert_eq(get_string_width("👩🏿‍❤️‍💋‍👩🏼"), 2)
  assert_eq(get_string_width("👩🏿‍❤‍💋‍👩🏼"), 5)
  assert_eq(get_string_width("👩🏿‍❤️‍💋‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏿‍❤‍💋‍👩🏽"), 5)
  assert_eq(get_string_width("👩🏿‍❤️‍💋‍👩🏾"), 2)
  assert_eq(get_string_width("👩🏿‍❤‍💋‍👩🏾"), 5)
  assert_eq(get_string_width("👩🏿‍❤️‍💋‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏿‍❤‍💋‍👩🏿"), 5)
  assert_eq(get_string_width("💑"), 2)
  assert_eq(get_string_width("💑🏻"), 2)
  assert_eq(get_string_width("💑🏼"), 2)
  assert_eq(get_string_width("💑🏽"), 2)
  assert_eq(get_string_width("💑🏾"), 2)
  assert_eq(get_string_width("💑🏿"), 2)
  assert_eq(get_string_width("🧑🏻‍❤️‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏻‍❤‍🧑🏼"), 5)
  assert_eq(get_string_width("🧑🏻‍❤️‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏻‍❤‍🧑🏽"), 5)
  assert_eq(get_string_width("🧑🏻‍❤️‍🧑🏾"), 2)
  assert_eq(get_string_width("🧑🏻‍❤‍🧑🏾"), 5)
  assert_eq(get_string_width("🧑🏻‍❤️‍🧑🏿"), 2)
  assert_eq(get_string_width("🧑🏻‍❤‍🧑🏿"), 5)
  assert_eq(get_string_width("🧑🏼‍❤️‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏼‍❤‍🧑🏻"), 5)
  assert_eq(get_string_width("🧑🏼‍❤️‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏼‍❤‍🧑🏽"), 5)
  assert_eq(get_string_width("🧑🏼‍❤️‍🧑🏾"), 2)
  assert_eq(get_string_width("🧑🏼‍❤‍🧑🏾"), 5)
  assert_eq(get_string_width("🧑🏼‍❤️‍🧑🏿"), 2)
  assert_eq(get_string_width("🧑🏼‍❤‍🧑🏿"), 5)
  assert_eq(get_string_width("🧑🏽‍❤️‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏽‍❤‍🧑🏻"), 5)
  assert_eq(get_string_width("🧑🏽‍❤️‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏽‍❤‍🧑🏼"), 5)
  assert_eq(get_string_width("🧑🏽‍❤️‍🧑🏾"), 2)
  assert_eq(get_string_width("🧑🏽‍❤‍🧑🏾"), 5)
  assert_eq(get_string_width("🧑🏽‍❤️‍🧑🏿"), 2)
  assert_eq(get_string_width("🧑🏽‍❤‍🧑🏿"), 5)
  assert_eq(get_string_width("🧑🏾‍❤️‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏾‍❤‍🧑🏻"), 5)
  assert_eq(get_string_width("🧑🏾‍❤️‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏾‍❤‍🧑🏼"), 5)
  assert_eq(get_string_width("🧑🏾‍❤️‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏾‍❤‍🧑🏽"), 5)
  assert_eq(get_string_width("🧑🏾‍❤️‍🧑🏿"), 2)
  assert_eq(get_string_width("🧑🏾‍❤‍🧑🏿"), 5)
  assert_eq(get_string_width("🧑🏿‍❤️‍🧑🏻"), 2)
  assert_eq(get_string_width("🧑🏿‍❤‍🧑🏻"), 5)
  assert_eq(get_string_width("🧑🏿‍❤️‍🧑🏼"), 2)
  assert_eq(get_string_width("🧑🏿‍❤‍🧑🏼"), 5)
  assert_eq(get_string_width("🧑🏿‍❤️‍🧑🏽"), 2)
  assert_eq(get_string_width("🧑🏿‍❤‍🧑🏽"), 5)
  assert_eq(get_string_width("🧑🏿‍❤️‍🧑🏾"), 2)
  assert_eq(get_string_width("🧑🏿‍❤‍🧑🏾"), 5)
  assert_eq(get_string_width("👩‍❤️‍👨"), 2)
  assert_eq(get_string_width("👩‍❤‍👨"), 5)
  assert_eq(get_string_width("👩🏻‍❤️‍👨🏻"), 2)
  assert_eq(get_string_width("👩🏻‍❤‍👨🏻"), 5)
  assert_eq(get_string_width("👩🏻‍❤️‍👨🏼"), 2)
  assert_eq(get_string_width("👩🏻‍❤‍👨🏼"), 5)
  assert_eq(get_string_width("👩🏻‍❤️‍👨🏽"), 2)
  assert_eq(get_string_width("👩🏻‍❤‍👨🏽"), 5)
  assert_eq(get_string_width("👩🏻‍❤️‍👨🏾"), 2)
  assert_eq(get_string_width("👩🏻‍❤‍👨🏾"), 5)
  assert_eq(get_string_width("👩🏻‍❤️‍👨🏿"), 2)
  assert_eq(get_string_width("👩🏻‍❤‍👨🏿"), 5)
  assert_eq(get_string_width("👩🏼‍❤️‍👨🏻"), 2)
  assert_eq(get_string_width("👩🏼‍❤‍👨🏻"), 5)
  assert_eq(get_string_width("👩🏼‍❤️‍👨🏼"), 2)
  assert_eq(get_string_width("👩🏼‍❤‍👨🏼"), 5)
  assert_eq(get_string_width("👩🏼‍❤️‍👨🏽"), 2)
  assert_eq(get_string_width("👩🏼‍❤‍👨🏽"), 5)
  assert_eq(get_string_width("👩🏼‍❤️‍👨🏾"), 2)
  assert_eq(get_string_width("👩🏼‍❤‍👨🏾"), 5)
  assert_eq(get_string_width("👩🏼‍❤️‍👨🏿"), 2)
  assert_eq(get_string_width("👩🏼‍❤‍👨🏿"), 5)
  assert_eq(get_string_width("👩🏽‍❤️‍👨🏻"), 2)
  assert_eq(get_string_width("👩🏽‍❤‍👨🏻"), 5)
  assert_eq(get_string_width("👩🏽‍❤️‍👨🏼"), 2)
  assert_eq(get_string_width("👩🏽‍❤‍👨🏼"), 5)
  assert_eq(get_string_width("👩🏽‍❤️‍👨🏽"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 37" {
  assert_eq(get_string_width("👩🏽‍❤‍👨🏽"), 5)
  assert_eq(get_string_width("👩🏽‍❤️‍👨🏾"), 2)
  assert_eq(get_string_width("👩🏽‍❤‍👨🏾"), 5)
  assert_eq(get_string_width("👩🏽‍❤️‍👨🏿"), 2)
  assert_eq(get_string_width("👩🏽‍❤‍👨🏿"), 5)
  assert_eq(get_string_width("👩🏾‍❤️‍👨🏻"), 2)
  assert_eq(get_string_width("👩🏾‍❤‍👨🏻"), 5)
  assert_eq(get_string_width("👩🏾‍❤️‍👨🏼"), 2)
  assert_eq(get_string_width("👩🏾‍❤‍👨🏼"), 5)
  assert_eq(get_string_width("👩🏾‍❤️‍👨🏽"), 2)
  assert_eq(get_string_width("👩🏾‍❤‍👨🏽"), 5)
  assert_eq(get_string_width("👩🏾‍❤️‍👨🏾"), 2)
  assert_eq(get_string_width("👩🏾‍❤‍👨🏾"), 5)
  assert_eq(get_string_width("👩🏾‍❤️‍👨🏿"), 2)
  assert_eq(get_string_width("👩🏾‍❤‍👨🏿"), 5)
  assert_eq(get_string_width("👩🏿‍❤️‍👨🏻"), 2)
  assert_eq(get_string_width("👩🏿‍❤‍👨🏻"), 5)
  assert_eq(get_string_width("👩🏿‍❤️‍👨🏼"), 2)
  assert_eq(get_string_width("👩🏿‍❤‍👨🏼"), 5)
  assert_eq(get_string_width("👩🏿‍❤️‍👨🏽"), 2)
  assert_eq(get_string_width("👩🏿‍❤‍👨🏽"), 5)
  assert_eq(get_string_width("👩🏿‍❤️‍👨🏾"), 2)
  assert_eq(get_string_width("👩🏿‍❤‍👨🏾"), 5)
  assert_eq(get_string_width("👩🏿‍❤️‍👨🏿"), 2)
  assert_eq(get_string_width("👩🏿‍❤‍👨🏿"), 5)
  assert_eq(get_string_width("👨‍❤️‍👨"), 2)
  assert_eq(get_string_width("👨‍❤‍👨"), 5)
  assert_eq(get_string_width("👨🏻‍❤️‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏻‍❤‍👨🏻"), 5)
  assert_eq(get_string_width("👨🏻‍❤️‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏻‍❤‍👨🏼"), 5)
  assert_eq(get_string_width("👨🏻‍❤️‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏻‍❤‍👨🏽"), 5)
  assert_eq(get_string_width("👨🏻‍❤️‍👨🏾"), 2)
  assert_eq(get_string_width("👨🏻‍❤‍👨🏾"), 5)
  assert_eq(get_string_width("👨🏻‍❤️‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏻‍❤‍👨🏿"), 5)
  assert_eq(get_string_width("👨🏼‍❤️‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏼‍❤‍👨🏻"), 5)
  assert_eq(get_string_width("👨🏼‍❤️‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏼‍❤‍👨🏼"), 5)
  assert_eq(get_string_width("👨🏼‍❤️‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏼‍❤‍👨🏽"), 5)
  assert_eq(get_string_width("👨🏼‍❤️‍👨🏾"), 2)
  assert_eq(get_string_width("👨🏼‍❤‍👨🏾"), 5)
  assert_eq(get_string_width("👨🏼‍❤️‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏼‍❤‍👨🏿"), 5)
  assert_eq(get_string_width("👨🏽‍❤️‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏽‍❤‍👨🏻"), 5)
  assert_eq(get_string_width("👨🏽‍❤️‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏽‍❤‍👨🏼"), 5)
  assert_eq(get_string_width("👨🏽‍❤️‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏽‍❤‍👨🏽"), 5)
  assert_eq(get_string_width("👨🏽‍❤️‍👨🏾"), 2)
  assert_eq(get_string_width("👨🏽‍❤‍👨🏾"), 5)
  assert_eq(get_string_width("👨🏽‍❤️‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏽‍❤‍👨🏿"), 5)
  assert_eq(get_string_width("👨🏾‍❤️‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏾‍❤‍👨🏻"), 5)
  assert_eq(get_string_width("👨🏾‍❤️‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏾‍❤‍👨🏼"), 5)
  assert_eq(get_string_width("👨🏾‍❤️‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏾‍❤‍👨🏽"), 5)
  assert_eq(get_string_width("👨🏾‍❤️‍👨🏾"), 2)
  assert_eq(get_string_width("👨🏾‍❤‍👨🏾"), 5)
  assert_eq(get_string_width("👨🏾‍❤️‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏾‍❤‍👨🏿"), 5)
  assert_eq(get_string_width("👨🏿‍❤️‍👨🏻"), 2)
  assert_eq(get_string_width("👨🏿‍❤‍👨🏻"), 5)
  assert_eq(get_string_width("👨🏿‍❤️‍👨🏼"), 2)
  assert_eq(get_string_width("👨🏿‍❤‍👨🏼"), 5)
  assert_eq(get_string_width("👨🏿‍❤️‍👨🏽"), 2)
  assert_eq(get_string_width("👨🏿‍❤‍👨🏽"), 5)
  assert_eq(get_string_width("👨🏿‍❤️‍👨🏾"), 2)
  assert_eq(get_string_width("👨🏿‍❤‍👨🏾"), 5)
  assert_eq(get_string_width("👨🏿‍❤️‍👨🏿"), 2)
  assert_eq(get_string_width("👨🏿‍❤‍👨🏿"), 5)
  assert_eq(get_string_width("👩‍❤️‍👩"), 2)
  assert_eq(get_string_width("👩‍❤‍👩"), 5)
  assert_eq(get_string_width("👩🏻‍❤️‍👩🏻"), 2)
  assert_eq(get_string_width("👩🏻‍❤‍👩🏻"), 5)
  assert_eq(get_string_width("👩🏻‍❤️‍👩🏼"), 2)
  assert_eq(get_string_width("👩🏻‍❤‍👩🏼"), 5)
  assert_eq(get_string_width("👩🏻‍❤️‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏻‍❤‍👩🏽"), 5)
  assert_eq(get_string_width("👩🏻‍❤️‍👩🏾"), 2)
  assert_eq(get_string_width("👩🏻‍❤‍👩🏾"), 5)
  assert_eq(get_string_width("👩🏻‍❤️‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏻‍❤‍👩🏿"), 5)
  assert_eq(get_string_width("👩🏼‍❤️‍👩🏻"), 2)
  assert_eq(get_string_width("👩🏼‍❤‍👩🏻"), 5)
  assert_eq(get_string_width("👩🏼‍❤️‍👩🏼"), 2)
  assert_eq(get_string_width("👩🏼‍❤‍👩🏼"), 5)
  assert_eq(get_string_width("👩🏼‍❤️‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏼‍❤‍👩🏽"), 5)
  assert_eq(get_string_width("👩🏼‍❤️‍👩🏾"), 2)
  assert_eq(get_string_width("👩🏼‍❤‍👩🏾"), 5)
  assert_eq(get_string_width("👩🏼‍❤️‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏼‍❤‍👩🏿"), 5)
  assert_eq(get_string_width("👩🏽‍❤️‍👩🏻"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 38" {
  assert_eq(get_string_width("👩🏽‍❤‍👩🏻"), 5)
  assert_eq(get_string_width("👩🏽‍❤️‍👩🏼"), 2)
  assert_eq(get_string_width("👩🏽‍❤‍👩🏼"), 5)
  assert_eq(get_string_width("👩🏽‍❤️‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏽‍❤‍👩🏽"), 5)
  assert_eq(get_string_width("👩🏽‍❤️‍👩🏾"), 2)
  assert_eq(get_string_width("👩🏽‍❤‍👩🏾"), 5)
  assert_eq(get_string_width("👩🏽‍❤️‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏽‍❤‍👩🏿"), 5)
  assert_eq(get_string_width("👩🏾‍❤️‍👩🏻"), 2)
  assert_eq(get_string_width("👩🏾‍❤‍👩🏻"), 5)
  assert_eq(get_string_width("👩🏾‍❤️‍👩🏼"), 2)
  assert_eq(get_string_width("👩🏾‍❤‍👩🏼"), 5)
  assert_eq(get_string_width("👩🏾‍❤️‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏾‍❤‍👩🏽"), 5)
  assert_eq(get_string_width("👩🏾‍❤️‍👩🏾"), 2)
  assert_eq(get_string_width("👩🏾‍❤‍👩🏾"), 5)
  assert_eq(get_string_width("👩🏾‍❤️‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏾‍❤‍👩🏿"), 5)
  assert_eq(get_string_width("👩🏿‍❤️‍👩🏻"), 2)
  assert_eq(get_string_width("👩🏿‍❤‍👩🏻"), 5)
  assert_eq(get_string_width("👩🏿‍❤️‍👩🏼"), 2)
  assert_eq(get_string_width("👩🏿‍❤‍👩🏼"), 5)
  assert_eq(get_string_width("👩🏿‍❤️‍👩🏽"), 2)
  assert_eq(get_string_width("👩🏿‍❤‍👩🏽"), 5)
  assert_eq(get_string_width("👩🏿‍❤️‍👩🏾"), 2)
  assert_eq(get_string_width("👩🏿‍❤‍👩🏾"), 5)
  assert_eq(get_string_width("👩🏿‍❤️‍👩🏿"), 2)
  assert_eq(get_string_width("👩🏿‍❤‍👩🏿"), 5)
  assert_eq(get_string_width("👨‍👩‍👦"), 2)
  assert_eq(get_string_width("👨‍👩‍👧"), 2)
  assert_eq(get_string_width("👨‍👩‍👧‍👦"), 2)
  assert_eq(get_string_width("👨‍👩‍👦‍👦"), 2)
  assert_eq(get_string_width("👨‍👩‍👧‍👧"), 2)
  assert_eq(get_string_width("👨‍👨‍👦"), 2)
  assert_eq(get_string_width("👨‍👨‍👧"), 2)
  assert_eq(get_string_width("👨‍👨‍👧‍👦"), 2)
  assert_eq(get_string_width("👨‍👨‍👦‍👦"), 2)
  assert_eq(get_string_width("👨‍👨‍👧‍👧"), 2)
  assert_eq(get_string_width("👩‍👩‍👦"), 2)
  assert_eq(get_string_width("👩‍👩‍👧"), 2)
  assert_eq(get_string_width("👩‍👩‍👧‍👦"), 2)
  assert_eq(get_string_width("👩‍👩‍👦‍👦"), 2)
  assert_eq(get_string_width("👩‍👩‍👧‍👧"), 2)
  assert_eq(get_string_width("👨‍👦"), 2)
  assert_eq(get_string_width("👨‍👦‍👦"), 2)
  assert_eq(get_string_width("👨‍👧"), 2)
  assert_eq(get_string_width("👨‍👧‍👦"), 2)
  assert_eq(get_string_width("👨‍👧‍👧"), 2)
  assert_eq(get_string_width("👩‍👦"), 2)
  assert_eq(get_string_width("👩‍👦‍👦"), 2)
  assert_eq(get_string_width("👩‍👧"), 2)
  assert_eq(get_string_width("👩‍👧‍👦"), 2)
  assert_eq(get_string_width("👩‍👧‍👧"), 2)
  assert_eq(get_string_width("🗣️"), 2)
  assert_eq(get_string_width("🗣"), 1)
  assert_eq(get_string_width("👤"), 2)
  assert_eq(get_string_width("👥"), 2)
  assert_eq(get_string_width("🫂"), 2)
  assert_eq(get_string_width("🧑‍🧑‍🧒"), 2)
  assert_eq(get_string_width("🧑‍🧑‍🧒‍🧒"), 2)
  assert_eq(get_string_width("🧑‍🧒"), 2)
  assert_eq(get_string_width("🧑‍🧒‍🧒"), 2)
  assert_eq(get_string_width("👣"), 2)
  assert_eq(get_string_width("🫆"), 2)
  assert_eq(get_string_width("🏼"), 2)
  assert_eq(get_string_width("🏽"), 2)
  assert_eq(get_string_width("🏾"), 2)
  assert_eq(get_string_width("🏿"), 2)
  assert_eq(get_string_width("🦰"), 2)
  assert_eq(get_string_width("🦱"), 2)
  assert_eq(get_string_width("🦳"), 2)
  assert_eq(get_string_width("🦲"), 2)
  assert_eq(get_string_width("🐵"), 2)
  assert_eq(get_string_width("🐒"), 2)
  assert_eq(get_string_width("🦍"), 2)
  assert_eq(get_string_width("🦧"), 2)
  assert_eq(get_string_width("🐶"), 2)
  assert_eq(get_string_width("🐕"), 2)
  assert_eq(get_string_width("🦮"), 2)
  assert_eq(get_string_width("🐕‍🦺"), 2)
  assert_eq(get_string_width("🐩"), 2)
  assert_eq(get_string_width("🐺"), 2)
  assert_eq(get_string_width("🦊"), 2)
  assert_eq(get_string_width("🦝"), 2)
  assert_eq(get_string_width("🐱"), 2)
  assert_eq(get_string_width("🐈"), 2)
  assert_eq(get_string_width("🐈‍⬛"), 2)
  assert_eq(get_string_width("🦁"), 2)
  assert_eq(get_string_width("🐯"), 2)
  assert_eq(get_string_width("🐅"), 2)
  assert_eq(get_string_width("🐆"), 2)
  assert_eq(get_string_width("🐴"), 2)
  assert_eq(get_string_width("🫎"), 2)
  assert_eq(get_string_width("🫏"), 2)
  assert_eq(get_string_width("🐎"), 2)
  assert_eq(get_string_width("🦄"), 2)
  assert_eq(get_string_width("🦓"), 2)
  assert_eq(get_string_width("🦌"), 2)
  assert_eq(get_string_width("🦬"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 39" {
  assert_eq(get_string_width("🐮"), 2)
  assert_eq(get_string_width("🐂"), 2)
  assert_eq(get_string_width("🐃"), 2)
  assert_eq(get_string_width("🐄"), 2)
  assert_eq(get_string_width("🐷"), 2)
  assert_eq(get_string_width("🐖"), 2)
  assert_eq(get_string_width("🐗"), 2)
  assert_eq(get_string_width("🐽"), 2)
  assert_eq(get_string_width("🐏"), 2)
  assert_eq(get_string_width("🐑"), 2)
  assert_eq(get_string_width("🐐"), 2)
  assert_eq(get_string_width("🐪"), 2)
  assert_eq(get_string_width("🐫"), 2)
  assert_eq(get_string_width("🦙"), 2)
  assert_eq(get_string_width("🦒"), 2)
  assert_eq(get_string_width("🐘"), 2)
  assert_eq(get_string_width("🦣"), 2)
  assert_eq(get_string_width("🦏"), 2)
  assert_eq(get_string_width("🦛"), 2)
  assert_eq(get_string_width("🐭"), 2)
  assert_eq(get_string_width("🐁"), 2)
  assert_eq(get_string_width("🐀"), 2)
  assert_eq(get_string_width("🐹"), 2)
  assert_eq(get_string_width("🐰"), 2)
  assert_eq(get_string_width("🐇"), 2)
  assert_eq(get_string_width("🐿️"), 2)
  assert_eq(get_string_width("🐿"), 1)
  assert_eq(get_string_width("🦫"), 2)
  assert_eq(get_string_width("🦔"), 2)
  assert_eq(get_string_width("🦇"), 2)
  assert_eq(get_string_width("🐻"), 2)
  assert_eq(get_string_width("🐻‍❄️"), 2)
  assert_eq(get_string_width("🐻‍❄"), 3)
  assert_eq(get_string_width("🐨"), 2)
  assert_eq(get_string_width("🐼"), 2)
  assert_eq(get_string_width("🦥"), 2)
  assert_eq(get_string_width("🦦"), 2)
  assert_eq(get_string_width("🦨"), 2)
  assert_eq(get_string_width("🦘"), 2)
  assert_eq(get_string_width("🦡"), 2)
  assert_eq(get_string_width("🐾"), 2)
  assert_eq(get_string_width("🦃"), 2)
  assert_eq(get_string_width("🐔"), 2)
  assert_eq(get_string_width("🐓"), 2)
  assert_eq(get_string_width("🐣"), 2)
  assert_eq(get_string_width("🐤"), 2)
  assert_eq(get_string_width("🐥"), 2)
  assert_eq(get_string_width("🐦"), 2)
  assert_eq(get_string_width("🐧"), 2)
  assert_eq(get_string_width("🕊️"), 2)
  assert_eq(get_string_width("🕊"), 1)
  assert_eq(get_string_width("🦅"), 2)
  assert_eq(get_string_width("🦆"), 2)
  assert_eq(get_string_width("🦢"), 2)
  assert_eq(get_string_width("🦉"), 2)
  assert_eq(get_string_width("🦤"), 2)
  assert_eq(get_string_width("🪶"), 2)
  assert_eq(get_string_width("🦩"), 2)
  assert_eq(get_string_width("🦚"), 2)
  assert_eq(get_string_width("🦜"), 2)
  assert_eq(get_string_width("🪽"), 2)
  assert_eq(get_string_width("🐦‍⬛"), 2)
  assert_eq(get_string_width("🪿"), 2)
  assert_eq(get_string_width("🐦‍🔥"), 2)
  assert_eq(get_string_width("🐸"), 2)
  assert_eq(get_string_width("🐊"), 2)
  assert_eq(get_string_width("🐢"), 2)
  assert_eq(get_string_width("🦎"), 2)
  assert_eq(get_string_width("🐍"), 2)
  assert_eq(get_string_width("🐲"), 2)
  assert_eq(get_string_width("🐉"), 2)
  assert_eq(get_string_width("🦕"), 2)
  assert_eq(get_string_width("🦖"), 2)
  assert_eq(get_string_width("🐳"), 2)
  assert_eq(get_string_width("🐋"), 2)
  assert_eq(get_string_width("🐬"), 2)
  assert_eq(get_string_width("🫍"), 2)
  assert_eq(get_string_width("🦭"), 2)
  assert_eq(get_string_width("🐟"), 2)
  assert_eq(get_string_width("🐠"), 2)
  assert_eq(get_string_width("🐡"), 2)
  assert_eq(get_string_width("🦈"), 2)
  assert_eq(get_string_width("🐙"), 2)
  assert_eq(get_string_width("🐚"), 2)
  assert_eq(get_string_width("🪸"), 2)
  assert_eq(get_string_width("🪼"), 2)
  assert_eq(get_string_width("🦀"), 2)
  assert_eq(get_string_width("🦞"), 2)
  assert_eq(get_string_width("🦐"), 2)
  assert_eq(get_string_width("🦑"), 2)
  assert_eq(get_string_width("🦪"), 2)
  assert_eq(get_string_width("🐌"), 2)
  assert_eq(get_string_width("🦋"), 2)
  assert_eq(get_string_width("🐛"), 2)
  assert_eq(get_string_width("🐜"), 2)
  assert_eq(get_string_width("🐝"), 2)
  assert_eq(get_string_width("🪲"), 2)
  assert_eq(get_string_width("🐞"), 2)
  assert_eq(get_string_width("🦗"), 2)
  assert_eq(get_string_width("🪳"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 40" {
  assert_eq(get_string_width("🕷️"), 2)
  assert_eq(get_string_width("🕷"), 1)
  assert_eq(get_string_width("🕸️"), 2)
  assert_eq(get_string_width("🕸"), 1)
  assert_eq(get_string_width("🦂"), 2)
  assert_eq(get_string_width("🦟"), 2)
  assert_eq(get_string_width("🪰"), 2)
  assert_eq(get_string_width("🪱"), 2)
  assert_eq(get_string_width("🦠"), 2)
  assert_eq(get_string_width("💐"), 2)
  assert_eq(get_string_width("🌸"), 2)
  assert_eq(get_string_width("💮"), 2)
  assert_eq(get_string_width("🪷"), 2)
  assert_eq(get_string_width("🏵️"), 2)
  assert_eq(get_string_width("🏵"), 1)
  assert_eq(get_string_width("🌹"), 2)
  assert_eq(get_string_width("🥀"), 2)
  assert_eq(get_string_width("🌺"), 2)
  assert_eq(get_string_width("🌻"), 2)
  assert_eq(get_string_width("🌼"), 2)
  assert_eq(get_string_width("🌷"), 2)
  assert_eq(get_string_width("🪻"), 2)
  assert_eq(get_string_width("🌱"), 2)
  assert_eq(get_string_width("🪴"), 2)
  assert_eq(get_string_width("🌲"), 2)
  assert_eq(get_string_width("🌳"), 2)
  assert_eq(get_string_width("🌴"), 2)
  assert_eq(get_string_width("🌵"), 2)
  assert_eq(get_string_width("🌾"), 2)
  assert_eq(get_string_width("🌿"), 2)
  assert_eq(get_string_width("☘️"), 2)
  assert_eq(get_string_width("☘"), 1)
  assert_eq(get_string_width("🍀"), 2)
  assert_eq(get_string_width("🍁"), 2)
  assert_eq(get_string_width("🍂"), 2)
  assert_eq(get_string_width("🍃"), 2)
  assert_eq(get_string_width("🪹"), 2)
  assert_eq(get_string_width("🪺"), 2)
  assert_eq(get_string_width("🍄"), 2)
  assert_eq(get_string_width("🪾"), 2)
  assert_eq(get_string_width("🍇"), 2)
  assert_eq(get_string_width("🍈"), 2)
  assert_eq(get_string_width("🍉"), 2)
  assert_eq(get_string_width("🍊"), 2)
  assert_eq(get_string_width("🍋"), 2)
  assert_eq(get_string_width("🍋‍🟩"), 2)
  assert_eq(get_string_width("🍌"), 2)
  assert_eq(get_string_width("🍍"), 2)
  assert_eq(get_string_width("🥭"), 2)
  assert_eq(get_string_width("🍎"), 2)
  assert_eq(get_string_width("🍏"), 2)
  assert_eq(get_string_width("🍐"), 2)
  assert_eq(get_string_width("🍑"), 2)
  assert_eq(get_string_width("🍒"), 2)
  assert_eq(get_string_width("🍓"), 2)
  assert_eq(get_string_width("🫐"), 2)
  assert_eq(get_string_width("🥝"), 2)
  assert_eq(get_string_width("🍅"), 2)
  assert_eq(get_string_width("🫒"), 2)
  assert_eq(get_string_width("🥥"), 2)
  assert_eq(get_string_width("🥑"), 2)
  assert_eq(get_string_width("🍆"), 2)
  assert_eq(get_string_width("🥔"), 2)
  assert_eq(get_string_width("🥕"), 2)
  assert_eq(get_string_width("🌽"), 2)
  assert_eq(get_string_width("🌶️"), 2)
  assert_eq(get_string_width("🌶"), 1)
  assert_eq(get_string_width("🫑"), 2)
  assert_eq(get_string_width("🥒"), 2)
  assert_eq(get_string_width("🥬"), 2)
  assert_eq(get_string_width("🥦"), 2)
  assert_eq(get_string_width("🧄"), 2)
  assert_eq(get_string_width("🧅"), 2)
  assert_eq(get_string_width("🥜"), 2)
  assert_eq(get_string_width("🫘"), 2)
  assert_eq(get_string_width("🌰"), 2)
  assert_eq(get_string_width("🫚"), 2)
  assert_eq(get_string_width("🫛"), 2)
  assert_eq(get_string_width("🍄‍🟫"), 2)
  assert_eq(get_string_width("🫜"), 2)
  assert_eq(get_string_width("🍞"), 2)
  assert_eq(get_string_width("🥐"), 2)
  assert_eq(get_string_width("🥖"), 2)
  assert_eq(get_string_width("🫓"), 2)
  assert_eq(get_string_width("🥨"), 2)
  assert_eq(get_string_width("🥯"), 2)
  assert_eq(get_string_width("🥞"), 2)
  assert_eq(get_string_width("🧇"), 2)
  assert_eq(get_string_width("🧀"), 2)
  assert_eq(get_string_width("🍖"), 2)
  assert_eq(get_string_width("🍗"), 2)
  assert_eq(get_string_width("🥩"), 2)
  assert_eq(get_string_width("🥓"), 2)
  assert_eq(get_string_width("🍔"), 2)
  assert_eq(get_string_width("🍟"), 2)
  assert_eq(get_string_width("🍕"), 2)
  assert_eq(get_string_width("🌭"), 2)
  assert_eq(get_string_width("🥪"), 2)
  assert_eq(get_string_width("🌮"), 2)
  assert_eq(get_string_width("🌯"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 41" {
  assert_eq(get_string_width("🫔"), 2)
  assert_eq(get_string_width("🥙"), 2)
  assert_eq(get_string_width("🧆"), 2)
  assert_eq(get_string_width("🥚"), 2)
  assert_eq(get_string_width("🍳"), 2)
  assert_eq(get_string_width("🥘"), 2)
  assert_eq(get_string_width("🍲"), 2)
  assert_eq(get_string_width("🫕"), 2)
  assert_eq(get_string_width("🥣"), 2)
  assert_eq(get_string_width("🥗"), 2)
  assert_eq(get_string_width("🍿"), 2)
  assert_eq(get_string_width("🧈"), 2)
  assert_eq(get_string_width("🧂"), 2)
  assert_eq(get_string_width("🥫"), 2)
  assert_eq(get_string_width("🍱"), 2)
  assert_eq(get_string_width("🍘"), 2)
  assert_eq(get_string_width("🍙"), 2)
  assert_eq(get_string_width("🍚"), 2)
  assert_eq(get_string_width("🍛"), 2)
  assert_eq(get_string_width("🍜"), 2)
  assert_eq(get_string_width("🍝"), 2)
  assert_eq(get_string_width("🍠"), 2)
  assert_eq(get_string_width("🍢"), 2)
  assert_eq(get_string_width("🍣"), 2)
  assert_eq(get_string_width("🍤"), 2)
  assert_eq(get_string_width("🍥"), 2)
  assert_eq(get_string_width("🥮"), 2)
  assert_eq(get_string_width("🍡"), 2)
  assert_eq(get_string_width("🥟"), 2)
  assert_eq(get_string_width("🥠"), 2)
  assert_eq(get_string_width("🥡"), 2)
  assert_eq(get_string_width("🍦"), 2)
  assert_eq(get_string_width("🍧"), 2)
  assert_eq(get_string_width("🍨"), 2)
  assert_eq(get_string_width("🍩"), 2)
  assert_eq(get_string_width("🍪"), 2)
  assert_eq(get_string_width("🎂"), 2)
  assert_eq(get_string_width("🍰"), 2)
  assert_eq(get_string_width("🧁"), 2)
  assert_eq(get_string_width("🥧"), 2)
  assert_eq(get_string_width("🍫"), 2)
  assert_eq(get_string_width("🍬"), 2)
  assert_eq(get_string_width("🍭"), 2)
  assert_eq(get_string_width("🍮"), 2)
  assert_eq(get_string_width("🍯"), 2)
  assert_eq(get_string_width("🍼"), 2)
  assert_eq(get_string_width("🥛"), 2)
  assert_eq(get_string_width("☕"), 2)
  assert_eq(get_string_width("🫖"), 2)
  assert_eq(get_string_width("🍵"), 2)
  assert_eq(get_string_width("🍶"), 2)
  assert_eq(get_string_width("🍾"), 2)
  assert_eq(get_string_width("🍷"), 2)
  assert_eq(get_string_width("🍸"), 2)
  assert_eq(get_string_width("🍹"), 2)
  assert_eq(get_string_width("🍺"), 2)
  assert_eq(get_string_width("🍻"), 2)
  assert_eq(get_string_width("🥂"), 2)
  assert_eq(get_string_width("🥃"), 2)
  assert_eq(get_string_width("🫗"), 2)
  assert_eq(get_string_width("🥤"), 2)
  assert_eq(get_string_width("🧋"), 2)
  assert_eq(get_string_width("🧃"), 2)
  assert_eq(get_string_width("🧉"), 2)
  assert_eq(get_string_width("🧊"), 2)
  assert_eq(get_string_width("🥢"), 2)
  assert_eq(get_string_width("🍽️"), 2)
  assert_eq(get_string_width("🍽"), 1)
  assert_eq(get_string_width("🍴"), 2)
  assert_eq(get_string_width("🥄"), 2)
  assert_eq(get_string_width("🔪"), 2)
  assert_eq(get_string_width("🫙"), 2)
  assert_eq(get_string_width("🏺"), 2)
  assert_eq(get_string_width("🌍"), 2)
  assert_eq(get_string_width("🌎"), 2)
  assert_eq(get_string_width("🌏"), 2)
  assert_eq(get_string_width("🌐"), 2)
  assert_eq(get_string_width("🗺️"), 2)
  assert_eq(get_string_width("🗺"), 1)
  assert_eq(get_string_width("🗾"), 2)
  assert_eq(get_string_width("🧭"), 2)
  assert_eq(get_string_width("🏔️"), 2)
  assert_eq(get_string_width("🏔"), 1)
  assert_eq(get_string_width("⛰️"), 2)
  assert_eq(get_string_width("⛰"), 1)
  assert_eq(get_string_width("🛘"), 2)
  assert_eq(get_string_width("🌋"), 2)
  assert_eq(get_string_width("🗻"), 2)
  assert_eq(get_string_width("🏕️"), 2)
  assert_eq(get_string_width("🏕"), 1)
  assert_eq(get_string_width("🏖️"), 2)
  assert_eq(get_string_width("🏖"), 1)
  assert_eq(get_string_width("🏜️"), 2)
  assert_eq(get_string_width("🏜"), 1)
  assert_eq(get_string_width("🏝️"), 2)
  assert_eq(get_string_width("🏝"), 1)
  assert_eq(get_string_width("🏞️"), 2)
  assert_eq(get_string_width("🏞"), 1)
  assert_eq(get_string_width("🏟️"), 2)
  assert_eq(get_string_width("🏟"), 1)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 42" {
  assert_eq(get_string_width("🏛️"), 2)
  assert_eq(get_string_width("🏛"), 1)
  assert_eq(get_string_width("🏗️"), 2)
  assert_eq(get_string_width("🏗"), 1)
  assert_eq(get_string_width("🧱"), 2)
  assert_eq(get_string_width("🪨"), 2)
  assert_eq(get_string_width("🪵"), 2)
  assert_eq(get_string_width("🛖"), 2)
  assert_eq(get_string_width("🏘️"), 2)
  assert_eq(get_string_width("🏘"), 1)
  assert_eq(get_string_width("🏚️"), 2)
  assert_eq(get_string_width("🏚"), 1)
  assert_eq(get_string_width("🏠"), 2)
  assert_eq(get_string_width("🏡"), 2)
  assert_eq(get_string_width("🏢"), 2)
  assert_eq(get_string_width("🏣"), 2)
  assert_eq(get_string_width("🏤"), 2)
  assert_eq(get_string_width("🏥"), 2)
  assert_eq(get_string_width("🏦"), 2)
  assert_eq(get_string_width("🏨"), 2)
  assert_eq(get_string_width("🏩"), 2)
  assert_eq(get_string_width("🏪"), 2)
  assert_eq(get_string_width("🏫"), 2)
  assert_eq(get_string_width("🏬"), 2)
  assert_eq(get_string_width("🏭"), 2)
  assert_eq(get_string_width("🏯"), 2)
  assert_eq(get_string_width("🏰"), 2)
  assert_eq(get_string_width("💒"), 2)
  assert_eq(get_string_width("🗼"), 2)
  assert_eq(get_string_width("🗽"), 2)
  assert_eq(get_string_width("⛪"), 2)
  assert_eq(get_string_width("🕌"), 2)
  assert_eq(get_string_width("🛕"), 2)
  assert_eq(get_string_width("🕍"), 2)
  assert_eq(get_string_width("⛩️"), 2)
  assert_eq(get_string_width("⛩"), 1)
  assert_eq(get_string_width("🕋"), 2)
  assert_eq(get_string_width("⛲"), 2)
  assert_eq(get_string_width("⛺"), 2)
  assert_eq(get_string_width("🌁"), 2)
  assert_eq(get_string_width("🌃"), 2)
  assert_eq(get_string_width("🏙️"), 2)
  assert_eq(get_string_width("🏙"), 1)
  assert_eq(get_string_width("🌄"), 2)
  assert_eq(get_string_width("🌅"), 2)
  assert_eq(get_string_width("🌆"), 2)
  assert_eq(get_string_width("🌇"), 2)
  assert_eq(get_string_width("🌉"), 2)
  assert_eq(get_string_width("♨️"), 2)
  assert_eq(get_string_width("♨"), 1)
  assert_eq(get_string_width("🎠"), 2)
  assert_eq(get_string_width("🛝"), 2)
  assert_eq(get_string_width("🎡"), 2)
  assert_eq(get_string_width("🎢"), 2)
  assert_eq(get_string_width("💈"), 2)
  assert_eq(get_string_width("🎪"), 2)
  assert_eq(get_string_width("🚂"), 2)
  assert_eq(get_string_width("🚃"), 2)
  assert_eq(get_string_width("🚄"), 2)
  assert_eq(get_string_width("🚅"), 2)
  assert_eq(get_string_width("🚆"), 2)
  assert_eq(get_string_width("🚇"), 2)
  assert_eq(get_string_width("🚈"), 2)
  assert_eq(get_string_width("🚉"), 2)
  assert_eq(get_string_width("🚊"), 2)
  assert_eq(get_string_width("🚝"), 2)
  assert_eq(get_string_width("🚞"), 2)
  assert_eq(get_string_width("🚋"), 2)
  assert_eq(get_string_width("🚌"), 2)
  assert_eq(get_string_width("🚍"), 2)
  assert_eq(get_string_width("🚎"), 2)
  assert_eq(get_string_width("🚐"), 2)
  assert_eq(get_string_width("🚑"), 2)
  assert_eq(get_string_width("🚒"), 2)
  assert_eq(get_string_width("🚓"), 2)
  assert_eq(get_string_width("🚔"), 2)
  assert_eq(get_string_width("🚕"), 2)
  assert_eq(get_string_width("🚖"), 2)
  assert_eq(get_string_width("🚗"), 2)
  assert_eq(get_string_width("🚘"), 2)
  assert_eq(get_string_width("🚙"), 2)
  assert_eq(get_string_width("🛻"), 2)
  assert_eq(get_string_width("🚚"), 2)
  assert_eq(get_string_width("🚛"), 2)
  assert_eq(get_string_width("🚜"), 2)
  assert_eq(get_string_width("🏎️"), 2)
  assert_eq(get_string_width("🏎"), 1)
  assert_eq(get_string_width("🏍️"), 2)
  assert_eq(get_string_width("🏍"), 1)
  assert_eq(get_string_width("🛵"), 2)
  assert_eq(get_string_width("🦽"), 2)
  assert_eq(get_string_width("🦼"), 2)
  assert_eq(get_string_width("🛺"), 2)
  assert_eq(get_string_width("🚲"), 2)
  assert_eq(get_string_width("🛴"), 2)
  assert_eq(get_string_width("🛹"), 2)
  assert_eq(get_string_width("🛼"), 2)
  assert_eq(get_string_width("🚏"), 2)
  assert_eq(get_string_width("🛣️"), 2)
  assert_eq(get_string_width("🛣"), 1)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 43" {
  assert_eq(get_string_width("🛤️"), 2)
  assert_eq(get_string_width("🛤"), 1)
  assert_eq(get_string_width("🛢️"), 2)
  assert_eq(get_string_width("🛢"), 1)
  assert_eq(get_string_width("⛽"), 2)
  assert_eq(get_string_width("🛞"), 2)
  assert_eq(get_string_width("🚨"), 2)
  assert_eq(get_string_width("🚥"), 2)
  assert_eq(get_string_width("🚦"), 2)
  assert_eq(get_string_width("🛑"), 2)
  assert_eq(get_string_width("🚧"), 2)
  assert_eq(get_string_width("⚓"), 2)
  assert_eq(get_string_width("🛟"), 2)
  assert_eq(get_string_width("⛵"), 2)
  assert_eq(get_string_width("🛶"), 2)
  assert_eq(get_string_width("🚤"), 2)
  assert_eq(get_string_width("🛳"), 1)
  assert_eq(get_string_width("⛴️"), 2)
  assert_eq(get_string_width("⛴"), 1)
  assert_eq(get_string_width("🛥️"), 2)
  assert_eq(get_string_width("🛥"), 1)
  assert_eq(get_string_width("🚢"), 2)
  assert_eq(get_string_width("✈️"), 2)
  assert_eq(get_string_width("✈"), 1)
  assert_eq(get_string_width("🛩️"), 2)
  assert_eq(get_string_width("🛩"), 1)
  assert_eq(get_string_width("🛫"), 2)
  assert_eq(get_string_width("🛬"), 2)
  assert_eq(get_string_width("🪂"), 2)
  assert_eq(get_string_width("💺"), 2)
  assert_eq(get_string_width("🚁"), 2)
  assert_eq(get_string_width("🚟"), 2)
  assert_eq(get_string_width("🚠"), 2)
  assert_eq(get_string_width("🚡"), 2)
  assert_eq(get_string_width("🛰️"), 2)
  assert_eq(get_string_width("🛰"), 1)
  assert_eq(get_string_width("🚀"), 2)
  assert_eq(get_string_width("🛸"), 2)
  assert_eq(get_string_width("🛎️"), 2)
  assert_eq(get_string_width("🛎"), 1)
  assert_eq(get_string_width("🧳"), 2)
  assert_eq(get_string_width("⌛"), 2)
  assert_eq(get_string_width("⏳"), 2)
  assert_eq(get_string_width("⌚"), 2)
  assert_eq(get_string_width("⏰"), 2)
  assert_eq(get_string_width("⏱️"), 2)
  assert_eq(get_string_width("⏱"), 1)
  assert_eq(get_string_width("⏲️"), 2)
  assert_eq(get_string_width("⏲"), 1)
  assert_eq(get_string_width("🕰️"), 2)
  assert_eq(get_string_width("🕰"), 1)
  assert_eq(get_string_width("🕛"), 2)
  assert_eq(get_string_width("🕧"), 2)
  assert_eq(get_string_width("🕐"), 2)
  assert_eq(get_string_width("🕜"), 2)
  assert_eq(get_string_width("🕑"), 2)
  assert_eq(get_string_width("🕝"), 2)
  assert_eq(get_string_width("🕒"), 2)
  assert_eq(get_string_width("🕞"), 2)
  assert_eq(get_string_width("🕓"), 2)
  assert_eq(get_string_width("🕟"), 2)
  assert_eq(get_string_width("🕔"), 2)
  assert_eq(get_string_width("🕠"), 2)
  assert_eq(get_string_width("🕕"), 2)
  assert_eq(get_string_width("🕡"), 2)
  assert_eq(get_string_width("🕖"), 2)
  assert_eq(get_string_width("🕢"), 2)
  assert_eq(get_string_width("🕗"), 2)
  assert_eq(get_string_width("🕣"), 2)
  assert_eq(get_string_width("🕘"), 2)
  assert_eq(get_string_width("🕤"), 2)
  assert_eq(get_string_width("🕙"), 2)
  assert_eq(get_string_width("🕥"), 2)
  assert_eq(get_string_width("🕚"), 2)
  assert_eq(get_string_width("🕦"), 2)
  assert_eq(get_string_width("🌑"), 2)
  assert_eq(get_string_width("🌒"), 2)
  assert_eq(get_string_width("🌓"), 2)
  assert_eq(get_string_width("🌔"), 2)
  assert_eq(get_string_width("🌕"), 2)
  assert_eq(get_string_width("🌖"), 2)
  assert_eq(get_string_width("🌗"), 2)
  assert_eq(get_string_width("🌘"), 2)
  assert_eq(get_string_width("🌙"), 2)
  assert_eq(get_string_width("🌚"), 2)
  assert_eq(get_string_width("🌛"), 2)
  assert_eq(get_string_width("🌜"), 2)
  assert_eq(get_string_width("🌡️"), 2)
  assert_eq(get_string_width("🌡"), 1)
  assert_eq(get_string_width("☀️"), 2)
  assert_eq(get_string_width("☀"), 1)
  assert_eq(get_string_width("🌝"), 2)
  assert_eq(get_string_width("🌞"), 2)
  assert_eq(get_string_width("🪐"), 2)
  assert_eq(get_string_width("⭐"), 2)
  assert_eq(get_string_width("🌟"), 2)
  assert_eq(get_string_width("🌠"), 2)
  assert_eq(get_string_width("🌌"), 2)
  assert_eq(get_string_width("☁️"), 2)
  assert_eq(get_string_width("☁"), 1)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 44" {
  assert_eq(get_string_width("⛅"), 2)
  assert_eq(get_string_width("⛈️"), 2)
  assert_eq(get_string_width("⛈"), 1)
  assert_eq(get_string_width("🌤️"), 2)
  assert_eq(get_string_width("🌤"), 1)
  assert_eq(get_string_width("🌥️"), 2)
  assert_eq(get_string_width("🌥"), 1)
  assert_eq(get_string_width("🌦️"), 2)
  assert_eq(get_string_width("🌦"), 1)
  assert_eq(get_string_width("🌧️"), 2)
  assert_eq(get_string_width("🌧"), 1)
  assert_eq(get_string_width("🌨️"), 2)
  assert_eq(get_string_width("🌨"), 1)
  assert_eq(get_string_width("🌩️"), 2)
  assert_eq(get_string_width("🌩"), 1)
  assert_eq(get_string_width("🌪️"), 2)
  assert_eq(get_string_width("🌪"), 1)
  assert_eq(get_string_width("🌫️"), 2)
  assert_eq(get_string_width("🌫"), 1)
  assert_eq(get_string_width("🌬️"), 2)
  assert_eq(get_string_width("🌬"), 1)
  assert_eq(get_string_width("🌀"), 2)
  assert_eq(get_string_width("🌈"), 2)
  assert_eq(get_string_width("🌂"), 2)
  assert_eq(get_string_width("☂️"), 2)
  assert_eq(get_string_width("☂"), 1)
  assert_eq(get_string_width("☔"), 2)
  assert_eq(get_string_width("⛱️"), 2)
  assert_eq(get_string_width("⛱"), 1)
  assert_eq(get_string_width("⚡"), 2)
  assert_eq(get_string_width("❄️"), 2)
  assert_eq(get_string_width("❄"), 1)
  assert_eq(get_string_width("☃️"), 2)
  assert_eq(get_string_width("☃"), 1)
  assert_eq(get_string_width("⛄"), 2)
  assert_eq(get_string_width("☄️"), 2)
  assert_eq(get_string_width("☄"), 1)
  assert_eq(get_string_width("🔥"), 2)
  assert_eq(get_string_width("💧"), 2)
  assert_eq(get_string_width("🌊"), 2)
  assert_eq(get_string_width("🎃"), 2)
  assert_eq(get_string_width("🎄"), 2)
  assert_eq(get_string_width("🎆"), 2)
  assert_eq(get_string_width("🎇"), 2)
  assert_eq(get_string_width("🧨"), 2)
  assert_eq(get_string_width("✨"), 2)
  assert_eq(get_string_width("🎈"), 2)
  assert_eq(get_string_width("🎉"), 2)
  assert_eq(get_string_width("🎊"), 2)
  assert_eq(get_string_width("🎋"), 2)
  assert_eq(get_string_width("🎍"), 2)
  assert_eq(get_string_width("🎎"), 2)
  assert_eq(get_string_width("🎏"), 2)
  assert_eq(get_string_width("🎐"), 2)
  assert_eq(get_string_width("🎑"), 2)
  assert_eq(get_string_width("🧧"), 2)
  assert_eq(get_string_width("🎀"), 2)
  assert_eq(get_string_width("🎁"), 2)
  assert_eq(get_string_width("🎗️"), 2)
  assert_eq(get_string_width("🎗"), 1)
  assert_eq(get_string_width("🎟️"), 2)
  assert_eq(get_string_width("🎟"), 1)
  assert_eq(get_string_width("🎫"), 2)
  assert_eq(get_string_width("🎖️"), 2)
  assert_eq(get_string_width("🎖"), 1)
  assert_eq(get_string_width("🏆"), 2)
  assert_eq(get_string_width("🏅"), 2)
  assert_eq(get_string_width("🥇"), 2)
  assert_eq(get_string_width("🥈"), 2)
  assert_eq(get_string_width("🥉"), 2)
  assert_eq(get_string_width("⚽"), 2)
  assert_eq(get_string_width("⚾"), 2)
  assert_eq(get_string_width("🥎"), 2)
  assert_eq(get_string_width("🏀"), 2)
  assert_eq(get_string_width("🏐"), 2)
  assert_eq(get_string_width("🏈"), 2)
  assert_eq(get_string_width("🏉"), 2)
  assert_eq(get_string_width("🎾"), 2)
  assert_eq(get_string_width("🥏"), 2)
  assert_eq(get_string_width("🎳"), 2)
  assert_eq(get_string_width("🏏"), 2)
  assert_eq(get_string_width("🏑"), 2)
  assert_eq(get_string_width("🏒"), 2)
  assert_eq(get_string_width("🥍"), 2)
  assert_eq(get_string_width("🏓"), 2)
  assert_eq(get_string_width("🏸"), 2)
  assert_eq(get_string_width("🥊"), 2)
  assert_eq(get_string_width("🥋"), 2)
  assert_eq(get_string_width("🥅"), 2)
  assert_eq(get_string_width("⛳"), 2)
  assert_eq(get_string_width("⛸️"), 2)
  assert_eq(get_string_width("⛸"), 1)
  assert_eq(get_string_width("🎣"), 2)
  assert_eq(get_string_width("🤿"), 2)
  assert_eq(get_string_width("🎽"), 2)
  assert_eq(get_string_width("🎿"), 2)
  assert_eq(get_string_width("🛷"), 2)
  assert_eq(get_string_width("🥌"), 2)
  assert_eq(get_string_width("🎯"), 2)
  assert_eq(get_string_width("🪀"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 45" {
  assert_eq(get_string_width("🪁"), 2)
  assert_eq(get_string_width("🔫"), 2)
  assert_eq(get_string_width("🎱"), 2)
  assert_eq(get_string_width("🔮"), 2)
  assert_eq(get_string_width("🪄"), 2)
  assert_eq(get_string_width("🎮"), 2)
  assert_eq(get_string_width("🕹️"), 2)
  assert_eq(get_string_width("🕹"), 1)
  assert_eq(get_string_width("🎰"), 2)
  assert_eq(get_string_width("🎲"), 2)
  assert_eq(get_string_width("🧩"), 2)
  assert_eq(get_string_width("🧸"), 2)
  assert_eq(get_string_width("🪅"), 2)
  assert_eq(get_string_width("🪩"), 2)
  assert_eq(get_string_width("🪆"), 2)
  assert_eq(get_string_width("♠️"), 2)
  assert_eq(get_string_width("♠"), 1)
  assert_eq(get_string_width("♥️"), 2)
  assert_eq(get_string_width("♥"), 1)
  assert_eq(get_string_width("♦️"), 2)
  assert_eq(get_string_width("♦"), 1)
  assert_eq(get_string_width("♣️"), 2)
  assert_eq(get_string_width("♣"), 1)
  assert_eq(get_string_width("♟️"), 2)
  assert_eq(get_string_width("♟"), 1)
  assert_eq(get_string_width("🃏"), 2)
  assert_eq(get_string_width("🀄"), 2)
  assert_eq(get_string_width("🎴"), 2)
  assert_eq(get_string_width("🎭"), 2)
  assert_eq(get_string_width("🖼️"), 2)
  assert_eq(get_string_width("🖼"), 1)
  assert_eq(get_string_width("🎨"), 2)
  assert_eq(get_string_width("🧵"), 2)
  assert_eq(get_string_width("🪡"), 2)
  assert_eq(get_string_width("🧶"), 2)
  assert_eq(get_string_width("🪢"), 2)
  assert_eq(get_string_width("👓"), 2)
  assert_eq(get_string_width("🕶️"), 2)
  assert_eq(get_string_width("🕶"), 1)
  assert_eq(get_string_width("🥽"), 2)
  assert_eq(get_string_width("🥼"), 2)
  assert_eq(get_string_width("🦺"), 2)
  assert_eq(get_string_width("👔"), 2)
  assert_eq(get_string_width("👕"), 2)
  assert_eq(get_string_width("👖"), 2)
  assert_eq(get_string_width("🧣"), 2)
  assert_eq(get_string_width("🧤"), 2)
  assert_eq(get_string_width("🧥"), 2)
  assert_eq(get_string_width("🧦"), 2)
  assert_eq(get_string_width("👗"), 2)
  assert_eq(get_string_width("👘"), 2)
  assert_eq(get_string_width("🥻"), 2)
  assert_eq(get_string_width("🩱"), 2)
  assert_eq(get_string_width("🩲"), 2)
  assert_eq(get_string_width("🩳"), 2)
  assert_eq(get_string_width("👙"), 2)
  assert_eq(get_string_width("👚"), 2)
  assert_eq(get_string_width("🪭"), 2)
  assert_eq(get_string_width("👛"), 2)
  assert_eq(get_string_width("👜"), 2)
  assert_eq(get_string_width("👝"), 2)
  assert_eq(get_string_width("🛍️"), 2)
  assert_eq(get_string_width("🛍"), 1)
  assert_eq(get_string_width("🎒"), 2)
  assert_eq(get_string_width("🩴"), 2)
  assert_eq(get_string_width("👞"), 2)
  assert_eq(get_string_width("👟"), 2)
  assert_eq(get_string_width("🥾"), 2)
  assert_eq(get_string_width("🥿"), 2)
  assert_eq(get_string_width("👠"), 2)
  assert_eq(get_string_width("👡"), 2)
  assert_eq(get_string_width("🩰"), 2)
  assert_eq(get_string_width("👢"), 2)
  assert_eq(get_string_width("🪮"), 2)
  assert_eq(get_string_width("👑"), 2)
  assert_eq(get_string_width("👒"), 2)
  assert_eq(get_string_width("🎩"), 2)
  assert_eq(get_string_width("🎓"), 2)
  assert_eq(get_string_width("🧢"), 2)
  assert_eq(get_string_width("🪖"), 2)
  assert_eq(get_string_width("⛑️"), 2)
  assert_eq(get_string_width("⛑"), 1)
  assert_eq(get_string_width("📿"), 2)
  assert_eq(get_string_width("💄"), 2)
  assert_eq(get_string_width("💍"), 2)
  assert_eq(get_string_width("💎"), 2)
  assert_eq(get_string_width("🔇"), 2)
  assert_eq(get_string_width("🔈"), 2)
  assert_eq(get_string_width("🔉"), 2)
  assert_eq(get_string_width("🔊"), 2)
  assert_eq(get_string_width("📢"), 2)
  assert_eq(get_string_width("📣"), 2)
  assert_eq(get_string_width("📯"), 2)
  assert_eq(get_string_width("🔔"), 2)
  assert_eq(get_string_width("🔕"), 2)
  assert_eq(get_string_width("🎼"), 2)
  assert_eq(get_string_width("🎵"), 2)
  assert_eq(get_string_width("🎶"), 2)
  assert_eq(get_string_width("🎙️"), 2)
  assert_eq(get_string_width("🎙"), 1)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 46" {
  assert_eq(get_string_width("🎚️"), 2)
  assert_eq(get_string_width("🎚"), 1)
  assert_eq(get_string_width("🎛️"), 2)
  assert_eq(get_string_width("🎛"), 1)
  assert_eq(get_string_width("🎤"), 2)
  assert_eq(get_string_width("🎧"), 2)
  assert_eq(get_string_width("📻"), 2)
  assert_eq(get_string_width("🎷"), 2)
  assert_eq(get_string_width("🎺"), 2)
  assert_eq(get_string_width("🪊"), 2)
  assert_eq(get_string_width("🪗"), 2)
  assert_eq(get_string_width("🎸"), 2)
  assert_eq(get_string_width("🎹"), 2)
  assert_eq(get_string_width("🎻"), 2)
  assert_eq(get_string_width("🪕"), 2)
  assert_eq(get_string_width("🥁"), 2)
  assert_eq(get_string_width("🪘"), 2)
  assert_eq(get_string_width("🪇"), 2)
  assert_eq(get_string_width("🪈"), 2)
  assert_eq(get_string_width("🪉"), 2)
  assert_eq(get_string_width("📱"), 2)
  assert_eq(get_string_width("📲"), 2)
  assert_eq(get_string_width("☎️"), 2)
  assert_eq(get_string_width("☎"), 1)
  assert_eq(get_string_width("📞"), 2)
  assert_eq(get_string_width("📟"), 2)
  assert_eq(get_string_width("📠"), 2)
  assert_eq(get_string_width("🔋"), 2)
  assert_eq(get_string_width("🪫"), 2)
  assert_eq(get_string_width("🔌"), 2)
  assert_eq(get_string_width("💻"), 2)
  assert_eq(get_string_width("🖥️"), 2)
  assert_eq(get_string_width("🖥"), 1)
  assert_eq(get_string_width("🖨️"), 2)
  assert_eq(get_string_width("🖨"), 1)
  assert_eq(get_string_width("⌨️"), 2)
  assert_eq(get_string_width("⌨"), 1)
  assert_eq(get_string_width("🖱️"), 2)
  assert_eq(get_string_width("🖱"), 1)
  assert_eq(get_string_width("🖲️"), 2)
  assert_eq(get_string_width("🖲"), 1)
  assert_eq(get_string_width("💽"), 2)
  assert_eq(get_string_width("💾"), 2)
  assert_eq(get_string_width("💿"), 2)
  assert_eq(get_string_width("📀"), 2)
  assert_eq(get_string_width("🧮"), 2)
  assert_eq(get_string_width("🎥"), 2)
  assert_eq(get_string_width("🎞️"), 2)
  assert_eq(get_string_width("🎞"), 1)
  assert_eq(get_string_width("📽️"), 2)
  assert_eq(get_string_width("📽"), 1)
  assert_eq(get_string_width("🎬"), 2)
  assert_eq(get_string_width("📺"), 2)
  assert_eq(get_string_width("📷"), 2)
  assert_eq(get_string_width("📸"), 2)
  assert_eq(get_string_width("📹"), 2)
  assert_eq(get_string_width("📼"), 2)
  assert_eq(get_string_width("🔍"), 2)
  assert_eq(get_string_width("🔎"), 2)
  assert_eq(get_string_width("🕯️"), 2)
  assert_eq(get_string_width("🕯"), 1)
  assert_eq(get_string_width("💡"), 2)
  assert_eq(get_string_width("🔦"), 2)
  assert_eq(get_string_width("🏮"), 2)
  assert_eq(get_string_width("🪔"), 2)
  assert_eq(get_string_width("📔"), 2)
  assert_eq(get_string_width("📕"), 2)
  assert_eq(get_string_width("📖"), 2)
  assert_eq(get_string_width("📗"), 2)
  assert_eq(get_string_width("📘"), 2)
  assert_eq(get_string_width("📙"), 2)
  assert_eq(get_string_width("📚"), 2)
  assert_eq(get_string_width("📓"), 2)
  assert_eq(get_string_width("📒"), 2)
  assert_eq(get_string_width("📃"), 2)
  assert_eq(get_string_width("📜"), 2)
  assert_eq(get_string_width("📄"), 2)
  assert_eq(get_string_width("📰"), 2)
  assert_eq(get_string_width("🗞️"), 2)
  assert_eq(get_string_width("🗞"), 1)
  assert_eq(get_string_width("📑"), 2)
  assert_eq(get_string_width("🔖"), 2)
  assert_eq(get_string_width("🏷️"), 2)
  assert_eq(get_string_width("🏷"), 1)
  assert_eq(get_string_width("🪙"), 2)
  assert_eq(get_string_width("💰"), 2)
  assert_eq(get_string_width("🪎"), 2)
  assert_eq(get_string_width("💴"), 2)
  assert_eq(get_string_width("💵"), 2)
  assert_eq(get_string_width("💶"), 2)
  assert_eq(get_string_width("💷"), 2)
  assert_eq(get_string_width("💸"), 2)
  assert_eq(get_string_width("💳"), 2)
  assert_eq(get_string_width("🧾"), 2)
  assert_eq(get_string_width("💹"), 2)
  assert_eq(get_string_width("✉️"), 2)
  assert_eq(get_string_width("✉"), 1)
  assert_eq(get_string_width("📧"), 2)
  assert_eq(get_string_width("📨"), 2)
  assert_eq(get_string_width("📩"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 47" {
  assert_eq(get_string_width("📤"), 2)
  assert_eq(get_string_width("📥"), 2)
  assert_eq(get_string_width("📦"), 2)
  assert_eq(get_string_width("📫"), 2)
  assert_eq(get_string_width("📪"), 2)
  assert_eq(get_string_width("📬"), 2)
  assert_eq(get_string_width("📭"), 2)
  assert_eq(get_string_width("📮"), 2)
  assert_eq(get_string_width("🗳️"), 2)
  assert_eq(get_string_width("🗳"), 1)
  assert_eq(get_string_width("✏️"), 2)
  assert_eq(get_string_width("✏"), 1)
  assert_eq(get_string_width("✒️"), 2)
  assert_eq(get_string_width("✒"), 1)
  assert_eq(get_string_width("🖋️"), 2)
  assert_eq(get_string_width("🖋"), 1)
  assert_eq(get_string_width("🖊️"), 2)
  assert_eq(get_string_width("🖊"), 1)
  assert_eq(get_string_width("🖌️"), 2)
  assert_eq(get_string_width("🖌"), 1)
  assert_eq(get_string_width("🖍️"), 2)
  assert_eq(get_string_width("🖍"), 1)
  assert_eq(get_string_width("📝"), 2)
  assert_eq(get_string_width("💼"), 2)
  assert_eq(get_string_width("📁"), 2)
  assert_eq(get_string_width("📂"), 2)
  assert_eq(get_string_width("🗂️"), 2)
  assert_eq(get_string_width("🗂"), 1)
  assert_eq(get_string_width("📅"), 2)
  assert_eq(get_string_width("📆"), 2)
  assert_eq(get_string_width("🗒️"), 2)
  assert_eq(get_string_width("🗒"), 1)
  assert_eq(get_string_width("🗓️"), 2)
  assert_eq(get_string_width("🗓"), 1)
  assert_eq(get_string_width("📇"), 2)
  assert_eq(get_string_width("📈"), 2)
  assert_eq(get_string_width("📉"), 2)
  assert_eq(get_string_width("📊"), 2)
  assert_eq(get_string_width("📋"), 2)
  assert_eq(get_string_width("📌"), 2)
  assert_eq(get_string_width("📍"), 2)
  assert_eq(get_string_width("📎"), 2)
  assert_eq(get_string_width("🖇️"), 2)
  assert_eq(get_string_width("🖇"), 1)
  assert_eq(get_string_width("📏"), 2)
  assert_eq(get_string_width("📐"), 2)
  assert_eq(get_string_width("✂️"), 2)
  assert_eq(get_string_width("✂"), 1)
  assert_eq(get_string_width("🗃️"), 2)
  assert_eq(get_string_width("🗃"), 1)
  assert_eq(get_string_width("🗄️"), 2)
  assert_eq(get_string_width("🗄"), 1)
  assert_eq(get_string_width("🗑️"), 2)
  assert_eq(get_string_width("🗑"), 1)
  assert_eq(get_string_width("🔒"), 2)
  assert_eq(get_string_width("🔓"), 2)
  assert_eq(get_string_width("🔏"), 2)
  assert_eq(get_string_width("🔐"), 2)
  assert_eq(get_string_width("🔑"), 2)
  assert_eq(get_string_width("🗝️"), 2)
  assert_eq(get_string_width("🗝"), 1)
  assert_eq(get_string_width("🔨"), 2)
  assert_eq(get_string_width("🪓"), 2)
  assert_eq(get_string_width("⛏️"), 2)
  assert_eq(get_string_width("⛏"), 1)
  assert_eq(get_string_width("⚒️"), 2)
  assert_eq(get_string_width("⚒"), 1)
  assert_eq(get_string_width("🛠️"), 2)
  assert_eq(get_string_width("🛠"), 1)
  assert_eq(get_string_width("🗡️"), 2)
  assert_eq(get_string_width("🗡"), 1)
  assert_eq(get_string_width("⚔️"), 2)
  assert_eq(get_string_width("⚔"), 1)
  assert_eq(get_string_width("💣"), 2)
  assert_eq(get_string_width("🪃"), 2)
  assert_eq(get_string_width("🏹"), 2)
  assert_eq(get_string_width("🛡️"), 2)
  assert_eq(get_string_width("🛡"), 1)
  assert_eq(get_string_width("🪚"), 2)
  assert_eq(get_string_width("🔧"), 2)
  assert_eq(get_string_width("🪛"), 2)
  assert_eq(get_string_width("🔩"), 2)
  assert_eq(get_string_width("⚙️"), 2)
  assert_eq(get_string_width("⚙"), 1)
  assert_eq(get_string_width("🗜️"), 2)
  assert_eq(get_string_width("🗜"), 1)
  assert_eq(get_string_width("⚖️"), 2)
  assert_eq(get_string_width("⚖"), 1)
  assert_eq(get_string_width("🦯"), 2)
  assert_eq(get_string_width("🔗"), 2)
  assert_eq(get_string_width("⛓️‍💥"), 2)
  assert_eq(get_string_width("⛓‍💥"), 3)
  assert_eq(get_string_width("⛓️"), 2)
  assert_eq(get_string_width("⛓"), 1)
  assert_eq(get_string_width("🪝"), 2)
  assert_eq(get_string_width("🧰"), 2)
  assert_eq(get_string_width("🧲"), 2)
  assert_eq(get_string_width("🪜"), 2)
  assert_eq(get_string_width("🪏"), 2)
  assert_eq(get_string_width("⚗️"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 48" {
  assert_eq(get_string_width("⚗"), 1)
  assert_eq(get_string_width("🧪"), 2)
  assert_eq(get_string_width("🧫"), 2)
  assert_eq(get_string_width("🧬"), 2)
  assert_eq(get_string_width("🔭"), 2)
  assert_eq(get_string_width("📡"), 2)
  assert_eq(get_string_width("💉"), 2)
  assert_eq(get_string_width("🩸"), 2)
  assert_eq(get_string_width("💊"), 2)
  assert_eq(get_string_width("🩹"), 2)
  assert_eq(get_string_width("🩼"), 2)
  assert_eq(get_string_width("🩺"), 2)
  assert_eq(get_string_width("🩻"), 2)
  assert_eq(get_string_width("🚪"), 2)
  assert_eq(get_string_width("🛗"), 2)
  assert_eq(get_string_width("🪞"), 2)
  assert_eq(get_string_width("🪟"), 2)
  assert_eq(get_string_width("🛏️"), 2)
  assert_eq(get_string_width("🛏"), 1)
  assert_eq(get_string_width("🛋️"), 2)
  assert_eq(get_string_width("🛋"), 1)
  assert_eq(get_string_width("🪑"), 2)
  assert_eq(get_string_width("🚽"), 2)
  assert_eq(get_string_width("🪠"), 2)
  assert_eq(get_string_width("🚿"), 2)
  assert_eq(get_string_width("🛁"), 2)
  assert_eq(get_string_width("🪤"), 2)
  assert_eq(get_string_width("🪒"), 2)
  assert_eq(get_string_width("🧴"), 2)
  assert_eq(get_string_width("🧷"), 2)
  assert_eq(get_string_width("🧹"), 2)
  assert_eq(get_string_width("🧺"), 2)
  assert_eq(get_string_width("🧻"), 2)
  assert_eq(get_string_width("🪣"), 2)
  assert_eq(get_string_width("🧼"), 2)
  assert_eq(get_string_width("🫧"), 2)
  assert_eq(get_string_width("🪥"), 2)
  assert_eq(get_string_width("🧽"), 2)
  assert_eq(get_string_width("🧯"), 2)
  assert_eq(get_string_width("🛒"), 2)
  assert_eq(get_string_width("🚬"), 2)
  assert_eq(get_string_width("⚰️"), 2)
  assert_eq(get_string_width("⚰"), 1)
  assert_eq(get_string_width("🪦"), 2)
  assert_eq(get_string_width("⚱️"), 2)
  assert_eq(get_string_width("⚱"), 1)
  assert_eq(get_string_width("🧿"), 2)
  assert_eq(get_string_width("🪬"), 2)
  assert_eq(get_string_width("🗿"), 2)
  assert_eq(get_string_width("🪧"), 2)
  assert_eq(get_string_width("🪪"), 2)
  assert_eq(get_string_width("🏧"), 2)
  assert_eq(get_string_width("🚮"), 2)
  assert_eq(get_string_width("🚰"), 2)
  assert_eq(get_string_width("♿"), 2)
  assert_eq(get_string_width("🚹"), 2)
  assert_eq(get_string_width("🚺"), 2)
  assert_eq(get_string_width("🚻"), 2)
  assert_eq(get_string_width("🚼"), 2)
  assert_eq(get_string_width("🚾"), 2)
  assert_eq(get_string_width("🛂"), 2)
  assert_eq(get_string_width("🛃"), 2)
  assert_eq(get_string_width("🛄"), 2)
  assert_eq(get_string_width("🛅"), 2)
  assert_eq(get_string_width("⚠️"), 2)
  assert_eq(get_string_width("⚠"), 1)
  assert_eq(get_string_width("🚸"), 2)
  assert_eq(get_string_width("⛔"), 2)
  assert_eq(get_string_width("🚫"), 2)
  assert_eq(get_string_width("🚳"), 2)
  assert_eq(get_string_width("🚭"), 2)
  assert_eq(get_string_width("🚯"), 2)
  assert_eq(get_string_width("🚱"), 2)
  assert_eq(get_string_width("🚷"), 2)
  assert_eq(get_string_width("📵"), 2)
  assert_eq(get_string_width("🔞"), 2)
  assert_eq(get_string_width("☢️"), 2)
  assert_eq(get_string_width("☢"), 1)
  assert_eq(get_string_width("☣️"), 2)
  assert_eq(get_string_width("☣"), 1)
  assert_eq(get_string_width("⬆️"), 2)
  assert_eq(get_string_width("⬆"), 1)
  assert_eq(get_string_width("↗️"), 2)
  assert_eq(get_string_width("↗"), 1)
  assert_eq(get_string_width("➡️"), 2)
  assert_eq(get_string_width("➡"), 1)
  assert_eq(get_string_width("↘️"), 2)
  assert_eq(get_string_width("↘"), 1)
  assert_eq(get_string_width("⬇️"), 2)
  assert_eq(get_string_width("⬇"), 1)
  assert_eq(get_string_width("↙️"), 2)
  assert_eq(get_string_width("↙"), 1)
  assert_eq(get_string_width("⬅️"), 2)
  assert_eq(get_string_width("⬅"), 1)
  assert_eq(get_string_width("↖️"), 2)
  assert_eq(get_string_width("↖"), 1)
  assert_eq(get_string_width("↕️"), 2)
  assert_eq(get_string_width("↕"), 1)
  assert_eq(get_string_width("↔️"), 2)
  assert_eq(get_string_width("↔"), 1)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 49" {
  assert_eq(get_string_width("↩️"), 2)
  assert_eq(get_string_width("↩"), 1)
  assert_eq(get_string_width("↪️"), 2)
  assert_eq(get_string_width("↪"), 1)
  assert_eq(get_string_width("⤴️"), 2)
  assert_eq(get_string_width("⤴"), 1)
  assert_eq(get_string_width("⤵️"), 2)
  assert_eq(get_string_width("⤵"), 1)
  assert_eq(get_string_width("🔃"), 2)
  assert_eq(get_string_width("🔄"), 2)
  assert_eq(get_string_width("🔙"), 2)
  assert_eq(get_string_width("🔚"), 2)
  assert_eq(get_string_width("🔛"), 2)
  assert_eq(get_string_width("🔜"), 2)
  assert_eq(get_string_width("🔝"), 2)
  assert_eq(get_string_width("🛐"), 2)
  assert_eq(get_string_width("⚛️"), 2)
  assert_eq(get_string_width("⚛"), 1)
  assert_eq(get_string_width("🕉️"), 2)
  assert_eq(get_string_width("🕉"), 1)
  assert_eq(get_string_width("✡️"), 2)
  assert_eq(get_string_width("✡"), 1)
  assert_eq(get_string_width("☸️"), 2)
  assert_eq(get_string_width("☸"), 1)
  assert_eq(get_string_width("☯️"), 2)
  assert_eq(get_string_width("☯"), 1)
  assert_eq(get_string_width("✝️"), 2)
  assert_eq(get_string_width("✝"), 1)
  assert_eq(get_string_width("☦️"), 2)
  assert_eq(get_string_width("☦"), 1)
  assert_eq(get_string_width("☪️"), 2)
  assert_eq(get_string_width("☪"), 1)
  assert_eq(get_string_width("☮️"), 2)
  assert_eq(get_string_width("☮"), 1)
  assert_eq(get_string_width("🕎"), 2)
  assert_eq(get_string_width("🔯"), 2)
  assert_eq(get_string_width("🪯"), 2)
  assert_eq(get_string_width("♈"), 2)
  assert_eq(get_string_width("♉"), 2)
  assert_eq(get_string_width("♊"), 2)
  assert_eq(get_string_width("♋"), 2)
  assert_eq(get_string_width("♌"), 2)
  assert_eq(get_string_width("♍"), 2)
  assert_eq(get_string_width("♎"), 2)
  assert_eq(get_string_width("♏"), 2)
  assert_eq(get_string_width("♐"), 2)
  assert_eq(get_string_width("♑"), 2)
  assert_eq(get_string_width("♒"), 2)
  assert_eq(get_string_width("♓"), 2)
  assert_eq(get_string_width("⛎"), 2)
  assert_eq(get_string_width("🔀"), 2)
  assert_eq(get_string_width("🔁"), 2)
  assert_eq(get_string_width("🔂"), 2)
  assert_eq(get_string_width("▶️"), 2)
  assert_eq(get_string_width("▶"), 1)
  assert_eq(get_string_width("⏩"), 2)
  assert_eq(get_string_width("⏭️"), 2)
  assert_eq(get_string_width("⏭"), 1)
  assert_eq(get_string_width("⏯️"), 2)
  assert_eq(get_string_width("⏯"), 1)
  assert_eq(get_string_width("◀️"), 2)
  assert_eq(get_string_width("◀"), 1)
  assert_eq(get_string_width("⏪"), 2)
  assert_eq(get_string_width("⏮️"), 2)
  assert_eq(get_string_width("⏮"), 1)
  assert_eq(get_string_width("🔼"), 2)
  assert_eq(get_string_width("⏫"), 2)
  assert_eq(get_string_width("🔽"), 2)
  assert_eq(get_string_width("⏬"), 2)
  assert_eq(get_string_width("⏸️"), 2)
  assert_eq(get_string_width("⏸"), 1)
  assert_eq(get_string_width("⏹"), 1)
  assert_eq(get_string_width("⏺️"), 2)
  assert_eq(get_string_width("⏺"), 1)
  assert_eq(get_string_width("⏏️"), 2)
  assert_eq(get_string_width("⏏"), 1)
  assert_eq(get_string_width("🎦"), 2)
  assert_eq(get_string_width("🔅"), 2)
  assert_eq(get_string_width("🔆"), 2)
  assert_eq(get_string_width("📶"), 2)
  assert_eq(get_string_width("🛜"), 2)
  assert_eq(get_string_width("📳"), 2)
  assert_eq(get_string_width("📴"), 2)
  assert_eq(get_string_width("♀️"), 2)
  assert_eq(get_string_width("♀"), 1)
  assert_eq(get_string_width("♂️"), 2)
  assert_eq(get_string_width("♂"), 1)
  assert_eq(get_string_width("⚧️"), 2)
  assert_eq(get_string_width("⚧"), 1)
  assert_eq(get_string_width("✖️"), 2)
  assert_eq(get_string_width("✖"), 1)
  assert_eq(get_string_width("➕"), 2)
  assert_eq(get_string_width("➖"), 2)
  assert_eq(get_string_width("➗"), 2)
  assert_eq(get_string_width("🟰"), 2)
  assert_eq(get_string_width("♾️"), 2)
  assert_eq(get_string_width("♾"), 1)
  assert_eq(get_string_width("‼️"), 2)
  assert_eq(get_string_width("‼"), 1)
  assert_eq(get_string_width("⁉️"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 50" {
  assert_eq(get_string_width("⁉"), 1)
  assert_eq(get_string_width("❓"), 2)
  assert_eq(get_string_width("❔"), 2)
  assert_eq(get_string_width("❕"), 2)
  assert_eq(get_string_width("❗"), 2)
  assert_eq(get_string_width("〰️"), 2)
  assert_eq(get_string_width("〰"), 2)
  assert_eq(get_string_width("💱"), 2)
  assert_eq(get_string_width("💲"), 2)
  assert_eq(get_string_width("⚕️"), 2)
  assert_eq(get_string_width("⚕"), 1)
  assert_eq(get_string_width("♻️"), 2)
  assert_eq(get_string_width("♻"), 1)
  assert_eq(get_string_width("⚜️"), 2)
  assert_eq(get_string_width("⚜"), 1)
  assert_eq(get_string_width("🔱"), 2)
  assert_eq(get_string_width("📛"), 2)
  assert_eq(get_string_width("🔰"), 2)
  assert_eq(get_string_width("⭕"), 2)
  assert_eq(get_string_width("✅"), 2)
  assert_eq(get_string_width("☑️"), 2)
  assert_eq(get_string_width("☑"), 1)
  assert_eq(get_string_width("✔️"), 2)
  assert_eq(get_string_width("✔"), 1)
  assert_eq(get_string_width("❌"), 2)
  assert_eq(get_string_width("❎"), 2)
  assert_eq(get_string_width("➰"), 2)
  assert_eq(get_string_width("➿"), 2)
  assert_eq(get_string_width("〽️"), 2)
  assert_eq(get_string_width("〽"), 2)
  assert_eq(get_string_width("✳️"), 2)
  assert_eq(get_string_width("✳"), 1)
  assert_eq(get_string_width("✴️"), 2)
  assert_eq(get_string_width("✴"), 1)
  assert_eq(get_string_width("❇️"), 2)
  assert_eq(get_string_width("❇"), 1)
  assert_eq(get_string_width("©️"), 2)
  assert_eq(get_string_width("©"), 1)
  assert_eq(get_string_width("®️"), 2)
  assert_eq(get_string_width("®"), 1)
  assert_eq(get_string_width("™️"), 2)
  assert_eq(get_string_width("™"), 1)
  assert_eq(get_string_width("🫟"), 2)
  assert_eq(get_string_width("#️⃣"), 2)
  assert_eq(get_string_width("#⃣"), 1)
  assert_eq(get_string_width("0️⃣"), 2)
  assert_eq(get_string_width("0⃣"), 1)
  assert_eq(get_string_width("1️⃣"), 2)
  assert_eq(get_string_width("1⃣"), 1)
  assert_eq(get_string_width("2️⃣"), 2)
  assert_eq(get_string_width("2⃣"), 1)
  assert_eq(get_string_width("3️⃣"), 2)
  assert_eq(get_string_width("3⃣"), 1)
  assert_eq(get_string_width("4️⃣"), 2)
  assert_eq(get_string_width("4⃣"), 1)
  assert_eq(get_string_width("5️⃣"), 2)
  assert_eq(get_string_width("5⃣"), 1)
  assert_eq(get_string_width("6️⃣"), 2)
  assert_eq(get_string_width("6⃣"), 1)
  assert_eq(get_string_width("7️⃣"), 2)
  assert_eq(get_string_width("7⃣"), 1)
  assert_eq(get_string_width("8️⃣"), 2)
  assert_eq(get_string_width("8⃣"), 1)
  assert_eq(get_string_width("9️⃣"), 2)
  assert_eq(get_string_width("9⃣"), 1)
  assert_eq(get_string_width("🔟"), 2)
  assert_eq(get_string_width("🔠"), 2)
  assert_eq(get_string_width("🔡"), 2)
  assert_eq(get_string_width("🔢"), 2)
  assert_eq(get_string_width("🔣"), 2)
  assert_eq(get_string_width("🔤"), 2)
  assert_eq(get_string_width("🅰️"), 2)
  assert_eq(get_string_width("🅰"), 1)
  assert_eq(get_string_width("🆎"), 2)
  assert_eq(get_string_width("🅱️"), 2)
  assert_eq(get_string_width("🅱"), 1)
  assert_eq(get_string_width("🆑"), 2)
  assert_eq(get_string_width("🆒"), 2)
  assert_eq(get_string_width("🆓"), 2)
  assert_eq(get_string_width("ℹ️"), 2)
  assert_eq(get_string_width("ℹ"), 1)
  assert_eq(get_string_width("🆔"), 2)
  assert_eq(get_string_width("Ⓜ"), 1)
  assert_eq(get_string_width("🆕"), 2)
  assert_eq(get_string_width("🆖"), 2)
  assert_eq(get_string_width("🅾️"), 2)
  assert_eq(get_string_width("🅾"), 1)
  assert_eq(get_string_width("🆗"), 2)
  assert_eq(get_string_width("🅿️"), 2)
  assert_eq(get_string_width("🅿"), 1)
  assert_eq(get_string_width("🆘"), 2)
  assert_eq(get_string_width("🆙"), 2)
  assert_eq(get_string_width("🆚"), 2)
  assert_eq(get_string_width("🈁"), 2)
  assert_eq(get_string_width("🈂️"), 2)
  assert_eq(get_string_width("🈂"), 2)
  assert_eq(get_string_width("🈷️"), 2)
  assert_eq(get_string_width("🈷"), 2)
  assert_eq(get_string_width("🈶"), 2)
  assert_eq(get_string_width("🈯"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 51" {
  assert_eq(get_string_width("🉐"), 2)
  assert_eq(get_string_width("🈹"), 2)
  assert_eq(get_string_width("🈚"), 2)
  assert_eq(get_string_width("🈲"), 2)
  assert_eq(get_string_width("🉑"), 2)
  assert_eq(get_string_width("🈸"), 2)
  assert_eq(get_string_width("🈴"), 2)
  assert_eq(get_string_width("🈳"), 2)
  assert_eq(get_string_width("㊗️"), 2)
  assert_eq(get_string_width("㊗"), 2)
  assert_eq(get_string_width("㊙️"), 2)
  assert_eq(get_string_width("㊙"), 2)
  assert_eq(get_string_width("🈺"), 2)
  assert_eq(get_string_width("🈵"), 2)
  assert_eq(get_string_width("🔴"), 2)
  assert_eq(get_string_width("🟠"), 2)
  assert_eq(get_string_width("🟡"), 2)
  assert_eq(get_string_width("🟢"), 2)
  assert_eq(get_string_width("🔵"), 2)
  assert_eq(get_string_width("🟣"), 2)
  assert_eq(get_string_width("🟤"), 2)
  assert_eq(get_string_width("⚫"), 2)
  assert_eq(get_string_width("⚪"), 2)
  assert_eq(get_string_width("🟥"), 2)
  assert_eq(get_string_width("🟧"), 2)
  assert_eq(get_string_width("🟨"), 2)
  assert_eq(get_string_width("🟩"), 2)
  assert_eq(get_string_width("🟦"), 2)
  assert_eq(get_string_width("🟪"), 2)
  assert_eq(get_string_width("🟫"), 2)
  assert_eq(get_string_width("⬛"), 2)
  assert_eq(get_string_width("⬜"), 2)
  assert_eq(get_string_width("◼️"), 2)
  assert_eq(get_string_width("◼"), 1)
  assert_eq(get_string_width("◻️"), 2)
  assert_eq(get_string_width("◻"), 1)
  assert_eq(get_string_width("◾"), 2)
  assert_eq(get_string_width("◽"), 2)
  assert_eq(get_string_width("▪️"), 2)
  assert_eq(get_string_width("▪"), 1)
  assert_eq(get_string_width("▫️"), 2)
  assert_eq(get_string_width("▫"), 1)
  assert_eq(get_string_width("🔶"), 2)
  assert_eq(get_string_width("🔷"), 2)
  assert_eq(get_string_width("🔸"), 2)
  assert_eq(get_string_width("🔹"), 2)
  assert_eq(get_string_width("🔺"), 2)
  assert_eq(get_string_width("🔻"), 2)
  assert_eq(get_string_width("💠"), 2)
  assert_eq(get_string_width("🔘"), 2)
  assert_eq(get_string_width("🔳"), 2)
  assert_eq(get_string_width("🔲"), 2)
  assert_eq(get_string_width("🏁"), 2)
  assert_eq(get_string_width("🚩"), 2)
  assert_eq(get_string_width("🎌"), 2)
  assert_eq(get_string_width("🏴"), 2)
  assert_eq(get_string_width("🏳️"), 2)
  assert_eq(get_string_width("🏳"), 1)
  assert_eq(get_string_width("🏳️‍🌈"), 2)
  assert_eq(get_string_width("🏳‍🌈"), 3)
  assert_eq(get_string_width("🏳️‍⚧️"), 2)
  assert_eq(get_string_width("🏳‍⚧️"), 3)
  assert_eq(get_string_width("🏳️‍⚧"), 3)
  assert_eq(get_string_width("🏳‍⚧"), 2)
  assert_eq(get_string_width("🏴‍☠️"), 2)
  assert_eq(get_string_width("🏴‍☠"), 3)
  assert_eq(get_string_width("🇦🇨"), 2)
  assert_eq(get_string_width("🇦🇩"), 2)
  assert_eq(get_string_width("🇦🇪"), 2)
  assert_eq(get_string_width("🇦🇫"), 2)
  assert_eq(get_string_width("🇦🇬"), 2)
  assert_eq(get_string_width("🇦🇮"), 2)
  assert_eq(get_string_width("🇦🇱"), 2)
  assert_eq(get_string_width("🇦🇲"), 2)
  assert_eq(get_string_width("🇦🇴"), 2)
  assert_eq(get_string_width("🇦🇶"), 2)
  assert_eq(get_string_width("🇦🇷"), 2)
  assert_eq(get_string_width("🇦🇸"), 2)
  assert_eq(get_string_width("🇦🇹"), 2)
  assert_eq(get_string_width("🇦🇺"), 2)
  assert_eq(get_string_width("🇦🇼"), 2)
  assert_eq(get_string_width("🇦🇽"), 2)
  assert_eq(get_string_width("🇦🇿"), 2)
  assert_eq(get_string_width("🇧🇦"), 2)
  assert_eq(get_string_width("🇧🇧"), 2)
  assert_eq(get_string_width("🇧🇩"), 2)
  assert_eq(get_string_width("🇧🇪"), 2)
  assert_eq(get_string_width("🇧🇫"), 2)
  assert_eq(get_string_width("🇧🇬"), 2)
  assert_eq(get_string_width("🇧🇭"), 2)
  assert_eq(get_string_width("🇧🇮"), 2)
  assert_eq(get_string_width("🇧🇯"), 2)
  assert_eq(get_string_width("🇧🇱"), 2)
  assert_eq(get_string_width("🇧🇲"), 2)
  assert_eq(get_string_width("🇧🇳"), 2)
  assert_eq(get_string_width("🇧🇴"), 2)
  assert_eq(get_string_width("🇧🇶"), 2)
  assert_eq(get_string_width("🇧🇷"), 2)
  assert_eq(get_string_width("🇧🇸"), 2)
  assert_eq(get_string_width("🇧🇹"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 52" {
  assert_eq(get_string_width("🇧🇻"), 2)
  assert_eq(get_string_width("🇧🇼"), 2)
  assert_eq(get_string_width("🇧🇾"), 2)
  assert_eq(get_string_width("🇧🇿"), 2)
  assert_eq(get_string_width("🇨🇦"), 2)
  assert_eq(get_string_width("🇨🇨"), 2)
  assert_eq(get_string_width("🇨🇩"), 2)
  assert_eq(get_string_width("🇨🇫"), 2)
  assert_eq(get_string_width("🇨🇬"), 2)
  assert_eq(get_string_width("🇨🇭"), 2)
  assert_eq(get_string_width("🇨🇮"), 2)
  assert_eq(get_string_width("🇨🇰"), 2)
  assert_eq(get_string_width("🇨🇱"), 2)
  assert_eq(get_string_width("🇨🇲"), 2)
  assert_eq(get_string_width("🇨🇳"), 2)
  assert_eq(get_string_width("🇨🇴"), 2)
  assert_eq(get_string_width("🇨🇵"), 2)
  assert_eq(get_string_width("🇨🇶"), 2)
  assert_eq(get_string_width("🇨🇷"), 2)
  assert_eq(get_string_width("🇨🇺"), 2)
  assert_eq(get_string_width("🇨🇻"), 2)
  assert_eq(get_string_width("🇨🇼"), 2)
  assert_eq(get_string_width("🇨🇽"), 2)
  assert_eq(get_string_width("🇨🇾"), 2)
  assert_eq(get_string_width("🇨🇿"), 2)
  assert_eq(get_string_width("🇩🇪"), 2)
  assert_eq(get_string_width("🇩🇬"), 2)
  assert_eq(get_string_width("🇩🇯"), 2)
  assert_eq(get_string_width("🇩🇰"), 2)
  assert_eq(get_string_width("🇩🇲"), 2)
  assert_eq(get_string_width("🇩🇴"), 2)
  assert_eq(get_string_width("🇩🇿"), 2)
  assert_eq(get_string_width("🇪🇦"), 2)
  assert_eq(get_string_width("🇪🇨"), 2)
  assert_eq(get_string_width("🇪🇪"), 2)
  assert_eq(get_string_width("🇪🇬"), 2)
  assert_eq(get_string_width("🇪🇭"), 2)
  assert_eq(get_string_width("🇪🇷"), 2)
  assert_eq(get_string_width("🇪🇸"), 2)
  assert_eq(get_string_width("🇪🇹"), 2)
  assert_eq(get_string_width("🇪🇺"), 2)
  assert_eq(get_string_width("🇫🇮"), 2)
  assert_eq(get_string_width("🇫🇯"), 2)
  assert_eq(get_string_width("🇫🇰"), 2)
  assert_eq(get_string_width("🇫🇲"), 2)
  assert_eq(get_string_width("🇫🇴"), 2)
  assert_eq(get_string_width("🇫🇷"), 2)
  assert_eq(get_string_width("🇬🇦"), 2)
  assert_eq(get_string_width("🇬🇧"), 2)
  assert_eq(get_string_width("🇬🇩"), 2)
  assert_eq(get_string_width("🇬🇪"), 2)
  assert_eq(get_string_width("🇬🇫"), 2)
  assert_eq(get_string_width("🇬🇬"), 2)
  assert_eq(get_string_width("🇬🇭"), 2)
  assert_eq(get_string_width("🇬🇮"), 2)
  assert_eq(get_string_width("🇬🇱"), 2)
  assert_eq(get_string_width("🇬🇲"), 2)
  assert_eq(get_string_width("🇬🇳"), 2)
  assert_eq(get_string_width("🇬🇵"), 2)
  assert_eq(get_string_width("🇬🇶"), 2)
  assert_eq(get_string_width("🇬🇷"), 2)
  assert_eq(get_string_width("🇬🇸"), 2)
  assert_eq(get_string_width("🇬🇹"), 2)
  assert_eq(get_string_width("🇬🇺"), 2)
  assert_eq(get_string_width("🇬🇼"), 2)
  assert_eq(get_string_width("🇬🇾"), 2)
  assert_eq(get_string_width("🇭🇰"), 2)
  assert_eq(get_string_width("🇭🇲"), 2)
  assert_eq(get_string_width("🇭🇳"), 2)
  assert_eq(get_string_width("🇭🇷"), 2)
  assert_eq(get_string_width("🇭🇹"), 2)
  assert_eq(get_string_width("🇭🇺"), 2)
  assert_eq(get_string_width("🇮🇨"), 2)
  assert_eq(get_string_width("🇮🇩"), 2)
  assert_eq(get_string_width("🇮🇪"), 2)
  assert_eq(get_string_width("🇮🇱"), 2)
  assert_eq(get_string_width("🇮🇲"), 2)
  assert_eq(get_string_width("🇮🇳"), 2)
  assert_eq(get_string_width("🇮🇴"), 2)
  assert_eq(get_string_width("🇮🇶"), 2)
  assert_eq(get_string_width("🇮🇷"), 2)
  assert_eq(get_string_width("🇮🇸"), 2)
  assert_eq(get_string_width("🇮🇹"), 2)
  assert_eq(get_string_width("🇯🇪"), 2)
  assert_eq(get_string_width("🇯🇲"), 2)
  assert_eq(get_string_width("🇯🇴"), 2)
  assert_eq(get_string_width("🇯🇵"), 2)
  assert_eq(get_string_width("🇰🇪"), 2)
  assert_eq(get_string_width("🇰🇬"), 2)
  assert_eq(get_string_width("🇰🇭"), 2)
  assert_eq(get_string_width("🇰🇮"), 2)
  assert_eq(get_string_width("🇰🇲"), 2)
  assert_eq(get_string_width("🇰🇳"), 2)
  assert_eq(get_string_width("🇰🇵"), 2)
  assert_eq(get_string_width("🇰🇷"), 2)
  assert_eq(get_string_width("🇰🇼"), 2)
  assert_eq(get_string_width("🇰🇾"), 2)
  assert_eq(get_string_width("🇰🇿"), 2)
  assert_eq(get_string_width("🇱🇦"), 2)
  assert_eq(get_string_width("🇱🇧"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 53" {
  assert_eq(get_string_width("🇱🇨"), 2)
  assert_eq(get_string_width("🇱🇮"), 2)
  assert_eq(get_string_width("🇱🇰"), 2)
  assert_eq(get_string_width("🇱🇷"), 2)
  assert_eq(get_string_width("🇱🇸"), 2)
  assert_eq(get_string_width("🇱🇹"), 2)
  assert_eq(get_string_width("🇱🇺"), 2)
  assert_eq(get_string_width("🇱🇻"), 2)
  assert_eq(get_string_width("🇱🇾"), 2)
  assert_eq(get_string_width("🇲🇦"), 2)
  assert_eq(get_string_width("🇲🇨"), 2)
  assert_eq(get_string_width("🇲🇩"), 2)
  assert_eq(get_string_width("🇲🇪"), 2)
  assert_eq(get_string_width("🇲🇫"), 2)
  assert_eq(get_string_width("🇲🇬"), 2)
  assert_eq(get_string_width("🇲🇭"), 2)
  assert_eq(get_string_width("🇲🇰"), 2)
  assert_eq(get_string_width("🇲🇱"), 2)
  assert_eq(get_string_width("🇲🇲"), 2)
  assert_eq(get_string_width("🇲🇳"), 2)
  assert_eq(get_string_width("🇲🇴"), 2)
  assert_eq(get_string_width("🇲🇵"), 2)
  assert_eq(get_string_width("🇲🇶"), 2)
  assert_eq(get_string_width("🇲🇷"), 2)
  assert_eq(get_string_width("🇲🇸"), 2)
  assert_eq(get_string_width("🇲🇹"), 2)
  assert_eq(get_string_width("🇲🇺"), 2)
  assert_eq(get_string_width("🇲🇻"), 2)
  assert_eq(get_string_width("🇲🇼"), 2)
  assert_eq(get_string_width("🇲🇽"), 2)
  assert_eq(get_string_width("🇲🇾"), 2)
  assert_eq(get_string_width("🇲🇿"), 2)
  assert_eq(get_string_width("🇳🇦"), 2)
  assert_eq(get_string_width("🇳🇨"), 2)
  assert_eq(get_string_width("🇳🇪"), 2)
  assert_eq(get_string_width("🇳🇫"), 2)
  assert_eq(get_string_width("🇳🇬"), 2)
  assert_eq(get_string_width("🇳🇮"), 2)
  assert_eq(get_string_width("🇳🇱"), 2)
  assert_eq(get_string_width("🇳🇴"), 2)
  assert_eq(get_string_width("🇳🇵"), 2)
  assert_eq(get_string_width("🇳🇷"), 2)
  assert_eq(get_string_width("🇳🇺"), 2)
  assert_eq(get_string_width("🇳🇿"), 2)
  assert_eq(get_string_width("🇴🇲"), 2)
  assert_eq(get_string_width("🇵🇦"), 2)
  assert_eq(get_string_width("🇵🇪"), 2)
  assert_eq(get_string_width("🇵🇫"), 2)
  assert_eq(get_string_width("🇵🇬"), 2)
  assert_eq(get_string_width("🇵🇭"), 2)
  assert_eq(get_string_width("🇵🇰"), 2)
  assert_eq(get_string_width("🇵🇱"), 2)
  assert_eq(get_string_width("🇵🇲"), 2)
  assert_eq(get_string_width("🇵🇳"), 2)
  assert_eq(get_string_width("🇵🇷"), 2)
  assert_eq(get_string_width("🇵🇸"), 2)
  assert_eq(get_string_width("🇵🇹"), 2)
  assert_eq(get_string_width("🇵🇼"), 2)
  assert_eq(get_string_width("🇵🇾"), 2)
  assert_eq(get_string_width("🇶🇦"), 2)
  assert_eq(get_string_width("🇷🇪"), 2)
  assert_eq(get_string_width("🇷🇴"), 2)
  assert_eq(get_string_width("🇷🇸"), 2)
  assert_eq(get_string_width("🇷🇺"), 2)
  assert_eq(get_string_width("🇷🇼"), 2)
  assert_eq(get_string_width("🇸🇦"), 2)
  assert_eq(get_string_width("🇸🇧"), 2)
  assert_eq(get_string_width("🇸🇨"), 2)
  assert_eq(get_string_width("🇸🇩"), 2)
  assert_eq(get_string_width("🇸🇪"), 2)
  assert_eq(get_string_width("🇸🇬"), 2)
  assert_eq(get_string_width("🇸🇭"), 2)
  assert_eq(get_string_width("🇸🇮"), 2)
  assert_eq(get_string_width("🇸🇯"), 2)
  assert_eq(get_string_width("🇸🇰"), 2)
  assert_eq(get_string_width("🇸🇱"), 2)
  assert_eq(get_string_width("🇸🇲"), 2)
  assert_eq(get_string_width("🇸🇳"), 2)
  assert_eq(get_string_width("🇸🇴"), 2)
  assert_eq(get_string_width("🇸🇷"), 2)
  assert_eq(get_string_width("🇸🇸"), 2)
  assert_eq(get_string_width("🇸🇹"), 2)
  assert_eq(get_string_width("🇸🇻"), 2)
  assert_eq(get_string_width("🇸🇽"), 2)
  assert_eq(get_string_width("🇸🇾"), 2)
  assert_eq(get_string_width("🇸🇿"), 2)
  assert_eq(get_string_width("🇹🇦"), 2)
  assert_eq(get_string_width("🇹🇨"), 2)
  assert_eq(get_string_width("🇹🇩"), 2)
  assert_eq(get_string_width("🇹🇫"), 2)
  assert_eq(get_string_width("🇹🇬"), 2)
  assert_eq(get_string_width("🇹🇭"), 2)
  assert_eq(get_string_width("🇹🇯"), 2)
  assert_eq(get_string_width("🇹🇰"), 2)
  assert_eq(get_string_width("🇹🇱"), 2)
  assert_eq(get_string_width("🇹🇲"), 2)
  assert_eq(get_string_width("🇹🇳"), 2)
  assert_eq(get_string_width("🇹🇴"), 2)
  assert_eq(get_string_width("🇹🇷"), 2)
  assert_eq(get_string_width("🇹🇹"), 2)
}

///|
// Reference: unicode-width 0.2.2 tests/tests.rs and tests/emoji-test.txt.
test "Unicode text reference group 54" {
  assert_eq(get_string_width("🇹🇻"), 2)
  assert_eq(get_string_width("🇹🇼"), 2)
  assert_eq(get_string_width("🇹🇿"), 2)
  assert_eq(get_string_width("🇺🇦"), 2)
  assert_eq(get_string_width("🇺🇬"), 2)
  assert_eq(get_string_width("🇺🇲"), 2)
  assert_eq(get_string_width("🇺🇳"), 2)
  assert_eq(get_string_width("🇺🇸"), 2)
  assert_eq(get_string_width("🇺🇾"), 2)
  assert_eq(get_string_width("🇺🇿"), 2)
  assert_eq(get_string_width("🇻🇦"), 2)
  assert_eq(get_string_width("🇻🇨"), 2)
  assert_eq(get_string_width("🇻🇪"), 2)
  assert_eq(get_string_width("🇻🇬"), 2)
  assert_eq(get_string_width("🇻🇮"), 2)
  assert_eq(get_string_width("🇻🇳"), 2)
  assert_eq(get_string_width("🇻🇺"), 2)
  assert_eq(get_string_width("🇼🇫"), 2)
  assert_eq(get_string_width("🇼🇸"), 2)
  assert_eq(get_string_width("🇽🇰"), 2)
  assert_eq(get_string_width("🇾🇪"), 2)
  assert_eq(get_string_width("🇾🇹"), 2)
  assert_eq(get_string_width("🇿🇦"), 2)
  assert_eq(get_string_width("🇿🇲"), 2)
  assert_eq(get_string_width("🇿🇼"), 2)
  assert_eq(get_string_width("🏴󠁧󠁢󠁥󠁮󠁧󠁿"), 2)
  assert_eq(get_string_width("🏴󠁧󠁢󠁳󠁣󠁴󠁿"), 2)
}
